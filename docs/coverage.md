# Coverage manifest

Authoritative **per assertion** (ADR-0003 findings 4 and 6). A requirement
spanning layers is split into one assertion per layer; an assertion whose
machinery lands in a later slice is `pending` and **never green**. Layers:
`pgTAP` · `multi-session` · `HTTP` · `storage` · `worker` · `review`
(properties enforced by reading the migrations, not by a runtime probe).
Tests live in `supabase/tests/`; `NNN:x–y` means that file's pgTAP case
numbers.

Statuses: **green** (asserted, passing) · **pending** (staged to the named
slice; the absent machinery is non-callable today) · **review** (process
property, verified at review gates).

## 1A — schema and privilege invariants (TSD §2.1–§2.3, §3.13)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| INV-01 | Every table in `public` has RLS enabled **and** forced | §2.1 | pgTAP | 1A | green | 001:31 |
| INV-02 | `hc.access_level` ordinal sequence ascending | §2.2, ADR-0002 c8 | pgTAP | 1A | green | 001:19 |
| INV-03 | Every §2.2 enum exists with exact ordered labels | §2.2 | pgTAP | 1A | green | 001:19–28 |
| INV-04 | `hc.all_domains()` = `enum_range(null::hc.domain)`; `hc.dom()` round-trips it | §2.2, §3.3 | pgTAP | 1A | green | 001:29–30 |
| INV-05 | Every FK between two circle-scoped tables is circle-consistent | §2.1 | pgTAP | 1A | green | 001:37 (catalog sweep) + fk_ok cases 32–34, 38–39, 49–50 |
| INV-06 | AC-ADMIN-3 declarative: composite FK to `accounts(id, kind)`; admin refused from membership (23503); member cannot flip to admin (23503) | §2.3, ADR-0002 c7 | pgTAP | 1A | green | 001:32 · 006:17–18 |
| INV-07 | One membership row per subject (partial unique) | §2.3 | pgTAP | 1A | green | 001:35 |
| INV-08 | `admin_users.mfa_enrolled_at` NOT NULL (AC-ADMIN-5) | §2.3 | pgTAP | 1A | green | 001:36 |
| INV-09 | `access_log` rejects UPDATE and DELETE **both ways** (privilege absent + unconditional trigger) | §2.8 | pgTAP | 1A | green | 001:41–43, 46–48 |
| INV-10 | Denial entries name no object (`denial_names_no_object`, AC-PPL-7) | §2.8 | pgTAP | 1A | green | 001:44 |
| INV-11 | `seq` unique per circle; gapless; chain linked; `entry_hash` recomputes as the **complete v1 canonical** — every immutable evidentiary column incl. session/request/correction linkage (collapsed_* excluded as mutable presentation, by design); the declaration FK is DEFERRABLE | §2.8; ADR-0004 F1/F2 | pgTAP | 1A | green | 001:45, 51, 54 · 006:13–16 |
| INV-12 | Invite token hash unique (single-use anchor) | §2.3 | pgTAP | 1A | green | 001:40 |
| INV-13 | No unexpected PUBLIC grants: tables zero; functions zero; deny-by-default ACLs in place for runner and `hc_internal` | ADR-0003 f8 | pgTAP | 1A | green | 001:13–14 · 002:5, 12 |
| INV-14 | Two-way privilege snapshot: our five roles hold exactly the expected inventory; anon/hc_pipeline/hc_admin hold **nothing** | §1.2, §3.7 | pgTAP | 1A | green | 002:10–11 |
| INV-15 | `hc_internal` policy list pinned to its exact sixteen names (cannot grow silently) | §3.4 | pgTAP | 1A | green | 002:13 |

## 1A — definer-function invariants (plan §definer; ADR-0003 finding 8)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| DEF-01 | Non-login owner: every `hc` function owned by `hc_internal`; NOLOGIN | plan | pgTAP | 1A | green | 001:4 · 002:1 |
| DEF-02 | SECURITY DEFINER only where required (exactly six) | plan | pgTAP | 1A | green | 002:3 |
| DEF-03 | `search_path` pinned on every definer (+ `hc.log`) | plan | pgTAP | 1A | green | 002:4 |
| DEF-04 | PUBLIC EXECUTE revoked per-function **and** by global default privileges | plan | pgTAP | 1A | green | 002:5 · 001:13–14 |
| DEF-05 | Exact overload inventory — no unexpected executable overloads | plan | pgTAP | 1A | green | 002:2 |
| DEF-06 | Explicit grants to named callers, exactly | plan | pgTAP | 1A | green | 002:6 |
| DEF-07 | No request-path/admin role a member of (or SET ROLE into) `hc_internal` (postgres = documented maintenance exemption) | plan | pgTAP | 1A | green | 002:7 |
| DEF-08 | No unreviewed dynamic SQL (1A allowlist is empty) | plan | pgTAP | 1A | green | 002:8 |
| DEF-09 | No caller-selectable identity outside `hc.ctx_for()`/`hc.grant_vectors()`; both executable by nothing request-facing | §3.2 | pgTAP | 1A | green | 004:22–25 · 007:54–55 |
| DEF-10 | Uniform result shapes: unauthorized vs nonexistent indistinguishable (`P0001` pair) | §3.9 posture | pgTAP | 1A | green | 007:41, 45 |
| DEF-11 | Owner and caller schema USAGE verified | plan | pgTAP | 1A | green | 001:16–18 · 002:9 |
| DEF-12 | Creation, ownership, revoke, grants atomic within one migration | plan | review | 1A | review | migration files M2, M6–M9 |

## 1A — `hc.visible_at()` truth table (TSD §3.3, §3.13, A.4)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| VIS-01 | Clause order 1→5 asserted independently (1-before-5, 2-before-4/5, 3-before-5, 3-not-falling-through) | §3.3 | pgTAP | 1A | green | 003:10, 12, 21, 16 |
| VIS-02 | Unresolved/NULL/empty taint → hidden at every rung below manage (AC-PERM-9); manage-on-five clears | §3.3, A.4 | pgTAP | 1A | green | 003:15–22 |
| VIS-03 | Freeze closes custodian and coordinators; share does not lift; missing/NULL `frozen` key freezes (AC-PERM-11) | §3.3, §3.8 | pgTAP | 1A | green | 003:11–14 |
| VIS-04 | A share cannot manufacture subject context (revoked-grants + kept-share case) | §3.3, A.4 | pgTAP | 1A | green | 003:9–10 |
| VIS-05 | care_circle ceiling incl. null owner; share passes the ceiling to view (AC-TASK-5) | §3.3 | pgTAP | 1A | green | 003:23–26 |
| VIS-06 | Share widens ONE named object to view — never a domain, never another id/type, never past manage | §3.3 | pgTAP | 1A | green | 003:27–31 |
| VIS-07 | Ladder = min over taint as containment; `hidden` in taint ⇒ object does not exist (AC-PERM-6/7) | §3.1 | pgTAP | 1A | green | 003:5–8, 32–36 |
| VIS-08 | `hc.dom()` refuses unknown domain names (22P02) | §2.2 | pgTAP | 1A | green | 003:4 |
| VIS-09 | Mutation test: freeze-clause removal → 003:11–14 red; lineage-clause removal → 003:16–21 red; restored green | plan | review | 1A | review | recorded in PR description |

## 1A — authorization context (TSD §3.2)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| CTX-01 | A row for EVERY reachable subject; all-hidden subjects present-but-empty | §3.2 | pgTAP | 1A | green | 004:4–5, 10 |
| CTX-02 | Removed membership contributes nothing (circles and subjects) | §3.2 | pgTAP | 1A | green | 004:2, 6 |
| CTX-03 | Cumulative vectors: manage ⊆ view ⊆ summary ⊆ log | §3.2 | pgTAP | 1A | green | 004:7–11 |
| CTX-04 | `frozen`: open ⇒ whole circle; narrowed unresolved ⇒ named subject (other reopens); unnarrowed unresolved ⇒ whole circle; dismissed clears | §3.8, ADR-0001/0003 | pgTAP | 1A | green | 004:16–21 |
| CTX-05 | `hc.ctx_for(account)` ≡ `hc.ctx()` for that account; memberless ctx empty-but-well-formed | §3.2 | pgTAP | 1A | green | 004:26–27 |
| CTX-06 | ctx `shares` key present-and-empty (placeholder) | plan d2 | pgTAP | 1A | green | 004:15 |
| CTX-07 | ctx `shares` populated from `object_shares`; ctx body per §3.2 verbatim (subjects entries additionally carry `cap`, ADR-0005 D5) | §3.2 | pgTAP | 1B | green | 015:6–7, 14–16 |

## 1A — identity-table RLS (§3.4 shape; Appendix A.1 subset)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| RLS-01 | A member reads own circle's rows; foreign circle returns **zero rows** (indistinguishable from nonexistence) | §3.4, AC-PERM-2 posture | pgTAP | 1A | green | 005:1–6 |
| RLS-02 | accounts self-row only; own grants only; admin_users and invites unreadable | plan d3 | pgTAP | 1A | green | 005:5–6, 10–11 |
| RLS-03 | Write privilege absent for authenticated on every 1A table (42501) | §3.7 posture | pgTAP | 1A | green | 005:7–9 |
| RLS-04 | anon holds nothing (42501) | §1.2 | pgTAP | 1A | green | 005:14 |
| RLS-05 | `hc_admin` gets `permission denied for table` — the A.1 distinguished failure mode, privilege absent, no policy consulted (AC-ADMIN-1) | A.1 | pgTAP | 1A | green | 005:15–17 |
| RLS-06 | A.1 five per-domain negatives against record tables, each with a distinguishing positive control; the five hc_admin permission-denied variants | A.1 | pgTAP | 1B | green | 010:31–45 |
| RLS-07 | A.3 twenty ordered-pair matrix, generated from one rule (1B channels: direct select = every count, hc.presence; search/notification/export staged — RLS-11) | A.3 | pgTAP | 1B | green | 017:1–5 |
| RLS-08 | A.2 revoked live session: the NEXT query on the SAME connection returns zero rows | A.2 | multi-session | 1B | green | test:concurrency case 4 |
| RLS-09 | A.2 invite token replayed after acceptance creates nothing (AC-PERM-4) — sequential AND racing (a revocation committing mid-wait defeats the acceptance the same way) | A.2, §5.10 | pgTAP + multi-session | 2A | green | 037:25–27 · concurrency 27 |
| RLS-10 | A.2 artifact-route 404 indistinguishability; pre-revocation URL fails | A.2, §1.3 | HTTP | 4B | green | **flipped 4B B7** (049 pre-discharged NOTHING — ADR-0018 Q-G; the route's own discipline is the proof): ONE byte-identical 404 across no-session/ghost/unauthorized/not-clean/quarantined; the signed URL created AND consumed server-side; evidence before bytes — tests/routes/artifact.test.ts · tests/hc/artifacts.test.ts · LIVE at HTTP depth in e2e/ingestion.spec.ts (the below-cliff member's probe answers the ghost's exact bytes; every request re-runs the steps from live tables, AC-PPL-4). **ROUND-17 ANNOTATION (ADR-0025 D8, S-2):** the below-cliff 404 byte-identity probe lives at `e2e/ingestion.spec.ts:432-434`, inside `:400` — the leg that has never executed at this tree (skipped in all three 6A gate runs behind a `describe.serial` failure). The route-level and unit halves stand; **this live half is owed re-observation at 6B** with UXA-01's. **RE-OBSERVED at 6B (2026-08-24, TARGETED RUN at `ae0ab05` — never a gate result):** the below-cliff leg executed alone by title and passed (1 passed, 45.3 s), the 404 byte-identity probe answering the ghost's exact bytes against the leg's own freshly-provisioned arrival — re-observed at the post-fire tree `3a84683` (1 passed, 49.6 s, first invocation). The full-gate result stays owed to the 6B increment head · **ROUND-18 (ADR-0027 D2, F-1): A STALL IS NO LONGER RENDERED AS AN ABSENCE, AND 404 ≡ 403 IS UNTOUCHED.** D20 collapsed a budget overrun on `liveSessionClaims`, `readableArtifact` and `readableRendition` into this route's ONE 404, arguing the caller learns nothing either way. The caller learns the one thing that matters — **whether to retry** — and under a systemic stall every member was told their documents were NOT FOUND during an availability incident. Those three reads, and the `?text=1` path's signed-URL hop and byte read, now answer a named **`read_timeout` / `storage_timeout` (504)**; `MachineReadText` already maps 404 → "No machine-read text is stored for this page" and every other non-ok → "couldn't be loaded right now", so the sentence a person reads is now the true one. **The MAIN BYTE PATH deliberately keeps its ONE 404** (D18 argued that shape explicitly, the review does not attack it, and it renders no sentence to anybody), and **ABSENCE keeps the ONE 404 everywhere**. The no-oracle property is PINNED, not argued: a row that exists and a row that does not answer byte-identically once the read overruns, because a timeout is decided by the clock and not by the row — `tests/routes/artifact.test.ts`, "THE NO-ORACLE CONTROL". The four-way byte-identity case is unchanged and still passes · **ROUND-19 (ADR-0028 D1, F-2): A SESSION THE ROUTE COULD NOT READ IS NO LONGER THE ONE 404 EITHER.** D2 named the OVERRUN, and that is all it could name: a `getUser()` that fails FAST — a refused socket, a 5xx from Kong, a 429 on `token_refresh` — never reaches the budget at all. It came back `null`, and `null` was this route's ONE 404, so an auth-server incident told a family their documents were NOT FOUND. It now answers a named **`session_unavailable` (503)**, and the no-oracle property is PINNED the same way: the fault is decided by the auth server's health and never by the row, so a row that EXISTS and a row that does NOT answer byte-identically — `tests/routes/artifact.test.ts`, "THE NO-ORACLE CONTROL for it". **SIGNED-OUT keeps the ONE 404**, and the four-way byte-identity case is still unchanged and still passes · **ROUND-20 SIGN-OFF (ADR-0027 D22, ruling on D20 item 3): the full-gate result is NO LONGER OWED — it is IN HAND.** The 38-leg gate came back **GREEN, 38/38** at `1066e2d` (run `r5`, `38 passed (5.1m)`; run `r4` is INVALID and is not a tally), and only `docs/` has changed since, so the green proves the increment head. This clause therefore **upgrades from targeted-run evidence to GATE evidence**. **This is NOT a row flip and no cell changes colour** — the status column is untouched, per ADR-0025 S16.7 |

## 1A — circle creation (TSD §2.3, AC-AUTH-6)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| CIR-01 | Universal, driven from **circles** (a circle with no declaration cannot pass invisibly): every circle carries one declaration per subject at exactly seq 1..n, before any other event; the seq-1 sweep retained as supplement | §2.3; ADR-0004 F4 | pgTAP | 1A | green | 006:3–4, 23 |
| CIR-02 | Declaration is **durably subject-bound**: preallocated subject id under the deferred FK, plus name/custodian/date in detail | §2.3, PRD §7.5; ADR-0004 F1 | pgTAP | 1A | green | 006:5 |
| CIR-03 | Subject-member rows: no account, founder custodian, coordinator tier (AC-PPL-3) | §2.3 | pgTAP | 1A | green | 006:7 |
| CIR-04 | Founder manage×5 per subject; each subject-member manage×5 on own record | §2.3, PRD §7.5 | pgTAP | 1A | green | 006:9–10 |
| CIR-05 | Two-subject cap refused in-function (deliberately not a fake CHECK) | §2.3 note | pgTAP | 1A | green | 006:11–12 |
| CIR-06 | Attach-parent-login: one UPDATE, row still subject-bound, custodianship retained (§3.13 regression) | §2.3, auth §6 | pgTAP | 1A | green | 006:19–20 |
| CIR-07 | Unauthenticated refusal; EXECUTE to authenticated only | plan | pgTAP | 1A | green | 006:1, 21–22 |

## 1A — freeze (TSD §2.3, §3.8; ADR-0001 as amended by ADR-0003)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| FRZ-01 | Open freeze cannot name a subject (`freezes_open_is_whole_circle`) | ADR-0001 | pgTAP | 1A | green | 007:1 |
| FRZ-02 | No non-open state without adjudication metadata (`freezes_outcome_is_adjudicated`) | ADR-0003 f2 | pgTAP | 1A | green | 007:2 |
| FRZ-03 | No narrowing without recorded assessment (`freezes_narrowing_is_assessed`) — direct AND through the function | ADR-0003 f2/f3 | pgTAP | 1A | green | 007:3, 42 |
| FRZ-04 | One OPEN freeze per circle; per-circle scope; re-freeze after a finding allowed | PRD §7.5 | pgTAP | 1A | green | 007:5–6, 49–50 |
| FRZ-05 | Claims disposition ⟷ attachment check; every report recorded incl. rate-limited | ADR-0003 f1 | pgTAP | 1A | green | 007:7–9, 33, 37–38, 40 |
| FRZ-06 | Second claimant attaches to the same freeze — report never swallowed | ADR-0003 f1 | pgTAP | 1A | green | 007:31–33 |
| FRZ-07 | Rate limits key on the **canonical contact** (hc.contact_key; verbatim form retained): dismissed-prior claimant refused; 3/claimant/circle/30d; 10/circle/30d; case/whitespace variants share one budget | PRD §7.5; ADR-0004 F3/R3 | pgTAP | 1A | green | 007:35–40, 51, 57–59 |
| FRZ-08 | Direct INSERT/UPDATE/DELETE refused on both tables from EVERY request-path entry point (42501, before any policy) | §2.3 | pgTAP | 1A | green | 007:10–22 |
| FRZ-09 | `hc_internal` bounded: freezes never deleted; claims ledger append-only even for the writer role | §2.3 | pgTAP | 1A | green | 007:23–26 |
| FRZ-10 | Writer proof rests on catalogs: exact table-privilege snapshot + zero triggers on both freeze tables + no dynamic SQL; the prosrc inventory scan is the labelled supplemental check | §2.3; ADR-0004 F5 | pgTAP | 1A | green | 002:8, 10–11 · 007:52–53, 56 |
| FRZ-11 | Freeze request/claim/adjudication are access-log events; claimant PII never in the log | PRD §7.5 | pgTAP | 1A | green | 007:30, 34, 46 |
| FRZ-12 | Adjudication outcomes: dismissed/upheld clear `frozen`; narrowed unresolved holds named subject, reopens the other; unnarrowed holds whole circle | §3.8 | pgTAP | 1A | green | 004:16–21 · 007:41–48 |
| FRZ-13 | Unresolved read-only carve-out: coordinators-not-objected-to at frozen=false + cap=view (visible_at applies least(result, cap) LAST); null objected_to ⇒ everyone closed | §3.8, ADR-0005 D2/D5 | pgTAP | 1B | green | 016:1–17 · **AMENDED BY MARKER (ADR-0043 D2, slice 8's close-out — RULED 2026-09-03; nothing above is rewritten and this row does not move): the carve-out is READ-ONLY BY INTENT, and one write definer does not honour that.** This row's own first words say *read-only*, as does `20260815230009`'s header. `hc.visible_at` applies `cap` as `least(…)`, so a coordinator who is not the objected-to member, under an **unresolved** freeze, resolves to `view` — which is exactly `hc.claim_task`'s floor, so she can TAKE A TASK while the circle is frozen. `assign_task`, `complete_task` and `snooze_task` are **NOT** exposed: each raises `freeze_active` from an explicit `state in ('open','unresolved')` test that never consults `hc.grant_vectors`, so a cap cannot lower it. **The READ carve-out this row asserts is unchanged and stays green on 016:1–17; the WRITE exposure is carried by FRZ-17 and `docs/owed.md` OW-27** |
| FRZ-14 | `hc.approve_proposal()` refuses under freeze — open AND unresolved, named signature freeze_active | §3.7 | pgTAP | 1B | green | 013:17–18 |
| FRZ-15 | Frozen arrival parked (claim AND advance refuse, store exempt); NO retry consumption (zero leases minted, asserted across a mid-wait race); terminal-transition refusal by the same predicate; outbox re-enqueue on dismissal IN the adjudication transaction (upheld/unresolved write nothing); sweeper skips parked work for re-queueing, exhaustion, stuck and queue-age; lost outbox message recovered by the ordinary sweeper | §4, A.5 | pgTAP + multi-session | 1C | green | 020:27–33 · 022:18–20 · 026:1–16 · concurrency 13–14; the pgmq relay itself is RLY-01 (pending) |
| FRZ-16a | Freeze suspends INVITES at circle level: creation AND acceptance refuse with the named freeze_active (open or unresolved, however narrowed); a freeze committing while an acceptance waits defeats it. Built as SUSPEND per TSD §2.3 — PRD §7.5 says "voided"; divergence flagged (ADR-0012) | §2.3, PRD §7.5 | pgTAP + multi-session | 2A | green | 037:15–17 · concurrency 26 |
| FRZ-16b | Freeze suspends exports and deletions at circle level | §2.3 | pgTAP | **2+** | **pending** | no export or deletion surface exists through 2A (DEL-01/G5 companions); split from FRZ-16 (ADR-0012) |

## 1A — performance (TSD §3.2, §3.12; ADR-0002 n2; ADR-0003 f9)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| PRF-01 | Every textual `(select hc.ctx())` reference is an InitPlan node; any SubPlan is hashed (one-shot); zero per-row plans | ADR-0002 n2 | pgTAP | 1A | green | 008:1–4 |
| PRF-02 | **Measured** ctx() executions == textual references (1 and 2), never per row, over a visible scan at volume | ADR-0003 f9 | pgTAP | 1A | green | 008:5–7 |
| PRF-03 | Volume wall clock under the 250 ms O(rows) tripwire (measured 2.9 / 11.2 ms local; §1.8 page budget 1.5 s) | §3.12, §1.8 | pgTAP | 1A | green | 008:8–9 |
| PRF-04 | InitPlan/LEFT-JOIN null-extension regression against the real search schema: measured ctx() == 6 textual refs over a 300-row §7.2 scan, never per row; the summary caller's row arrives NULL-extended, not filtered | §7.2, ADR-0002 c1; ADR-0009 D7 | pgTAP | 1D | green | 029:10, 22–23 · 000 keeps the synthetic pin |

## Round-5 conditions (ADR-0004)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| UID-EQ | `hc.uid()` ≡ `auth.uid()`: absent claims, claim.sub, legacy claims, conflicting (claim.sub wins), malformed → same error class | ADR-0004 R1 | pgTAP | 1A | green | 002:14–18 |
| PIN-01 | PostgREST exposed schemas pinned to [public, graphql_public] — `hc` never exposed; exposure gated on live-denial test or fixed image | ADR-0004 R2 | CI | 1A | green | scripts/check-exposed-schemas.mjs + ci.yml step |

## Platform regression net (pre-1A)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| PLT-01 | The 15 Postgres behavioural assumptions from the Step-2 spike | ADR-0002 | pgTAP | init | green | 000:1–15 |
| PLT-02 | Lock ordering: raw opposite-order repro deadlocks (exactly one 40P01 ×3); the same contention through approve serializes on the per-circle advisory lock (×10, zero deadlocks) | ADR-0002 n3, ADR-0005 D6 | multi-session | 1B | green | test:concurrency cases 1–2 |
| PLT-03 | ALTER TYPE … ADD VALUE upgrade-path fixture: M3 ADDs claimed/exhausted and uses neither; M4 is first use; 021 pins the eight labels in order, proves the committed pair casts, and probes 55P04 LIVE against the real enum; the CI upgrade leg applies the split increment to the shipped base on every run | ADR-0002 n5, ADR-0003 f7 | pgTAP + CI | 1C | green | 021:1–3 · ci.yml upgrade leg |
| PLT-04 | Function-ACL-denial segfault on this image (backend signal 11) — closure asserted via catalog until fixed upstream | this slice | review | 1A | review | round-5 packet, upstream report |

## 1B — record schema and write path (TSD §2.4–§2.6, §3.5–§3.7; ADR-0005)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| ING-01 | §2.4 prerequisite tables (arrivals, proposals, approval_attempts, proposal_commits): shapes, checks, one-live lineage index, circle-consistent composites; fail-closed for every request-path role (1B posture; 1C opened exactly the staged reach — ING-02, machinery grants in 002's snapshot) | §2.4, ADR-0005 D1 | pgTAP | 1B | green | 009:1–39 |
| REC-01 | Shared tenancy/provenance/taint block NOT NULL on all five record tables; approved_* carry NO default | §2.5 | pgTAP | 1B | green | 010:7–8 |
| REC-02 | dsc pins circle AND subject via the ONE composite FK (carries the cascade; the §2.1 sweep forbids the circle-blind second FK) | §2.5, §2.1 | pgTAP | 1B | green | 010:15–18 · 001:37 |
| REC-03 | profile_facts supersession: one current per (subject, field); supersede-then-insert is the only path; approval marks old and links old↔new | §2.5, AC-INBOX-6 | pgTAP | 1B | green | 010:19–22 · 013:21–24 |
| REC-04 | §2.7 temporal shapes: three accepted, conflation refused | §2.7 | pgTAP | 1B | green | 010:23–26 |
| REC-05 | dsc: ZERO grants in both directions for every role incl. hc_internal until 1D | §2.5, plan | pgTAP | 1B | green | 010:44–48 · 002:19 |
| GRD-01 | Provenance quartet immutable on all five record tables + proposals (per-column on documents, 16-probe sweep elsewhere) | §3.7, N2 | pgTAP | 1B | green | 011:1–5 |
| GRD-02 | Taint never shrinks; marker row-scoped to new.id; false→true resolved only under the marker; true→false always open | §3.7, PRD §7.6 | pgTAP | 1B | green | 011:6–15 |
| GRD-03 | Trigger inventory exact: guard ×6 + claim ×5; dsc none | §3.7, §2.4 | pgTAP | 1B | green | 011:17 · 002:20 |
| TNT-01 | link_provenance: ONE refusal shape for missing/cross-circle/cross-subject; unsupported endpoint types refused; cycle refused BEFORE the write | §2.6 | pgTAP | 1B | green | 012:21–25 |
| TNT-02 | Delta growth: child at link, GRANDCHILD at propagate (the stale-recompute regression), diamond idempotent | §2.6, §3.13 | pgTAP | 1B | green | 012:11–20 |
| TNT-03 | Depth cap: applied under it, MARKED at it, nothing silently widened past it | §2.6, AC-PERM-9 | pgTAP | 1B | green | 012:26–29 |
| TNT-04 | Walk failure ⇒ marked-and-committed; partial updates rolled back to the savepoint | §2.6 mech 1 | pgTAP | 1B | green | 012:30–32 |
| TNT-05 | Reclassify: manage-on-current-taint; path-complete (a second path retains its domain); resolved restored only by completed recompute; audience_changed names both audiences; nonexistent/unauthorized ONE shape | §2.6, AC-DOC-6, DEF-10 | pgTAP | 1B | green | 012:33–41 |
| TNT-06 | Sweep marks dangling and cross-circle edges (detector 3) | §2.6 mech 3 | pgTAP | 1B | green | 012:43–45 |
| TNT-07 | Growth-vs-shrink serialize on the per-circle advisory lock (blocking asserted from pg_locks; serial-equivalent result); an aborted link writes nothing | §2.6, ADR-0005 D6 | multi-session | 1B | green | test:concurrency case 3 |
| APR-01 | Full approval: claim → object with provenance → edges → log → recorded result, one transaction; idempotent replay returns the STORED result | §3.7, §2.4, AC-INBOX-12 | pgTAP | 1B | green | 013:3–12 |
| APR-02 | Refusals: version drift distinct; nonexistent/unauthorized/decided/key-misuse ONE shape; the care ceiling binds the writer; high-risk unconfirmed refused | §3.7, DEF-10, PRD §6.4 | pgTAP | 1B | green | 013:13–16, 19, 28–29 |
| APR-03 | Taint at write = the D7 union (own ∪ drafted ∪ parents' CURRENT taints), manage on the union; drafted covers parents (1C drafting contract) | ADR-0005 D7 as amended by ADR-0006 | pgTAP | 1B | green | 013:5 · 018:12–13 |
| APR-06 | Step-up, as amended by 2A M2 (A3: "§5.7 replaces this guard"): a non-null `p_step_up_token` is now VALIDATED — operation/target/actor-bound, consumed in-transaction; an invalid, foreign or replayed token refuses; null still approves; signature stays §3.7-verbatim | ADR-0006 F6; TSD annex A3; ADR-0012 | pgTAP | 1B→2A | green | 018:1–2 (invalid refused) · 036:24–28 |
| APR-07 | Idempotency hardening: replay actor-bound (another actor gets approval_refused, not the stored result); key bounded 1..200, empty refused | ADR-0006 P5 | pgTAP | 1B | green | 018:3–6 |
| APR-08 | Duplicate payload parents collapse to ONE edge; no raw 23505 escapes the definer | ADR-0006 P5 | pgTAP | 1B | green | 018:7–8 |
| APR-09 | Drift refusal (D7 amended): parents' CURRENT union beyond own ∪ drafted → `proposal_taint_changed`, post-authorization (order pinned: authorization outranks it); the refusal writes nothing | ADR-0006 F8/Q4 | pgTAP | 1B | green | 018:9–11 |
| APR-04 | Manual entry through the same function: source_arrival_id null, provenance of the same shape | §3.7, AC-TL-2 | pgTAP | 1B | green | 013:25–27 |
| APR-05 | Unclaimed writes die in BOTH places: the insert policy (hc_internal, 42501) and the deferred claim trigger (everything else, P0001; SECURITY DEFINER per M10) | §3.7, §2.4 | pgTAP | 1B | green | 013:30–31 |
| REV-01 | revise_object: manage at write time; per-type content allowlist; provenance/taint unaddressable; profile_facts supersede-only; revision row (before/after, sequenced) in-transaction | §3.7 | pgTAP | 1B | green | 014:1–13 |
| SHR-01 | share_object: one-transaction validation (existence, circle+subject agreement, live grantee, granter manage); ONE refusal shape; logged; end-to-end widening of ONE object; no propagation; revocation closes on the next query; a committed freeze refuses at the next evaluation | §2.5, §3.6, AC-PERM-10 | pgTAP | 1B | green | 015:1–16 · 018:14 |
| RAC-01 | R-rule, freeze vs approval: a freeze committing while an approval waits on the per-circle lock defeats it WITH the named FRZ-14 signature (`freeze_active`, nothing written) | ADR-0006 F1; TSD annex A4 | multi-session | 1B | green | test:concurrency case 5 |
| RAC-02 | R-rule, grant revocation and membership removal vs approval: either committing mid-wait defeats the approval (ctx evaluates under the lock) | ADR-0006 F1 | multi-session | 1B | green | test:concurrency cases 6–7 |
| RAC-03 | R-rule, transitions vs revision: authorization binds to the version the write touches — a stale-taint edit is refused and the row unchanged; a freeze committing mid-wait defeats the revision | ADR-0006 F1 | multi-session | 1B | green | test:concurrency cases 8, 10 |
| RAC-04 | R-rule, the shrink path: reclassify re-reads and authorizes UNDER the lock — an actor without manage on the grown taint is refused | ADR-0006 F1 | multi-session | 1B | green | test:concurrency case 9 |
| PRS-01 | presence(): ids/dates/types only — no title column EXISTS; log on every taint domain per row; circle pre-filter bounds arbitrary p_subject; freeze closes it | §3.5 | pgTAP | 1B | green | 016:18–24 |
| WRT-01 | Writer allowlist (catalog-based): exact grantee×privilege inventory for documents + dsc from information_schema.role_table_grants; exact pg_trigger inventory | plan | pgTAP | 1B | green | 002:19–20 |
| PRF-05 | Record policies at volume: two InitPlans per §3.4 two-clause policy, zero SubPlans; measured ctx executions == 2 over a 2,000-row scan (the deterministic O(rows) tripwire); wall clock RECORDED (~0.95 s quiet, 1:4 load variance — round-6 question) | §3.12, ADR-0003 f9 | pgTAP | 1B | green | 008:10–14 |
| MUT-01 | Mutation: guard_row shrink-guard removed ⇒ 011:6, 7, 9, 14 red by name; restored green | plan | review | 1B | review | round-6 packet |
| MUT-02 | Mutation: tasks_internal_write with_check dropped ⇒ 013:31 red; the deferred-trigger branch (013:30) stays green — the belts are independent; restored green | plan | review | 1B | review | round-6 packet |
| UPG-01 | Upgrade leg: pinned 1A baseline (10 migrations @ 03a0c12) materialised in a TEMP worktree; ONLY the 1B migrations applied on top; both suites green; worktree removed | plan | review | 1B | review | round-6 packet + addendum |
| UPG-02 | The upgrade rehearsal and db:verify are repeatable CI merge gates: exact two-way migration-state check after every reset (clean and base), increment applied via `supabase migration up`, both suites re-run against the upgraded database, schema lint failing on warnings | ADR-0006 F5 | CI | 1B | green | ci.yml + scripts/verify-migration-state.mjs |

## 1B → staged forward (new pending rows)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| ING-02 | arrivals/proposals §3.4 read policies: summary reaches the arrival row (fail-closed all-domain taint; column grant excludes auth_detail and current_lease_id, so `select *` refuses for everyone); view reaches auth_detail through hc.arrival_auth_detail (DEF-10 one shape); proposals read at MANAGE over their own drafted taint (per-proposal selectivity); freeze closes all three; hc_admin privilege-absent; two-InitPlan/zero-SubPlan policy shape | §3.4, ADR-0007 | pgTAP | 1C | green | 025:1–7, 11–20 |
| ING-03 | A.1 health paired half, asserted in ONE member: the summary member who CAN read the arrival row gets ZERO extraction rows; view resolves them; single-domain view does not | A.1 | pgTAP | 1C | green | 025:8–10 |
| RLS-11a | A.3 search channel: twenty generated pairs absent across ALL THREE search relations; positive control | A.3; ADR-0009 D9 | pgTAP | 1D | green | 029:19–21 |
| RLS-11b | A.3 remaining channels: the send-time notification check, export | A.3 | pgTAP+ | **2+** | **pending** | surfaces land there |
| DSC-01 | dsc view-level read policy (EXISTS on the document at view — the §7.2 LEFT JOIN is the level decision); the search writer allowlist FINALIZED: hc_internal S/I/U + authenticated SELECT, DELETE for nobody | §2.5, §2.11 | pgTAP | 1D | green | 029:2–18 · 028:5 · 002:14, 19–20 · 010:44–48 |
| TNT-08 | Request-path callers landed: reclassify EXECUTE to authenticated, authorizing through visible_at UNDER the lock (freeze/cap/ceiling bind; unresolved needs manage-on-five; DEF-10 one shape); sweep scheduling via hc.run_taint_sweep (hc_pipeline); freeze and revocation mid-wait each defeat the reclassify | §2.6; ADR-0009 D5/D6 | pgTAP + multi-session | 1D | green | 032:1–6 · concurrency 24–25 |
| SHR-02 | Share revocation surfaces: hc.assign_task / unassign revokes assignment-created shares | §3.6, AC-TASK-7 | pgTAP + app + e2e | **7A/7B** | green | **pgTAP half GREEN (7A M1, `20260829120001`)**: 066:32–41 drive SHR-02 BOTH WAYS — a FOREIGN share (`created_by_assignment_of` NULL) is neither duplicated, adopted nor revoked by the assignment or its unassign (066:34–37); a coordinator KEEPS one by id and it survives live, still naming the assignment it came from, while the task share is revoked and the grantee loses the task on her next query (066:38–41); keeping is a coordinator's explicit decision and every kept id must be THIS assignment's live share, else the whole call refuses (066:43–44); a manage-holder unassigns without a keep list (066:45); reassign closes the prior assignment's shares and its written instruction in one transaction (066:31). concurrency 52, both orders. History: `pending` since 1D; retagged 1C/1D → 2+ (ADR-0009 D9) → **7** at the slice-7 plan gate (2026-08-28). **App half owed (7B B2)**: the assign flow and the unassign control, checked from the grantee's live context · **APP HALF GREEN (7B B2/B4)**: the assign flow by path 2 and the unassign control, checked from the grantee's live context — `e2e/record.spec.ts` "unassign withdraws the share, checked from her context (SHR-02, AC-TASK-7; path 2 behind the §5.7 step-up)": the original readable to her and two live shares after the share, 404 and zero live after take-back; `tests/hc/tasks.test.ts` the same from Omar's context. The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| PRF-06 | The quantitative gate ran and BREACHED (page p95 535–1,680 ms vs 250; search_broad 3,490 ms vs 2,500), so the breach clause executed: ladder→jsonb containment, visible_at→one inline-friendly expression, three page indexes. Post-rewrite the GATE — warm 25-run p95 — is ALL BOUNDS MET (page p95 ≤ 216 ms; scans p95 ≤ 1,915 ms); the "margins ≥ 3×" claim is the SCAN queries', not the page tripwire's. The cold leg is a report-only diagnostic (exits 0 by design): cold warm-up can transiently exceed the 250 ms page tripwire on a cold or contended host (round-8 reviewer observed 280–329 ms) while staying far inside PRD §13.2's 1.5 s p95 page budget (round-8 F2); truth table + 033 grid green over the rewrite | ADR-0006 F7/Q6; ADR-0009 D7 as amended by ADR-0010 | pgTAP + bench | 1D | green | 033:1–12 · scripts/bench/prf06.mjs (tables in round-8 packet) |
| MNL-01 | Manual entry end-to-end (model pinned ADR-0006 Q12): hc.create_manual_proposal creates the synthetic manual-channel arrival WITH its proposal in ONE transaction (any refusal leaves neither row, incl. across a mid-wait freeze race); hc.assert_manual_flag makes flag/channel disagreement unrepresentable BOTH ways; manual documents refused; approval flows through hc.approve_proposal with null source_arrival_id (APR-04 as built); create_arrival refuses the manual channel | ADR-0006 F9/Q12; ADR-0007 | pgTAP + multi-session | 1C | green | 024:1–17 · 020:6 · concurrency 15. **NARROWED AT ROUND 17 (6A M6, packet Q-B, ADR-0025 D3): manual entry now requires `view` over all five domains**, in `hc.visible_at`'s LADDER form — the arrival is created in the same transaction, so it can carry no `object_shares` row and the share rung is dead there. It closes the seam 6A M2 opened (create what you cannot approve, pinned open at 060:16 and now inverted). **The cost is on the record**: a below-cliff member loses manual entry ENTIRELY rather than losing only the approve half, and `024:14` — a GREEN assertion — was inverted with its argument written at the site. 064:14–16 drives it both ways |
| OPS-01 | RULED AND LANDED (ADR-0009 D6): scheduler identity = the RLY-01 worker as hc_pipeline (sweeper_pass per minute; run_taint_sweep nightly, recorded in hc.sweep_runs); retry = idempotent next tick; alerting = admin_meta.sweep_health (page on findings > 0; page on last_run_at > 24 h — the missing row IS the signal); bounds = worker statement_timeout 60 s + §13.3 scale; window ≤ 24 h; posture over-taint (findings marked unresolved) | ADR-0006 F10/Q10; ADR-0009 D6 | pgTAP + review | 1D | green | 032:7–13 · ADR-0009 D6 |

## 1C — ingestion state machine (TSD §2.4, §4; ADR-0007)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| ING-04 | §2.4 pipeline tables (arrival_events, pipeline_leases, known_senders, extractions, pipeline_outbox) + hc.reason_codes/hc.stage_budgets seeded: shapes, circle-consistent composites, citation_present structural, live-unique senders, arrivals.current_lease_id circle-consistent, arrival_events append-only, fail-closed staging; DELETE granted to nobody | §2.4, §4.3 | pgTAP | 1C | green | 019:1–40 |
| ING-05 | hc.advance_arrival: the six enumerated outcomes each produced by their own scenario (A.5 row 1); stale_lease in all four shapes with state UNCHANGED and NO event row (the silent-skip regression); the event insert takes already-bound values; reason codes FK-bound — a raw provider string is unrepresentable; cancelled outranks the fence (recorded reorder) | §4.2, A.5 | pgTAP | 1C | green | 020:14–26, 34 |
| ING-06 | hc.create_arrival intake: idempotent on (circle, key) — a replay returns the same id; channels closed (manual/sms refused); children inherit circle AND subject; accept-and-store proceeds under freeze; P5 caps (50 MB, 200 pages, key ≤ 200, auth_detail ≤ 16 KB, address/display/message-id/mime bounds) each refused before any write | §4.1, §4.6, ADR-0006 P5 | pgTAP | 1C | green | 020:2–13, 27 |
| ING-07 | hc.claim_stage: budget claimed before the provider call; claim-while-live refused (stale_lease); expiry transfers ownership (superseded lease marked expired, fenced from publishing — A.5 pgTAP half); exhaustion = terminal state + enumerated reason, no fifth lease; redelivery acks out (already_advanced from the event trail); interpret's in-flight transition at claim, reclaim after mid-flight death without duplicate events; commit-standalone proven two-session | §4.3, A.5 | pgTAP + multi-session | 1C | green | 022:1–24 · concurrency 11–12 |
| ING-08 | Publication is ONE transaction gated by the transition (§4.5): finalize_extraction/finalize_interpretation run the CAS first; a cancellation between the provider returning and finalization leaves ZERO extractions and proposals (A.5 orphan row, single-session AND racing two-session); every P5 refusal (≤200 facts, ≤8 KB values, cited-or-refused, ≤50 proposals, ≤64 KB payloads, ≤20 in-circle parents, own-arrival extraction ids) writes NOTHING and rolls the transition back; write halves owner-only and lease-bound (F6 posture) | §4.5, A.5, ADR-0006 P5 | pgTAP + multi-session | 1C | green | 023:1–5, 7–10, 14–21, 25 · concurrency 16 |
| ING-09 | The 1C drafting contract (APR-03/APR-09 modelled, not fought): hc.draft_proposal sets taint = own_domain ∪ parents' CURRENT taints at draft; a machinery draft approves cleanly through hc.approve_proposal; a conflict draft requires parents and carries their union (A.4 conflict oracle bound); profile_fact without a declared domain refused (D3 fail-closed) | ADR-0005 D7 as amended; A.4 | pgTAP | 1C | green | 023:4–6, 19, 22–24 |
| CNL-01 | hc.cancel_arrival (§4.5): any member who can approve (manage over the all-domain taint); freeze-first Q5 order (named freeze_active); nonexistent/unauthorized ONE shape (DEF-10); cancel_invalid_state post-authorization; records actor + event + closes the lease as cancelled | §4.5 | pgTAP | 1C | green | 023:7, 10–13 |
| SND-01 | hc.sender_recognised: address and domain match case-blind on live rows only; a display name wearing a known address NEVER matches (PRD §4.2.8); the gate claims and advances scanned → extracting/held with enumerated reasons | §4.3, §5.3, AC-INBOX-7 | pgTAP | 1C | green | 020:35 · 022:21 |
| SWP-01 | hc.sweeper_pass (§4.11): expires dead workers' leases; terminalizes budget-spent UNFROZEN arrivals with stated reasons under the R-rule lock in circle order — **re-validating state, stage, live lease, freeze, deletion and budget against the row-locked LIVE row, updating conditionally on the re-read state (round-7 B2)**; lists re-queueables; stuck > 24 h (from last event) excludes human-wait states and parked work; queue-age > 4 h excludes parked; idempotent; steps 3–5 are read-only advisory listings revalidated by claim_stage | §4.11; ADR-0008 B2 | pgTAP + multi-session | 1C | green | 026:1–3, 10–17 · concurrency 17–21 |
| RAC-05 | R-rule extended to pipeline writers: a freeze committing while advance_arrival / claim_stage / create_manual_proposal waits on the per-circle lock defeats each — parked state unchanged, zero leases minted, neither manual row surviving | ADR-0007; ADR-0006 A4 | multi-session | 1C | green | concurrency 13–15 |
| RAC-06 | The writer-by-writer race matrix (round-7 M3 — replaces the general "R-rule extends to every writer" sentence): sweeper vs cancellation / claim-exhaust / freeze / claim+finalize; two sweepers single-terminalization; drain vs drain disjoint; relay-crash vs ack boundary; intake vs intake conflicting AND matching; structural exclusions (adjudication-outbox atomicity, ungated intake) recorded in ADR-0008 | ADR-0008 M3 | multi-session | 1C | green | concurrency 17–23; ADR-0008 §matrix |
| ING-10 | Transition-graph closure (round-7 B1): hc.arrival_transitions is the CLOSED §4.3 stage-exit allowlist (exact seeded set pinned); the CAS refuses skipped stages, backward transitions, terminal revival and WRONG-STAGE leases with invalid_state, mutating nothing; stage-bound controls still advance; §4.7 duplicate edges append with their machinery | §4.2/§4.3 as amended by A6; ADR-0008 B1 | pgTAP | 1C | green | 027:1–10 |
| ING-11 | Intake idempotency identity (round-7 M2): a key replay returns the prior id ONLY when subject/channel/parent/message-id/sender (case-blind) agree; conflicts raise idempotency_conflict writing NOTHING — sequential per-field AND concurrent (the unique-violation loser conflicts, never aliases; a matching concurrent replay still aliases) | §4.1 as amended by A6; ADR-0008 M2 | pgTAP + multi-session | 1C | green | 027:19–26 · concurrency 23 |
| OBX-01 | Outbox delivery contract (round-7 B3): claim/ack at-least-once — an unacked claim past the 300 s window re-delivers; an acked row never does; double-ack idempotent; ack binds to a claim; concurrent drains disjoint (SKIP LOCKED); hc.outbox_ack is hc_pipeline-only; the sweeper stays the backstop; end-to-end pgmq delivery is RLY-01 (pending) | §4.2 as amended by A6; ADR-0008 B3 | pgTAP + multi-session | 1C | green | 027:11–18 · concurrency 22 · 026:7–10 |
| BGT-01 | Budget seeds are PROVISIONAL operational hypotheses (round-7 M5, Q10 overturned): frequency/max-elapsed math and revision criteria in ADR-0008; the no-rapid-burn property asserted — duplicate queue deliveries against a live gate lease consume nothing; per-outcome metrics and pre-exhaustion alerts land behind OPS-01/RLY-01 | ADR-0008 M5 | pgTAP + review | 1C | green | 027:30; ADR-0008 §budgets |
| MUT-03 | Mutation: advance_arrival's freeze clause removed ⇒ 020:28–32 red by name (the whole freeze-parking family); restored green | plan | review | 1C | review | round-7 packet |

## 1C → staged forward (new pending rows)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| RLY-01 | The outbox relay and sweeper scheduler as workers: pgmq delivery of claimed outbox rows (claim/ack, OBX-01) and sweeper re-queue lists; kill-before-transition and outbox-loss end-to-end (the A.5 worker-layer halves). **Scope ruling (ADR-0008 M1): 1C = database state-machine substrate complete; the OPERATIONAL pipeline is NOT complete and is production-disabled until this row lands** | §4.2, §4.11, A.5; ADR-0008 M1 | worker | 4B | green | **flipped 4B B5** — /api/worker/relay (per-minute cron, checked in) drains→enqueues-with-lineage→acks exactly what was sent, failed enqueues re-deliver on the 300 s window; /api/worker/nightly (taint sweep, cache expiry, held-mail expiry, the §11.5 byte purge); the A.5 halves live: tests/hc/relay.test.ts (kill-before-transition; outbox-loss recovery) · tests/routes/relay.test.ts · e2e/ingestion.spec.ts (one relay pass finishing a duplicate resolution). **The production-disabled ruling lifts for store/scan/gate; ACTIVATION stays G4/G7-gated** (Q7 — no real forwarding address exists) |
| UXA-01 | Inbox-surface entry gate (round-7 M4 — D7 approval is CONDITIONAL on it): visibility matrix by role composition; a coordinator-diagnosis guarantee (a status affordance not gated on all five domains, or a documented coordinator grant-shape requirement); the below-cliff representation of "an item is processing" without existence leakage; the share-based disclosure flow. The cliff itself is a pinned fact (manage on 4 of 5 ⇒ zero rows) | ADR-0008 M4; Q6 SETTLED | review | 4B | review | **flipped 4B B6 with the Q6 disposition recorded** (slice-4-plan, ratified as presented): manage-×5 audience by the RLS cliff · below-cliff = zero rows + an empty state that never asserts the world is empty (probed LIVE from a family-tier session, e2e/ingestion.spec.ts) · the coordinator-diagnosis guarantee stands as the recorded requirement on any future coordinator-minting path · hc.share_object is the named disclosure channel. ADR-0019 D12. **ROUND-17 ANNOTATION (ADR-0025 D8, S-2): the LIVE half of this cell has not been observed at the 6A head.** It is `e2e/ingestion.spec.ts:400`, the LAST test in a `test.describe.serial` block, and it was skipped in all three of the 6A gate runs — **it has never executed at this tree at all**. The cell is green from 4B on evidence that did run then and nothing in 6A touches `app/`, `lib/` or `e2e/`; but F12 binds the gate when `supabase/` moves, so this half is **owed re-observation at 6B**, by title, recorded as a targeted run rather than a gate result. **RE-OBSERVED at 6B (2026-08-24, TARGETED RUN at the gate-repairs head `ae0ab05` — never a gate result):** the leg is now SELF-SUFFICIENT under ADR-0025 D8 condition 1 (no `describe.serial`; memoized provisioning; its own arrival, its own ghost-404 baseline) and executed alone by title — `npx playwright test e2e/ingestion.spec.ts -g "below the cliff"` → **1 passed (45.3 s)**, first invocation — and **re-observed at the post-fire tree `3a84683`** (B5's eager fire moved the leg's stable observation to the event log): **1 passed (49.6 s)**, first invocation. The full-gate result stays owed to the 6B increment head · **ROUND-20 SIGN-OFF (ADR-0027 D22, ruling on D20 item 3): the full-gate result is NO LONGER OWED — it is IN HAND.** The 38-leg gate came back **GREEN, 38/38** at `1066e2d` (run `r5`, `38 passed (5.1m)`; run `r4` is INVALID and is not a tally), and only `docs/` has changed since, so the green proves the increment head. This clause therefore **upgrades from targeted-run evidence to GATE evidence**. **This is NOT a row flip and no cell changes colour** — the status column is untouched, per ADR-0025 S16.7 |
| PST-01 | hc.product_state / state_rank / state_label (§4.4): the family-facing vocabulary is EXACTLY PRD §4.2.2's fifteen strings over a total distinct rank order (stuck-below-moving per phase); a parent reports the least-advanced LIVE child — deleted AND cancelled children leave the rollup, no live children ⇒ own state — over the CALLER's visible children only (A.4); DEF-10 one shape incl. the manage-on-4-of-5 cliff and non-member real-vs-ghost byte-identity | §4.4, A.4; ADR-0017 D6 | pgTAP | 4A | green | 046:1–19 |
| CNF-01 | Conflict resolution outcomes (§4.8): use-new/keep/keep-both through hc.approve_proposal — supersession the only path to a current value; keep writes NOTHING (no commit row); keep-both COMMITS the drafted task as the one object (Q9); the idempotency identity includes the outcome; version race + double-approve + same-key-different-outcome pinned | §4.8; Q9 | pgTAP | **5A** | **green** | **flipped at 5A M4** — 054:1–23 · concurrency 39–40 (pending since 1C; the refusal lifted) |
| SND-02 | known_senders accept/revoke member surfaces + held-mail release and the 30-day expiry of unaccepted stranger mail: coordinator-only acceptance (exactly-one-of address\|domain; freeze_active under any freeze); release in the SAME transaction via a real gate lease + the CAS release edge + outbox re-queue (relay = RLY-01, sweeper backstop); revocation immediate on live rows; expiry to nothing_filed reason held_expired, skipping frozen circles, accepted-meanwhile senders, and no-held-event arrivals; acceptance takes the R-rule lock BEFORE its predicates (M8, round-9 F2) — a freeze committing mid-wait defeats it with no sender row, no log entry, nothing released | §5.3–§5.4; round-9 F2 (ADR-0013) | pgTAP + multi-session | 2A | green | 040:1–24 · concurrency 30 |

## 1D — derived & operational surfaces (TSD §2.8–§2.11, §7, §3.9; ADR-0009)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| SRCH-01 | Vectors by trigger in the writing transaction: tsv_summary = title(A)+summary(B) and NEVER extraction text; tasks/timeline one whole-row vector (no column carve-out exists); dsc built in ONE place from the SAME string the snippet is cut from (recomputed equality); extracted_text = the approved extraction values; ocr at weight D, findable, never outranking the title; the rename rebuilds BOTH vectors in the same transaction (A.5); trigger + grant + policy inventories exact; the only dsc-writing trigger lives ON documents (c3 order by construction) | §2.11, §7.1; ADR-0002 c3 | pgTAP | 1D | green | 028:1–29 · 011:17 · 002:19–20 |
| SRCH-02 | The §7.2 query per rung: view matches and snippets from tsv_full/search_text_full incl. TITLE matches (A.5); summary null-extends and falls through to exactly what it may read; body-only terms return zero AND a count identical to a term present nowhere (A.5); log/hidden/non-member/care-ceiling closed; a share widens the ONE named object through search and never propagates; freeze closes the channel | §7.2–§7.6; A.4/A.5 | pgTAP | 1D | green | 029:1–18 |
| LOG-01 | The §2.8 family read: domain-less circle-level entries member-visible (freeze trail stays readable under the freeze); subject entries at ≥ log on the entry's domain via visible_at; a subject entry with no domain fails closed to ALL DOMAINS; a domained entry with no subject fails closed to ALL SUBJECTS — ≥ log on that domain for every live subject, empty set dark, freeze closing it through the same one function (round-8 F4); non-members and removed members read nothing; freeze closes subject rows for the coordinator too | §2.8, §10.5; ADR-0009 D1 as amended by ADR-0010 | pgTAP | 1D | green | 030:1–9, 20–21 · 034:6–12 · **APP HALF GREEN (7C C5, PPL-04)**: the surface reads through this policy and asserts it subtracts nothing — the reader's projection rendered and printed, driven both ways live (`tests/hc/people.test.ts`: the health grant entry is not Marisol's to see) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** the 7C app-half addendum's *"asserts it subtracts nothing"* is FALSE — `lib/hc/people#accessLog` is `order by seq desc limit 300` and the page asks for 300, undisclosed (R4/F-3). The row stayed green on its **pgTAP/1D layer** throughout. **THE APP HALF IS RE-EARNED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021` (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override)** — in narrower words than it had. `accessLog`'s docstring no longer says it *"simply orders what the policy already decided"*: it orders and then TRUNCATES at `limit`, newest first, so `seq` 1 is the first row to go, and the docstring now tells its caller to ask for one more than it means to show and to DISCLOSE the window. The page does exactly that. **What is still owed is the CURSOR — OW-26, home slice 8** — so this half is green on *"the reader's access is the filter, and the surface says which entries it is not showing"*, never again on *"subtracts nothing"* · **AMENDED BY MARKER (8C U2, `2f2c509`, ADR-0042 D7 — nothing above is rewritten): THE CURSOR LANDED.** Every sentence above stays exactly as it was written, including *"What is still owed is the CURSOR — OW-26, home slice 8"*, because that was true when it was written and the record of what was owed is the point. What is no longer true is the OWING: `lib/hc/people#accessLog` takes a `before` cursor, the page walks it, and `seq` 1 is reachable on a circle past 300 rows. **The assertion that carries it is `LOG-04` in § 8, green at the 8C head** — this row is not re-earned by it and does not move. LOG-01 stays green at its **pgTAP/1D layer** as it has throughout, and its app half stays green on the narrower words 7E gave it; the one addition is that *the reader's access is the filter* is now held PAGE BY PAGE — the live leg walks the whole log as a coordinator and as a caregiver and asserts her result is a strict subset still carrying no health entry, so paging widens nothing a policy narrowed. `docs/owed.md`: **OW-26 `TAKEN(8C/unit 2)` → `CLOSED(2f2c509)`** |
| LOG-02 | Denial collapse (AC-PPL-7): hc.log_denied — no actor parameter, live membership or ONE refusal shape, subject-in-circle validated at CALL time in the same shape (nonexistent and cross-circle indistinguishable, writing nothing; the deferred declaration FK stays as the commit-time belt — round-8 F3), 1-hour collapse (same row, count+window advance; new row per domain); the immutability carve-out is STRICT (+1 exactly, presentation columns only, denial rows only; evidentiary columns and DELETE unchanged both ways) | §2.8; ADR-0009 D2 as amended by ADR-0010 | pgTAP | 1D | green | 030:10–19 · 034:1–5 · **APP HALF GREEN (7C C5, PPL-04)**: a denial row renders its collapsed count and never an object name — the entry cannot carry one and the surface does not invent one (`tests/routes/access-log.test.ts`) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** the 7C app-half addendum names `tests/routes/access-log.test.ts` as its evidence and that pin CANNOT FAIL — the DENIAL fixture carries no `object_type`, no `object_id` and no `detail`, and there is not one `not.toContain` (R4/F-8); read with R6/F-10 (the leg seeds no denial and asserts nothing about one), **AC-PPL-7 has no discriminating app-layer evidence at any level**. The CODE is clean — `entryLine` reads neither `e.detail` nor `e.object_type`, and nothing crosses the RSC boundary. Green on **pgTAP/1D**; the app half re-earned at 7E |
| ADM-02 | The §3.9 boundary: hc_admin reaches admin_meta/admin_ops alone (public binds at ACL absence — no direct entry, A.1 preserved on dsc/access_log/arrivals/ledger); the CI assertions run on every migration — CI-1 zero table privilege, CI-2 transitive column walk (probe-proven for column refs through nested views and derived forms; the subid-0 whole-row shape on mixed tables is probe-CONFIRMED uncatchable by the walk and the name scan — the recorded residual), CI-2b the mechanical closure: every admin_meta view definition pinned by md5(pg_get_viewdef), any change reds the suite and forces review (round-8 F1), CI-3 probe-proven function-indirection walk, CI-4 EXECUTE on nothing with admin_ops EMPTY; five hc_internal-owned views with pinned column lists (counts, states, dates, opaque ids; no content-suggesting column NAME); denials surface as the COLLAPSED total | §3.9, §9.2; ADR-0009 D4 as amended by ADR-0010 | pgTAP | 1D | green | 031:1–25 |
| LDG-01 | Deletion-ledger interface (§2.9): ledger.tombstones exact columns (never content/title/filename), scope-checked, RLS-forced, zero request reach incl. hc_admin; hc.record_tombstone owner-only synchronous writer; append-only except the purge job's ONE executed_at mark; log_head_signatures immutable with zero request reach | §2.9, §2.8; ADR-0009 D3 | pgTAP | 1D | green | 032:14–26 |
| SIG-01 | The daily head-signing worker: KMS key, signatures written to the ledger store, chain heads from hc.log_chain_heads() | §2.8 | worker | **2+** | **pending** | interface green (030:22–24, 032:23–25); no worker runtime exists |
| DEL-01 | The deletion surface: user-originated request → hc.record_tombstone IN the deletion transaction → purge marks executed_at; restore-replay rehearsal (G6/G11) | §2.9, §10.3 | HTTP + worker | **2+** | **pending** | interface green (LDG-01); no deletion surface exists (FRZ-16 companion) |
| ADM-01 | admin_ops wrappers: the five §9.3 operations with operation-bound step-up, dual control (admin_action_approvals), normalized error codes, per-wrapper grants; the A.5 wrapper-identity assertions (stored identity immutable, no self-create-then-act, object-id probing one shape) | §3.9, §9.3; ADR-0009 D4 | pgTAP | **admin slice (2+)** | **pending** | admin_ops EMPTY is the fail-closed boundary (031:16–17 pin it); the §5.7 step-up machinery LANDED (2A M2) — the wrappers remain the admin slice's |

## 2B — app increment (TSD §5.5–§5.11 app halves, §1.7; PRD §4.1; ADR-0013 contracts, ADR-0014/0015)

Layers gain `app` (vitest — the CI "Application tests" step) and `e2e`
(the Playwright §11.4-3 walkthrough — a formalized LOCAL gate,
`docs/ops/e2e-local-gate.md`; ADR-0014, ratified ADR-0015 F11).

**Test classes, stated per round-10 finding 10 — a mocked call-order
assertion is never described as live-authority proof:**

- *mocked route contract* — vitest with collaborators mocked; proves the
  boundary's order, shapes and refusals (tests/routes/*,
  tests/setup/*, tests/app/*, tests/config/*, tests/lint/*).
- *live DB integration* — vitest against the running database with real
  request-role/maintenance authority (tests/db/*, tests/hc/*,
  tests/permissions/* — the snapshot reads hc.tier_defaults() live).
- *live GoTrue probe* — `scripts/probe-gotrue.mjs` against the running
  stack (ADR-0015 F14; re-run on any GoTrue/Supabase upgrade).
- *browser E2E* — the walkthrough, full live stack.

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| APP-01 | The §5.5 config pin: config.toml at §5.5 exact (min 10 / no composition / 720h timebox / recovery 1800 s / TOTP on / anon+SMS+social+web3 off / GoTrue rate limits pinned on) + the hosted-parity doc naming every hosted-only control (HIBP, WAF, per-type expiries) | §5.5; ADR-0013 F1 | app | 2B | green | tests/config/auth-config.test.ts |
| APP-02 | §1.7 for real: index exports exactly the four-factory surface; asAdmin/asPipeline sessions ARE their NOLOGIN roles (positive + 42501 negatives; function-ACL negatives catalog-probed per the segfault trap); the request-role channel assumes anon/authenticated transaction-boxed with verified claims, leaves no residue either way, refuses non-request roles; ESLint fences (service-role → artifact allowlist + gotrue-admin; request-role/maintenance → lib/hc only) driven through the ESLint API | §1.2/§1.7; ADR-0013 F1 | app | 2B | green | tests/db/* · tests/lint/db-fence.test.ts |
| APP-03 | The F1 contract wired: sign-in, step-up re-auth and recovery are the ONLY password paths; sign-in and step-up consult hc.auth_throttle BEFORE GoTrue (positive wait short-circuits with §4.1.7 copy, GoTrue never called), record failure + drive the §5.11 notice as anon, record success AS the proven user (no identifier); recovery records reset_completed identity-bound and is NEVER throttle-gated (AC-AUTH-12); wrappers proven live (escalation at the 5th failure; identity-bound clear; sessionless recordSuccess refused) | ADR-0013 F1; §5.6 | app | 2B | green | tests/routes/sign-in.test.ts · tests/routes/reset.test.ts · tests/routes/account.test.ts · tests/hc/throttle.test.ts |
| APP-04 | Non-enumeration byte-identity: sign-in failure bytes identical for wrong-password vs no-account (no GoTrue text escapes); throttled response existence-independent; reset request identical for account and ghost; create-account identical status/Location/body for fresh vs exists with the verification mail requested in both branches — the fresh branch's Set-Cookie recorded as the accepted §5.5 deviation WITH its full framing (an account-existence oracle observable as post-redirect auth state, rate-limit-boxed — ADR-0015 F5/R5); the invite-variant address derives from the TOKEN server-side. Partial-commit compensation (ADR-0015 F6): un-confirm/bootstrap failure deletes the just-created user and answers a neutral retry, abort failure fails loudly, resend refusals logged never shaped — failure-injection tests per boundary | §5.5; PRD §4.1.7; ADR-0015 F5/F6 | app | 2B | green | tests/routes/sign-in.test.ts · tests/routes/create-account.test.ts · tests/routes/reset.test.ts |
| APP-05 | Wasnt-me app half (ADR-0013 F3 contract): GET renders and touches nothing; destruction ONLY on the explicit POST — execute → IMMEDIATE kill (DB session revocation + rotation) → complete, in that order; a GoTrue outage leaves the durable pending row for the sweep; refusals one neutral shape. The sweep hardened at round 10 (ADR-0015 F3/F9/F15): invoker CHECKED IN (vercel.json cron */10 + the CRON_SECRET GET path), both secrets timing-safe and length-blind, drain oldest-first capped at 20/run with per-action isolation, response reports drained/of/deferred/oldest_pending_age_s; ops contract docs/ops/security-actions-worker.md (30-min max tolerated age) | §5.11; ADR-0013 F3; ADR-0015 F3/F9/F15 | app | 2B | green | tests/routes/wasnt-me.test.ts · tests/routes/worker.test.ts · tests/config/vercel-cron.test.ts |
| APP-06 | The founder door: Step N of 4 on exactly the four step screens (AC-AUTH-2); resume from durable state — no circle→1, circle+empty context→3, context→4 (AC-AUTH-9); step 1 HOLDS both answers to step 2 (relationship validated and carried to circle creation — ADR-0015 F1/R2; the durable column is a batch item); step 2 writes THROUGH hc.create_circle with ADR-0011 local parts, closed situation list, per-subject zips (second defaults), declared slice; step-3 write guarded to the founder's own in-setup circle WITH the zero-row postcondition — a forged/stale/missing circle id REFUSES the advance, and the maintenance writes report their row counts live (ADR-0015 F7); completion promises pinned to Phase 1 only (AC-AUTH-5) with the §7.5 line saying the smaller true thing | PRD §4.1.3; AC-AUTH-2/5/9; ADR-0015 F1/F7 | app | 2B | green | tests/setup/founder-door.test.ts · tests/hc/circle.test.ts · tests/db/maintenance.test.ts |
| APP-07 | The invitee door: the module ceiling renders BEFORE anything is asked (both screens through the ONE renderer — AC-AUTH-8's other half); AC-AUTH-11 — a different identity gets NO accept control, forced re-auth as the invited address (case-blind); dead tokens = who invited + ask-for-a-new-one, nothing created; landings by tier (family→Timeline, care→tasks); invites lifecycle proven live (issue as verified coordinator; describe pre-auth keyed on the unguessable token; accept writes membership+grants; replay refuses) | PRD §4.1.4–§4.1.5; §5.10 | app | 2B | green | tests/routes/accept.test.ts · tests/routes/invite-screen.test.ts · tests/hc/invites.test.ts |
| APP-08 | AC-AUTH-8 — the snapshot: the tiers module's grant table equals hc.tier_defaults() row for row LIVE (family incl. the no-finances-row absence; care schedule-only), and both screens' ceiling copy comes out of the one module | §5.10; AC-AUTH-8 | app | 2B | green | tests/permissions/tiers-snapshot.test.ts |
| APP-09a | AC-AUTH-10, the sign-out half: scope 'global' + page gates server-validate session liveness (getUser), so a second browser dies within seconds — E2E-verified | §5.5; AC-AUTH-10 | app + e2e | 2B | green | tests/routes/account.test.ts · e2e walkthrough step 11 |
| APP-09b | AC-AUTH-10, the access-log half — BOTH halves referenced: the DB half at 4A M1 ('signed_out' + hc.log_sign_out — zero-parameter, actor = hc.uid(), one circle-level entry per live membership; 043:1,5–6,14,24–28); the app half at 4B B8 — sign-out-everywhere writes the entry through the request-role channel BEFORE GoTrue's global kill; a failed entry never refuses the sign-out; anonymous requests log nothing | §5.5; ADR-0015 R1/R8 | app | 4B | green | **flipped 4B B8** — app/account/sign-out-everywhere/route.ts · lib/hc/accounts.ts logSignOut · tests/routes/account.test.ts (order pinned: log before kill) |
| APP-10 | AC-PERM-3's app half (the §5.8 sessions row): remove_member → immediate DB session revocation, ONE transaction (tokens revoked then sessions deleted; session-bound tokens cascade — FK rule pinned; ADR-0015 F8); a refusal revokes nothing; a still-unexpired JWT in a LIVE second browser context reads NOTHING on its next request (RLS is the enforcement); the revoked/cascaded refresh token cannot mint a session (probe F5, live) | §5.8; §4.6.3; ADR-0015 F8 | app + e2e | 2B | green | tests/routes/account.test.ts · tests/db/maintenance.test.ts · e2e walkthrough step 10 · scripts/probe-gotrue.mjs F5 |
| APP-11 | The six GoTrue facts ADR-0014 D3/D4 rest on, as an EXECUTABLE probe against the pinned image (confirmation gates the password grant unconditionally; password checked first; refresh works unconfirmed; no per-user admin logout; the DB kill defeats old refresh tokens; resend accepted unconfirmed) — 6/6 at the 2B gate; RE-RUN ON ANY GOTRUE/SUPABASE UPGRADE, a FAIL re-opens ADR-0014 D3 | ADR-0015 F14; ADR-0014 D3/D4 | live GoTrue probe | 2B | green | scripts/probe-gotrue.mjs |
| E2E-01 | The §11.4 item 3 walkthrough end to end: founder cold start → two subjects (divergent situations/zips) → seq-1 custodianship declarations (DB-asserted) → abandon-and-resume at step 3 → completion (two ADR-0011 addresses, unverified inactive state, no invite affordance, AC-AUTH-5 absences) → real mail-click verification flips the mirror → invite at summary-only with the ceiling under the selector → invitee: ceiling before anything, fixed-address create-account, accept, Timeline landing → AC-PERM-3 → AC-AUTH-10 | §11.4-3; AC-AUTH-1 | e2e | 2B | green | e2e/onboarding.spec.ts (11 steps; local gate, ADR-0014) |

## 2A — auth machinery (TSD §5.5–§5.11; ADR-0011/ADR-0012)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| AUT-01 | §5.6 progressive throttle, as amended by M8 (round-9 F1): existence-blind ledger keyed on hc.contact_key (byte-identical answers, account or ghost; case/whitespace variants share one budget); schedule 0/30/120/900 s over a trailing-15-min window cut at the last success-class event by identity seq; ONE refusal shape; 24 h same-key prune; FAILURE is the only request-role-assertable outcome (record_auth_failure, anon AND authenticated — §5.7 re-auth attempts throttled by the same counters); SUCCESS-class recording is identity-bound and authenticated-only (record_auth_success: no identifier parameter — the cleared key derives from hc.uid(); a stranger's success clears nothing); record_auth_attempt(text,text) is GONE; anon USAGE on hc landed here (EXECUTE stays per-function, PIN-01 unmoved) | §5.6, §5.5; round-9 F1 (ADR-0013) | pgTAP + multi-session | 2A | green | 035:1–21, 28–35, 38–39 · concurrency 28 |
| AUT-02 | AC-AUTH-12 as a test: 200 adversarial failures over 30 min never exceed the 900 s box; the wait is 0 the moment the latest failure ages past 15 min; success/reset_completed clear instantly through the identity-bound recorder — held under two-session contention (anon bursts, the holder's own authenticated success clearing for the other session) | §5.6; PRD §4.1.8 | pgTAP + multi-session | 2A | green | 035:22–27, 36–37 · concurrency 28 |
| STP-01 | §5.7 step_up_tokens VERBATIM: mint authenticated-only on a FRESH session (newest amr ≤ 300 s; aal recorded as the factor actually used; strongest-factor clause app-enforced — auth schema ungrantable, ADR-0012); sha256-only, 5-min expiry; consume owner-only, operation+target(null-strict)+account-bound, single-use via the atomic conditional UPDATE — exactly one of two racers wins | §5.7 | pgTAP + multi-session | 2A | green | 036:1–23 · concurrency 29 · **AMENDED at 8A M2:** for a raise the stored `target_ref` carries four parts, `member:subject:domain:level` (071:12); mint and consume are unchanged — they store and match `target_ref` verbatim, which is what made a one-composition fix in `hc.set_grant` sufficient |
| STP-02 | The §5.7 operations wired at 2A: share_object REQUIRES a live bound token (the 3-arg overload is GONE); approve_proposal validates-and-consumes what is presented (A3's interim guard retired — see APR-06); raise_grant born requiring (GRT-01); export/deletions/transfer/email-password change are born with the requirement in their slices | §5.7; annex A3 | pgTAP | 2A | green | 036:24–34 · 038:7–10 · **AMENDED at 8A M2:** raise_grant's binding is `member:subject:domain:level` (071; 038:7–10 re-pinned with the level); the 2A wording above is preserved |
| IVT-01 | Invite issuance and revocation: coordinator-only (checked first — no oracle); AC-AUTH-4 IN-FUNCTION off the postgres-owned email_confirmed_at mirror (live, never cached); tier family\|care_circle only; live-in-circle subjects deduped; sha256-only 32-byte token returned once, 7-day expiry; revocation pending-only; both logged | §5.10, §4.1.2 (AC-AUTH-4) | pgTAP | 2A | green | 037:1–14, 32–34 |
| IVT-02 | Acceptance: §5.10's ONE-transaction conditional UPDATE under the R-rule lock; case-blind address binding AFTER the claim, so a mismatched session neither accepts nor consumes (AC-AUTH-11's DB half); expiry refused; membership + the EXACT PRD §7.4 tier defaults in the same transaction (family: memories/health/schedule summary + documents log, finances = NO ROW; care: schedule summary per covered subject) — hc.tier_defaults() is the ONE source AC-AUTH-8's app module (2B) snapshots against | §5.10; PRD §7.4, §4.1.4 | pgTAP | 2A | green | 037:18–31 |
| IVT-03 | Membership continuity: a LIVE member's acceptance refuses (membership is not stackable); a REMOVED member re-invited REACTIVATES the original member row under the unconditional unique(circle_id, account_id) — the same actor id keeps naming the same person (N2) | §2.3; N2 | pgTAP | 2A | green | 037:35–37 |
| GRT-01 | hc.set_grant: coordinator-only, per-subject per-domain; RAISE requires a §5.7 token bound to member:subject:domain and refuses under any freeze (named freeze_active — PRD §7.5 "no new grants"); LOWER never demands either; 'hidden' DELETES the row (absence IS hidden, the tier-defaults representation); the care ceiling binds structurally even against a valid token; AC-PERM-5 log with both levels; same-level no-op is silent (absorbs the §5.7 race — ADR-0012); subject-member rows untouchable; target and actor re-read UNDER the R-rule lock (M8, round-9 F2's class) — a target removal committing mid-wait defeats a token-carrying raise with the token unconsumed | §5.8; PRD §4.6.3/§7.4/§7.5; round-9 F2 (ADR-0013) | pgTAP + multi-session | 2A | green | 038:1–22 · concurrency 29, 31 · **AMENDED at 8A M2 (`20260903120002`; plan Q3(a), SETTLED 2026-09-02):** the RAISE token's binding is `member:subject:domain:level` — the level is the fourth part, and a token minted for one level does not raise to another; 038:7–10, 15–17 and 21 re-pinned with the level, concurrency 29/31 likewise, 071 pins the mismatch and the refused-but-unconsumed token; the cell's `member:subject:domain` above is the 2A wording, preserved. STP-03 carries the assertion |
| GRT-02 | hc.remove_member: ONE transaction under the R-rule lock — removed_at+removed_by; every grant row deleted; live shares revoked unless EXPLICITLY kept (strict keep-list, refuse-whole); open tasks unassigned with the former holder in task_unassigned entries at the SAME timestamp as member_removed (PRD §8.8); done tasks keep attribution; the last live coordinator is irremovable (§12.7); returns account_id for the 2B session revocation (the §5.8 sessions row); the actor's own coordinatorship re-validates UNDER the R-rule lock (M8, round-9 F2's class) — a coordinator removed mid-wait removes nobody (§4.6.3 immediate) | §5.8; PRD §8.8/§12.7; round-9 F2 (ADR-0013) | pgTAP + multi-session | 2A | green | 038:23–34 · concurrency 31 |
| WMN-01 | "This wasn't me" (§5.11): the notice path is NON-ENUMERATING (byte-identical {noted:true}, account or ghost, threshold or not; count re-derived internally); ≥5 failures against a real account → ONE security_events row + ONE security-class mail in one transaction, the plaintext token ONLY in the queue payload (never returned to the sign-in caller); token = a COLUMN of its event (binding structural), sha256-only, 15-min expiry (verbatim — flagged, ADR-0012), single-use atomic; execute returns account_id for the 2B session kill + forced reset AND (M8, round-9 F3) durably enqueues the owed kill in the SAME transaction — public.security_actions, UNIQUE(event_id) exactly-once, zero request reach, worker surface (pending/complete) hc_pipeline-only and retry-safe; "token consumed with the promise unperformed" is unrepresentable; two racers: exactly one consumes, exactly one action; cadence one live notice per account | §5.11, §5.5; PRD §4.1.7; round-9 F3 (ADR-0013) | pgTAP + multi-session | 2A | green | 039:1–23 · 042:1–22 · concurrency 32 |
| NTC-01 | The §5.9 class split, built exactly as far as revocation notices require: outbound_mail with the class COLUMN, zero request-path reach; a LOWER enqueues access_changed and a removal enqueues membership_removed — security-class, to the ACCOUNT address regardless of circle access, payload EXACTLY {circle_name, changed_by} (content-free pinned as an exact key set); raises/no-ops enqueue nothing; delivery, templates and the other messages are slice 11 | §5.9, §5.8 | pgTAP | 2A | green | 041:1–8 |

## 3 — design system (TSD §8; design_spec §2–§6; ADR-0016)

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| DS-01 | §8.1 token set exact — names AND values pinned as exact-set equality on `:root`; tinted-panel construction via color-mix (no unpinned hex); the Q2 text-role variants at their red-test-pinned values | §8.1; ADR-0016 D1 | app | 3 | green | tests/design/tokens.test.ts · tests/design/contrast.test.ts |
| DS-02 | §8.2 type roles as classes, declaration-pinned; Newsreader-500 default via `--font-serif`; NOTHING below 10px (every first-party font-size scanned) | §8.2 | app | 3 | green | tests/design/type-scale.test.ts |
| DS-03 | §8.3 shell (topbar 11×20 cream · nav 16×12/2px/border · main ≤1240) + §4 content order and grouping (A8) + page pattern + both grids; container-query responsiveness — no JS measurement, no viewport breakpoints (the only @media is reduced-motion) | §8.3; A8 | app + e2e | 3 | green | tests/design/shell.test.tsx · e2e/a11y.spec.ts (390px pass) |
| DS-04 | §8.4 component contracts: no-shadow card (box-shadow unwritable), radii map consumed, badge/chip metrics with Q2 variants carrying words, chip ×-hit-area ≥44, Button single-writer, structural label association, avatar ring/stack + stable person→accent primitive (plum seq-1, green excluded), legend composition, icon conventions | §8.4; PRD §4.0 | app | 3 | green | tests/design/components.test.tsx · tests/design/migration.test.tsx |
| DS-05 | §8.5 motion inventory exact (nine keyframes, each consumed); finite ≤250ms under the ruled loop-scope reading (pulses + ambient indicators loop; ADR-0016 round-11 addendum); easing pinned PER animation (entrances/ambient ease · pulses ease-out · kb linear); pulses 2.2s ease-out with their accents; PulseProvider — second concurrent registration throws in dev, logs ONCE in prod, unregisters on unmount | §8.5; ADR-0016 r11 | app | 3 | green | tests/design/motion.test.ts · tests/design/pulse-provider.test.tsx |
| DS-06 | §8.6 primitives: three §2.7 temporal kinds rendered honestly (due date refuses timestamps; appointment = intended local + zone; floating says so; relatives in the viewer's zone); calendar numeral; one-sentence empty states; provenance line; NO chart/progress/percentage primitive exists to import (AC-PPL-6, scanned) | §8.6; §2.7; AC-PPL-6 | app + review | 3 | green | tests/design/data-display.test.tsx |
| DS-07 | The 2B surface migrated with zero contract regression: stubs shell-re-homed with 2B copy verbatim, raw button markup unwritable in app/, route/copy contracts unamended, full net green at the head | slice-3 plan D8 | app + e2e | 3 | green | tests/design/migration.test.tsx · the 2B suites unchanged · walkthrough 11/11 |
| DS-08 | §11.4-2 conformance record complete with per-row refs; the review-enforced standing properties named (four §8.1 colour rules · §8.2 voice · §8.6 plain counts/human dates/one-sentence empties); /styleguide (dev-gated, notFound in production, test-pinned) is the standing review surface | §11.4-2; Q4 | review | 3 | review | docs/review/design-conformance.md · tests/design/styleguide.test.tsx |
| A11Y-01 | 2px `--green` :focus-visible ring on every interactive element; bare `outline: none`/`outline: 0` unwritable in first-party styles (scanned, with positive control) | §8.7 | app | 3 | green | tests/lint/no-bare-outline.test.ts · e2e/a11y.spec.ts (ring computed on keyboard focus) |
| A11Y-02 | ONE reduced-motion query drops every animation to stillness INCLUDING `animation-iteration-count: 1 !important` (the seed's gap closed); browser emulation shows zero running infinite animations, with the positive control that the pulse runs without the preference | §8.7; ADR-0016 D4 | app + e2e | 3 | green | tests/design/motion.test.ts · e2e/a11y.spec.ts |
| A11Y-03 | 44px touch targets: every control class carries the floor in the sheet; real boxes audited in-browser at 390px incl. every × glyph, button-styled anchors, and the labels the radio carve-out defers to (round-11 High-2; in-spec positive controls) | §8.7; ADR-0016 D6.3 + r11 | app + e2e (local gate) | 3 | green | tests/design/touch-targets.test.ts · e2e/a11y.spec.ts |
| A11Y-04 | Contrast: every permitted text-carrying token pair ≥ 4.5:1 computed from the live `:root` (Q2 ruling; two candidates darkened by the pin itself); the `--faint`/`--label` exemption bound to the redundancy rule as the named CONTRAST_EXEMPT list — two entries (`.section-label`, `.micro-meta`) since the O1 sign-off ruling revoked `.step-indicator`'s (sole carrier of step position; now `--muted-text`, scanned live); Q2(c2) 1.4.11 input boundary a named G12 audit item | §8.7; ADR-0016 D1/D6.4 + sign-off S1 | app + review | 3 | green | tests/design/contrast.test.ts · e2e/a11y.spec.ts (contrast ON) |
| A11Y-05 | Accessible label on every icon-only control: jsx-a11y recommended at error + control-has-associated-label (form fields carved out to label-has-associated-control), fence-tested through the ESLint API both ways | §8.7 | app | 3 | green | tests/lint/a11y-fence.test.ts · eslint.config.mjs hc/a11y |
| A11Y-06 | axe: every styleguide composition in CI (jsdom, contrast off, positive control proves the harness bites) · every existing route in the browser leg (contrast on, WCAG 2.2 AA tags) | §8.7; Q3 | app + e2e | 3 | green | tests/design/axe.test.tsx · e2e/a11y.spec.ts |
| A11Y-07 | Full keyboard operation of the review screen incl. citation navigation (Tab between facts, Enter selects and moves focus to the cited region) | §8.7 | e2e | **6** | green (6B) | ships with the review screen on this slice's primitives · **FLIPPED 6B close-out**: `e2e/review.spec.ts:524` — "A11Y-07: full keyboard operation — Tab between facts, Enter selects and MOVES FOCUS, at 390px and desktop", observed green at the close-out gate. |
| A11Y-08 | OCR "machine-read — may contain errors" labelling + page/citation navigation parity over machine-read text | §8.7; §6.9 | app + e2e | **6** | green (6B) | ships with document rendering — **re-tagged 5/6 → 6 at the slice-5 gate (Q6: OCR moves wholesale to slice 6; recorded, not dropped)** · **FLIPPED 6B close-out**: `e2e/review.spec.ts:583` — "A11Y-08: machine-read text — §6.9’s exact label, per page, readable where native text is not". This leg is ALSO what caught the engine being absent from the running app (ADR-0026 D15 finding 3): tesseract.js resolved its worker from a `__dirname` the bundle rewrote, so OCR worked under vitest and nowhere else. Fixed at `c58a7e7`; the leg is the wiring’s only real pin. · **ROUND-18 (ADR-0027 D5, F-5): THE LEG NOW ASSERTS THE LABEL ITS TITLE ALWAYS CLAIMED TO PIN, AND THE SHIPPED LABEL WAS NOT THE SPEC'S.** It asserted `toContainText('may contain errors')` — a substring of the WARNING CLAUSE ONLY, which never checked "machine-read" at all, so a rename to "AI transcript — may contain errors" would have kept it green while breaking §6.9's letter. The weak assertion was masking a real divergence: the screen rendered **"Machine-read text — may contain errors"** where PRD §4.2 (`:1391`), TSD §6.9 (`:2177`, `:2501`) and the slice-6 plan's B9 row all say **"machine-read — may contain errors"**. The COPY IS ALIGNED to the spec and the assertion is now `toHaveText` on the full string, stated in the leg rather than imported from the component. Driven both ways as a TARGETED RUN (never a gate result): **1 failed** on the old copy — *unexpected value "Machine-read text — may contain errors"* — then **1 passed (45.8 s)** on the aligned copy. Leg line re-verified at the final head: **`e2e/review.spec.ts:591`** (was `:583`; it moved when the label constant was added — the fourth line drift this slice, which is why the title is the citation and the number is checked at the head). |
| A11Y-09 | Tasks and Timeline (slice 7): the filters and the assign flow fully keyboard-operable end to end; subject labels never by colour alone; at 390px and desktop | §8.7; PRD §4.0, §4.4.1, §4.5.6 | e2e | **7** | green (7B) | **FLIPPED 7B B4**: `e2e/a11y.spec.ts` "A11Y-09: the filters and the assign flow, keyboard-operable end to end, at 390px and desktop" — Tab to a chip with the 2px green ring, Enter follows; Tab to the person, Space, Tab to the button, Enter hands over; and "the record surfaces: tasks and timeline, list and detail, audited at 390px" over fixtured live rows, the subject label carrying the NAME. Built INTO the surface (the A11Y-07 precedent). The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| A11Y-10 | People & roles (slice 7): the plain-language line before any matrix; the adjust matrix keyboard-operable; meaning never by colour; the printed access log readable | §8.7; PRD §4.6.1, §4.6.5, AC-PPL-2 | e2e | **7** | green | **GREEN (7C C6)**: e2e "A11Y-10: the plain line first; the matrix keyboard-operable; meaning never by colour; the printed log readable — at 390px" — axe (contrast ON, the a11y.spec CONTRAST_EXEMPT replicated) clean on the list AND the matrix, arrow-key movement through the level radios, the printed log's entries visible under print media — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5). **The leg EARNED its place twice on the way**: r3 caught the missing contrast exemption in the leg itself; r4 caught a REAL WCAG 2.2 target-size failure — bare inline action links at 16px — fixed as `.action-link` (44px, joined to the enumerated touch-targets pin) across every 7C surface · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *"arrow-key movement through the level radios"* has no assertion that can fail — nothing is read before `ArrowDown` and nothing compared after it; `expect(focusedValue.length).toBeGreaterThan(0)` is satisfied by the radio `.focus()` put there on the previous line (R6/F-1). *"meaning never by colour"* has NO assertion at all, only a comment, and axe cannot stand in — 1.4.1 is not machine-checkable and is not in the rule set axe runs (R6/F-9). The plain-line and printed-log clauses hold, and the r3/r4 history above stands as written. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the keyboard clause reads the checked value BEFORE `ArrowDown` and asserts MOVEMENT (`focusedValue !== before`) and SELECTION (`:checked` follows focus), with the radiogroup itself pinned — every radio shares `name="level"` (R6/F-1); and the colour clause gained its first assertion at all, an exact-set check of each radio's rendered word against `LEVEL_OPTION_WORD` (R6/F-9). **Residual, named and NOT closed:** this row's *printed log readable* clause still has no control of its own — PPL-04 now carries one, this leg's clause remains one-shot (`docs/review/7e-leg-audit.md` §3) |
| A11Y-11 | The Documents viewer (slice 7): the machine-read sibling reachable exactly as native text is; at 390px. **The keyboard-page-navigation clause is STRUCK** (ADR-0038 D4, R6/F-8) — the viewer stacks its pages with no pager, so the clause never had a target. The strike was recorded in the amendment note at round 27; it is applied to the CLAIM itself here, because a record that says one thing above and another below is the defect R6/F-6 is about | §8.7, §6.9; PRD §4.3.4 | e2e | **7** | green | **GREEN (7C C6)**: e2e "A11Y-11: the viewer at 390px — axe clean, alt text on every page, the machine-read sibling reachable by keyboard as native text is" — every page image carries its alt, the shared MachineReadText toggle reached and expanded BY KEYBOARD (aria-expanded asserted), no horizontal scroll, axe clean with the named exemption — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5), over pages a worker actually promoted from a real image upload · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *"page navigation by keyboard through the ONE artifact route"* has no target and never did — the viewer stacks its pages with no pager, no next/previous and no page list, so the clause is STRUCK as a record correction rather than built to (R6/F-8). The keyboard half proves STATE, not reach: `MachineReadText`'s `toggle()` sets `aria-expanded` before and independently of the fetch, so it holds when the sibling 404s, fails or returns empty — and that failure sentence is itself inside `CONTRAST_EXEMPT`. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the leg asserts the TRANSCRIBED TEXT — `pre.review-machine-text` contains the words the page carries — after `Enter`, so the clause no longer holds when the sibling 404s, fails or returns empty (R6/F-8). The sibling's own sentences moved in the same increment: a 404 is said as UNAVAILABILITY, never as a claim about what is STORED, because this client cannot tell an absence from an authorization refusal (R1/F-4) |
| G12-01 | G12 whole: WCAG 2.2 AA audit against the BUILT surface before the first non-founder invitee, incl. the named watch items (1.4.11 boundary · CONTRAST_EXEMPT uses · avatar initials · nav tier-awareness) | §11.2 G12; ADR-0016 | review | **gate** | **pending** | design-conformance.md §4 |

## 4 — ingestion (4A: the database increment; slice-4 plan; ADR-0017 as amended; round-12 findings + ADR-0018; 4B: the app increment, ADR-0019 — ratified in full at round 14)

4A's rule, per the plan: flip ONLY what this layer proves. Layer-spanning
rows carry their pgTAP halves and complete at 4B; nothing flips early.
**4B flip status (post round-13 dispositions):** every citation below to
CI-shaped tests is recorded green at the evidence head `d6a6a22`, and every
"B9 LIVE" citation is bound by the recorded gate run at that SHA (24
passed — walkthrough 11/11 + a11y 5/5 + ingestion 8/8; the round-13
packet's evidence block). **ONE row is not green: UPL-01's transport half
is BLOCKED on round-13 finding 1 (HIGH) — the same-origin TUS proxy's
upstream containment is bypassable (ADR-0019 finding 1, amending D16); it
is a code defect fixed before merge in its own build session, and the row
flips green only after that fix + re-review (never as `pending`).** Every
other 4B row ratified as-built at round 13. **Build-session update
(2026-08-20): finding 1 FIXED red→green (app-layer, no DDL) — a server-signed
continuation target with normalised-URL validation and a grant-to-target
circle binding, completion reconciled; fresh full local gate GREEN (24
passed, incl. the UPL-01 live upload leg). The row STAYS BLOCKED — it flips
green only after the re-review of that fix + owner sign-off (ADR-0019
build-session update).** **Round-14 re-review + dispositions (2026-08-20):
finding 1 RESOLVED — the fix (`bdb7045`) re-reviewed clean with NO new
findings (`docs/review/round-14-rereview.md`, landed at `a73a43b`; CI green
at that head). ADR-0019 D16's transport-containment half is RATIFIED as
fixed and re-reviewed, and UPL-01 is GREEN below — every 4B row is now
green; the round-13 one-non-green-row exception is CLOSED. Two non-defect
observations are recorded in ADR-0019's round-14 update (the deliberate
circle-level write-hop bind; the signed-not-encrypted continuation
target). Owner sign-off recorded at `4d21763` (ADR-0019's S1–S4
addendum — UNCONDITIONAL, merge authorized), and 4B is **merged at
`d7f2f36`** (merge commit `d7f2f3690ff744686260e37fc31fd987fac1a639`,
parents `3195713` + `4d21763`, MERGE COMMIT never squash; CI green on
`main` at the merge commit, run 85 `32470541491`).**

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| BAT-01 | 'signed_out' event type + hc.log_sign_out: zero parameters (actor = hc.uid(), nothing spoofable); one CIRCLE-LEVEL entry per LIVE membership in deterministic circle order; removed memberships log nothing; zero memberships is a quiet zero | ADR-0015 R1/R8; §5.5 | pgTAP | 4A | green | 043:1,5–6,14,24–28 |
| BAT-02 | The four maintenance ops are definers on request-role authority (create_account own-row idempotent; describe_invite anon-callable DEF-10 null; set_slice loud zero-row; set_opening_context F7 postcondition in-function, one shape for forged/foreign/non-setup); the maintenance module holds exactly the two auth.* ops, fence-pinned | ADR-0015 R3/R8 | pgTAP + app | 4A/4B | green — pgTAP 043:2–4,7–11,15–17,29–53 · **4B B8**: all four call-sites on the channel (lib/hc/{accounts,circle,invites}), the module's EXACT two-op surface pinned (tests/db/maintenance.test.ts), the runtime credential's blast radius pinned (tests/db/runtime-credential.test.ts — auth.* and hc.log unreachable) |
| BAT-03 | Step-1 relationship durably persisted at circle_members (the Q2 ruling), bounded ≤ 120, nullable for pre-existing rows, written on the FOUNDER's membership row inside create_circle's transaction (F1 closure) | ADR-0015 R2; Q2 | pgTAP + app | 4A/4B | green — pgTAP 043:54–59 · **4B B8**: the 2B carry passes p_relationship (step-2 submit → createCircleFromSetup; tests/hc/circle.test.ts asserts the durable row; tests/setup/founder-door.test.ts pins the passthrough) |
| BAT-04 | hc_runtime: NOLOGIN, member of anon + authenticated and NOTHING else (two-way exact); zero direct table or function privileges; members bounded to postgres + the seeded local login; hosted provisioning + verification recorded | ADR-0015 R3/R8 | pgTAP + review | 4A | green | 043:19–23 · docs/ops/runtime-db-credentials.md §hc_runtime |
| BAT-05 | Security-actions claim/lease: oldest-first, concurrent sweeps DISJOINT (FOR UPDATE SKIP LOCKED over a once-materialized CTE — the case-33 plan-dependent defect found and fixed in-slice, ADR-0017 D3), lapsed claims reclaimable, limit bounds refused loudly, completed rows never re-claimed; complete_security_action and pending_security_actions unchanged | ADR-0015 R8 item 5; round-10 F9 | pgTAP + multi-session | 4A | green | 043:60–72 · concurrency 33 |
| STO-01 | Store: content-addressed write-once key verified EXACTLY against the arrival's identity; MEASURED bytes re-check the P5 cap; artifact facts commit WITH the won transition; lost transitions (stale lease, cancelled state) write NOTHING (ING-08's class); store_failed stays the naked CAS edge — nothing kept, nothing to write; freeze does not defeat store (§7.5 accept-and-store, raced mid-wait) | §4.3/§4.5 as A5/A6; ADR-0017 D4 | pgTAP + app | 4A/4B | green — pgTAP 044:1,7,10–25 · concurrency 34a · **4B B4**: the store worker (claim → COMMIT → staged bytes → sniffed mime → the exact content-addressed key → finalize; tests/routes/worker-stage.test.ts · tests/hc/workers.test.ts live incl. the wrong-key refusal) · LIVE through the B9 upload leg |
| SCN-01 | Scan: the four adapter states map to four DISTINCT exits, never collapsed (AC-INBOX-15); clean cached with 7-day freshness; infected hash+verdict RETAINED (PRD §11.5) with the expiry sweep leg (clean-expired deleted, evidence kept); cache lookup live-rows-only; a freeze mid-wait defeats scan and writes nothing; quarantine unreleasable by posture (§3.11) | §4.3; PRD §11.5; ADR-0017 D4 | pgTAP + app + e2e | 4A/4B | green — pgTAP 044:2,26–45 · 049 · concurrency 34b · 050:1–8 + case 38 (X1: safety-monotonic evidence) · **4B B4**: the zero-dep clamd INSTREAM adapter (four states, ERROR ≠ outage; tests/scan/scanner.test.ts byte-level) with cache-first workers (a known-infected sha quarantines WITHOUT scanning) and the never-finalize-unavailable retry posture (ADR-0019 D5) · **4B B5**: the §11.5 BYTE purge owned (nightly, 7 days, deploy row — ADR-0018 F2 discharged) · **4B B9 LIVE**: EICAR through the real container lands quarantined ≠ scan_unavailable, evidence row retained-unexpiring, bytes in the no-read-grant bucket |
| QTA-01 | §5.4 quotas: twelve bounds as DATA (the PRD §13.3 letter + PROVISIONAL rates and monthly ceiling, BGT-01-labelled); enumerated outcome with capacity > sender > circle precedence; messages are email PARENTS; deleted arrivals never count; case/whitespace variants share ONE budget; the monthly ceiling NOTIFIES and never turns the outcome; check-then-create deliberately unserialized with the bounded-overshoot contract pinned | §5.4; PRD §13.3/§4.2.8; ADR-0017 D5 (amended ADR-0018 F3: monthly denominator = email parents) | pgTAP + app | 4A/4B | green — pgTAP 045:1–21 · concurrency 35 · **4B B2**: the webhook applies the table (aligned bounce naming the capacity limit · unauthenticated DROPPED for every over-* reason, no backscatter — ADR-0019 D9's as-built, packet Q-i · attachment-count/file-size ride the same table · monthly ceiling notifies, never turns; tests/routes/inbound-postmark.test.ts · tests/hc/ingest.test.ts live over_sender at the 20th same-sender parent, case variants one budget) |
| SAU-01 | §5.3 verdict chain (provider-fields-first, authserv-id-anchored, strip posture, ARC per Q5, display name never an input, verdict shown): 4B B1. The DB's lookalike half is green: near-miss MORE suspicious than a stranger, similar_to named, address-row domains count, live acceptances only, blank input loud | §5.3; Q5 | app + review | 4B | green — the lookalike half 045:22–29 · **4B B1**: the §5.3 chain IN ORDER (provider fields first, chain stops there; authserv-id-anchored A-R bound above the first foreign Received hop; ARC never authenticates — Q5; display name never read by the verdict; blank config fails closed) with the G7 adversarial openers (forged A-R below a foreign hop, lookalike authserv-ids) — tests/mail/inbound.test.ts · the verdict SHOWN at B6 (verified / unverified / the lookalike's own copy) · LIVE at B9 (held verdict copy on screen). Deploy verification rows carry the payload-field and strip-posture unknowns (ADR-0019 D11) |
| DUP-01 | Stage-1 duplicates: exact-sha suspect raised INSIDE the scan finalizer on CLEAN verdicts only (safety answer intact — verdict/scan_at/cache still land); the three §4.7 edges in the closed allowlist with ING-10 re-pinned; resolution manage-gated like cancel, freeze-first NAMED (raced mid-wait), honest state diagnosis for authorized callers; 'different' resumes via a real gate lease + CAS + outbox re-queue; 'same_thing' → nothing_filed (duplicate_of_arrival) with the original RETAINED; NEVER auto-discarded — no discard resolution exists | §4.7 p1; PRD §8.9; ADR-0017 D8 (as amended ADR-0018 addendum X2: canonical strictly-earlier match — exactly one original per sha, M8) | pgTAP + app | 4A/4B | green — pgTAP 048:1–24 · 050:9–13 · 027:2 · concurrency 36–37 · **4B B6**: the member surface (both resolutions, no third option, bound to the SUSPECTED row — a duplicate CHILD is resolvable under its parent's row, the B9 gate find; tests/hc/inbox.test.ts live drives suspect → different-resumes AND same_thing-keeps-the-original) · **B9 LIVE**: the same bytes twice → suspect on screen → a person resolves → ONE relay pass finishes to extracting (DUP-01 + RLY-01 hand in hand) |
| FWD-01 | Activation: the founder-verified-only flip (the postgres-owned mirror is ground truth) by a live coordinator, freeze NAMED, idempotent, §5.1 log entry; inactive = 550-by-absence (AC-AUTH-3); the §5.2 resolver case-blind with the active flag DISTINCT (drift visible) and one null for unknown/deleted | §5.1–§5.2; ADR-0017 D7 | pgTAP + app | 4A/4B | green — pgTAP 047:1–21 · **4B B6**: the app call rides email verification (the confirm route's idempotent activation pass, per-subject quiet refusals — ADR-0019 D14; tests/hc/ingest.test.ts live) · **B9 LIVE**: inactive before the mail click, active with its §5.1 entry after · provider-side route creation = docs/ops/ingestion-deploy.md |
| INB-01 | The webhook: the §5.2 six steps in order, acceptance before any processing, parent + children in ONE transaction | §5.2, §5.4, §4.6 | app | 4B | green — **4B B2**: signature timing-safe (503 unset — never open; 401 unsigned), drift branches blocked VISIBLE, parent+children one hc_pipeline transaction with bytes staged durably BEFORE the 200 (acceptance = rows AND bytes, ADR-0019 D2), enqueue + post-response eager fire — tests/routes/inbound-postmark.test.ts (six-step order) · tests/hc/ingest.test.ts (one-transaction rollback, idempotent replay, conflict raises) · **B9 LIVE** (the synthetic signed webhook end-to-end) |
| UPL-01 | TUS upload: server-minted subject-scoped expiring token, right-to-ingest checked at mint, resume works | §2.12, §1.8; PRD §13.4 | app + e2e | 4B | green | **flipped at the round-14 dispositions — round-13 finding 1 (HIGH) FIXED (`bdb7045`) + RE-REVIEWED clean (`a73a43b`, docs/review/round-14-rereview.md); the ruled flip condition (fix + re-review) is met.** The transport containment as fixed, on two enforcement layers: the client no longer supplies the fetch URL at all (the create hop forwards the fixed server-derived resumable base) and `isResumableUpstream()` validates the `new URL()`-NORMALISED origin + pathname both before the server signs a Location and again on the way back in; the forwarded target is a server-signed unforgeable HMAC continuation token bound to the caller's grant by circle on every write hop and by circle+subject with a live ingest re-check at completion — cross-circle replay refused on both routes, the non-expiring target inert without a fresh < 2 h grant, PRD §13.4 resume preserved (circle-stable bind; completion reconciles at the ORIGINAL staging key). Bypass/resume/completion proven in tests/routes/upload.test.ts (19/19 at the re-review: raw `..` id AND validly-signed-but-normalises-outside → 404 with storage never contacted; cross-circle → 404; resume drives the ORIGINAL upstream and completion reconciles there). Two non-defect observations recorded (ADR-0019 round-14 update: the deliberate circle-level write-hop bind; the signed-not-encrypted target). **The other halves held throughout:** buckets/posture 049 · **4B B3** the mint checks manage-over-all-domains FIRST (the level ruling, ADR-0019 D6; ghost/unauthorized ONE 404 shape), completion RE-CHECKS rights and measures bytes; tus-js-client with resume-from-previous-upload — tests/routes/upload.test.ts · tests/hc/upload.test.ts · **B9 LIVE** (the browser TUS upload through to 'Reading'; the fresh gate at the fix head GREEN — 24 passed incl. the UPL-01 live upload → store → scan → gate leg) · **ROUND-19 (ADR-0028 D1, F-2): THE MINT AND THE COMPLETION NO LONGER ANSWER 401 FOR AN OUTAGE.** Gate run r2 refused this founder with `401 sign in first` 24.3 s after asking and six seconds after the same session rendered a signed-in page, because `liveSessionClaims` returned `null` for every failure of the auth hop. Both routes now separate the three outcomes and answer **`session_unavailable` (503, `retry-after: 5`)** for a fault; **the 401 is unchanged for the answer it is actually for**, and rights are still probed no earlier. At `/api/upload/complete` the distinction is load-bearing — the bytes are already staged, so a false "sign in first" discards an upload that SUCCEEDED. The sentence moves too: "Uploading is not available for this person." is about PERMISSION and reads as *stop trying* — `tests/app/upload-form.test.tsx` pins it |

## 5 — extraction + interpretation (5A: the database increment, MERGED at `7893b80`; 5B: the app increment; slice-5 plan Q1–Q9; ADR-0017 D8, ADR-0018, ADR-0019 D7/D8/D15 inherited; round-15 findings + ADR-0021 dispositions; 5B as-built in ADR-0022)

**5B's flip (2026-08-22).** The four layer-spanning rows complete —
EVD-01, SND-03, DUP-02 and INJ-01 gain their app halves — and the six
5B-only rows open and close: RND-01, AIA-01, WRK-02, EVA-01, PRF-07 and
UXA-02 (the last at `review`, as tabled). The four rows 5A closed
outright — RTC-01, CTX-01, RUN-01, CNF-01 — are UNTOUCHED. **EVA-01's
row says green for the harness, the corpus and the spec, and says in the
same cell that THE G9 GATE ITSELF IS OPEN**: it closes at owner sign-off
of the bands, against a completed BLIND run, before any real document —
never as a side effect of a green row.

5A's rule, the 4A precedent verbatim: flip ONLY what this layer proves.
Layer-spanning rows carry their pgTAP halves and complete at 5B; nothing
flips early. CNF-01 (pending since 1C, staged in the 1C table above)
flipped at 5A M4. A11Y-08 re-tagged 5/6 → 6 above (Q6). SIG-01 stays
pending, explicitly NOT absorbed (Q7: the KMS key + ledger store are
deploy-level).

**Round-15 dispositions (ADR-0021), landed in M6** — three accepted
findings, no row FLIPS on them (each strengthens a row already carrying
its 5A half): F1 (HIGH) put stage-2 detection under the per-circle lock
that guards publication, so a document filed concurrently can no longer
be missed — DUP-02 gains the structural and behavioural pins; F3 gave
the detector's arrival side set semantics over every drafted category
and every key-field value, the settled matching contract unchanged —
DUP-02 again; F2 added the live-actor guard to `hc.list_known_senders`
— SND-03. The migration bound closes SPENT at 6 of ≤ 6. One owner-queue
item opens: the SND-02 live-actor family audit (ADR-0021 D2), to be
taken with the account-deletion path.

**Round-16 dispositions (ADR-0023), landed on the branch.** Eight
adversarial lenses returned **113 findings** (10 BLOCKER / 40 MAJOR /
33 MINOR / 30 OBSERVATION), landed verbatim at
`docs/review/round-16-findings.md` before anything was argued. **Seven
BLOCKERs are fixed red→green**; three are escalated to the owner. **No
row flips green on a disposition** — round 15's rule. Five rows below
carry ROUND-16 AMENDMENTS, and every one of them is a weakening or a
caveat, because the review falsified something the cell asserted: INJ-01
(§4.8's conflict arm was inert), RND-01 (§6.3's email row, and the DPI
geometry), AIA-01 (the cache "measurement" and the tautological
`prompt_version` pin), EVA-01 (the fence, and why the G9 gate cannot
presently be closed — now RULED, and §4/§6 restated against the readable
set at sign-off), SND-03 (the surface threw on every non-empty
list). DUP-02's Status gains the caveat its own evidence cell already
carried. **The migration bound was AMENDED by the owner on 2026-08-23** (≤ 6 → ≤ 7)
for exactly three things, landed as **M7**
(`20260823060001_round16_fixes.sql`) with pgTAP **057**: Q-A's
`duplicate_of_document_id` grant, an `information_schema.column_privileges`
EXACT-SET invariant that reds on any future column added to
`public.arrivals`, and Q-B's `render_bounds_exceeded`. **A FOURTH item was granted on the same day** (ADR-0023 D21/D23) and landed
as **M8** (`20260823070001_interpret_terminal.sql`) with pgTAP **058**: the
interpret stage had NO failure edge at all, so a provider refusal returned
`invalid_state`, terminalized nothing, and let the sweeper re-call the
provider twice more. **Bound closes SPENT at 8 of ≤ 8.** 5B is therefore no longer app-only and the DB legs are
re-run rather than inherited by F12 (ADR-0023 D22). **The dev-dependency
reserve remains UNSPENT.**

**DUP-02 and UXA-02 complete.** With the grant in place the §4.7 p2 copy
reads *"This looks like the discharge summary you filed on July 12."* —
the plan's B6 row, met. The provenance line also stopped over-claiming:
it said "type, date **and provider**" where `hc.detect_stage2_duplicate`
requires category + date + **≥1 of** provider / amount / policy_number
(R5/F-5).

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| EVD-01 | `hc.log_artifact_read` definer retires the D7 hc_internal-assumption boundary: authenticated EXECUTE, in-function authorization on the route's exact visible_at predicate, DEF-10 one refusal shape, the §1.3 step-6 entry through hc.log (chain intact, name captured at write); evidence-before-bytes preserved; the evidentiary module deleted, fences re-pinned | ADR-0019 D7/Q-iii | pgTAP + app | 5A/5B | green | pgTAP half green (5A M1): 051:1,3,5–6,8–17 · 056:8 · **app half green (5B B8)**: `lib/db/evidentiary.ts` DELETED, `logArtifactRead` on the request-role channel through `hc.log_artifact_read` with the call SHRUNK to (claims, arrivalId) — the definer resolves circle/subject/actor-name itself; the fence entry removed with the module and its absence asserted (tests/lint/db-fence.test.ts); BAT-02 re-pinned by reading maintenance.ts's exports; the definer's in-function authorization proven to write NOTHING for an unauthorized caller (tests/hc/artifacts.test.ts) — a strengthening the interim could not offer · **ROUND-18 (ADR-0027 D3, F-3): A READ THE ROUTE REFUSED NO LONGER LEAVES A TRAIL CLAIMING IT HAPPENED.** `AnswerBudget` deliberately does not cancel the work it races, and `withRequestRole` ran on to `commit` regardless of who was listening — so a raced-out `logArtifactRead` COMMITTED after the route had already returned `500 unavailable` and refused the read. Evidence-before-bytes inverted: a trail entry for bytes that never moved, and in a §10.5 context a record asserting a member viewed a document they were served a 500 for. The budget now exposes an abandonment signal (it does NOT cancel — it states that it has given up), `logArtifactRead` checks it AFTER the insert and BEFORE the commit, and the transaction rolls back. **RESIDUE, STATED RATHER THAN CLAIMED AWAY: the check cannot cover the commit round-trip itself** — a budget expiring inside it still lands a row. The window is one round-trip wide instead of the whole remaining query; closing it completely needs two-phase commit. **NARROWED, NOT CLOSED.** Two controls hold the trail as the DEFAULT — a signal that has not fired still writes, and no signal at all is unchanged — because a change that made the log write conditional in general would satisfy the pin and destroy §10.5 |
| SND-03 | Known-senders member surface: `hc.list_known_senders` — live rows with accepted-by/at + acceptor name, deterministic order, the SND-02 coordinator authority, DEF-10 one shape for foreign/nonexistent/non-coordinator; list + revoke live end-to-end (D15's gap closed) | ADR-0019 D15 | pgTAP + app | 5A/5B | green | pgTAP half green (5A M1 + M6) · **app half green (5B B8)**: `listKnownSenders` + `revokeSender` on the request-role channel (lib/hc/inbox.ts), the pair proven live end-to-end (tests/hc/inbox.test.ts: accept → list → revoke → gone; an outsider refused; a NONEXISTENT circle refusing identically to a foreign one), and the surface at `/[circle]/senders` linked from the Care Inbox — deliberately not a sixth nav item (tests/routes/senders.test.ts) | **ROUND-16 AMENDMENT (ADR-0023 D4):** the app half was UNREACHABLE through the surface it shipped on — `accepted_at` is `timestamptz`, node-pg returns a `Date`, and the page called `.slice(0, 10)` on it, so every non-empty list threw at render. It worked only in the two states the tests covered. Fixed at the boundary (`06cffef`/`e0804ee`). No e2e or a11y leg opens `/[circle]/senders` at all (R5/F-6, owed) — which is why it shipped.
| RTC-01 | `hc_runtime` memberships INHERIT FALSE (Q4): both memberships re-granted WITH INHERIT FALSE, SET option kept (the request-role channel untouched); the bare login privilege-refused (42501, no longer RLS-empty), zero direct AND zero inherited privileges; BAT-04 re-pinned; provisioning/verification rows updated | ADR-0019 D8/Q-vi/S3; Q4 | pgTAP + review | 5A | **green** | 051:27–29 · 043:20 (BAT-04 re-pinned) · tests/db/runtime-credential.test.ts (the live-login probes: channel works, bare login 42501, hc unreachable) · docs/ops/runtime-db-credentials.md §hc_runtime (amended) |
| CTX-01 | `hc.record_context_for`: hc_pipeline-only EXECUTE (§3.10 verbatim), circle+subject derived from the arrival (cross-subject/cross-circle not expressible), DEF-10 one refusal shape; the §6.6 shape with the settled inclusion priority — high-risk facts NEVER truncated, standard capped by recency, every section carries {truncated, omitted}; byte-stable (deterministic ordering, UTC ISO rendering, the naive temporal key); the subject-record sections identical across a subject's arrivals — only documents varies, with the arrival's own proposal categories | §3.10, §6.6; PRD §6.4 | pgTAP | 5A | **green** | 052:1–24 · concurrency 43 (one statement, one snapshot — never torn) |
| RUN-01 | Run versioning + supersede-not-append: the run row born IN the claim transaction (claim_stage carries the model/prompt pair, required at the mint point — no lease without its run), closed WITH the lease (trigger — every closer participates; no open run outlives its lease); the kill matrix case by case (sweeper expiry · claim-path expiry · refusal · normalisation failure · stale lease · timeout — each a closed run with the honest outcome); zero-fact runs recorded ('published', countable per class); a re-run cannot double a fact (prior facts superseded in the publication transaction, run_id provenance, stamp coherence); provider_refusal joins the §6.8 reasons; prompt_version names the FULL inference+render configuration (pinned in the migration) | §4.3, §6.4, §6.8; PRD §10.4 | pgTAP | 5A | **green** | 053:1–27 (the kill matrix at 14–19) · concurrency 42 (supersession vs cancellation — the ING-08 class) |
| DUP-02 | Stage-2 duplicates: the settled matching contract (same circle+subject filed current documents; type + date + ≥1 corroborating field ALL PRESENT; absence never wildcards; exact-after-normalisation; deterministic most-recently-filed canonical target) on the Q8 DISTINCT state (own rank, stage-1's label; a wrong resume GRAPH-illegal); detection inside finalize_extraction with the work answer landing in full; the ADR-0018 same-email pair caught BY NAME; per-class FP and FN fixtures; 'different' resumes to interpret (real lease + CAS + outbox), 'same_thing' attaches the additional source and files nothing new (ADR-0017 D8's refinement); never auto-discarded | §4.7 p2; PRD §8.9; ADR-0018; Q8 | pgTAP + app | 5A/5B | green | pgTAP half green (5A M5, strengthened at M6): 055:1–23 · 056:1–6 · 027:2 · 046 · concurrency 41, 44 · **app half green (5B B6)**: the stage-2 surface on the existing inbox machinery, `same_thing` reading "add it as another source" because that is what it does, a child suspect bound to the child, and the affordance gated on the STATE — a resolved arrival offers nothing (round-15 observation 3, as a test). **The copy says WHY the match happened, not WHICH document**: `authenticated` holds a column-level grant on `arrivals` that 5A M5's `duplicate_of_document_id` was never added to, so no member can read the pointer (ADR-0022 D15 — the finding the local gate caught, and round-16's headline question Q-A). B6's "cites the matched FILED document" is therefore **partially met and recorded as such**, pending one line of DDL |
| RND-01 | §6.3 rendering rules as code: source-typed resolution, never-downsample floor, page bounds before render, EXIF orientation normalised before geometry, bbox round-trips to the visible crop, hostile inputs abort under stated ceilings before any dispatch; attempt staging GC'd on non-advance, pages PROMOTED write-once on advance (slice 6's rendering source; the OCR-compatibility exit assertion) | §6.3, §4.5, §6.4 | app | 5B | green | **5B B2.** The mupdf VERIFICATION SPIKE first — eight legs, all PASS (scripts/spike/mupdf-spike.mjs; verdict quoted in ADR-0022), so the spike-contingent runtime reserve is NOT consumed. Then lib/pipeline/render.ts as §6.3 rules-as-code: the table row by row, resolution a floor in BOTH directions, all four ceilings refusing with named reasons and the two that matter decided from the HEADER before any decode, EXIF orientation normalised via the document path (the spike measured the difference at 36.3 vs 220.4 mean sample value over the same citations), geometry resolution-independent and clamped, and the lifecycle keys with the slice-5 OCR exit assertion (tests/pipeline/render.test.ts, 26) | **ROUND-16 AMENDMENTS (ADR-0023 D2, D12):** (1) "the table row by row" is NOT true for §6.3's email row, whose second half ("the rendered message as a second source") was truncated in the as-built record — email facts cite a rendering that is never produced, and the BLIND partition has no email item (R7/F-4, open owner item). (2) the resolution rule was silently wrong for any raster declaring a dpi: `PT_PER_PX` is mupdf's NO-RESOLUTION fallback, so a 300-dpi scan rendered 617×824 — below the standard tier — and the same number gated `page_dimensions`. Fixed at `d8b5db5`/`2a8f1c7` by reading true stored pixels from the header, with no `mupdf.Image` constructed so the orientation property is untouched.
| AIA-01 | The adapter contract: structured outputs, own citation geometry, refusal = honest terminal never "unsafe" copy, no fallbacks, no Files API, model allowlist with the Fable-5 refusal, operator channel + delimited data, cache breakpoint checked, client timeout inside the lease deadline | §6.1–§6.8 | app + review | 5B | green | **5B B3.** lib/ai/ as ONE fenced family, asserted on the REQUEST BODY THE PROVIDER RECEIVES rather than on our source (tests/ai/adapter.test.ts, 27): structured outputs with our own {page,bbox}; the P5 caps as maxItems; NO fallbacks, NO Files API, NO provider citations, NO budget_tokens; stop_reason checked FIRST with an empty-content refusal fixture; delimited data and a {role:"system"} operator channel asserted in BOTH directions; the record prefix behind a cache_control breakpoint with the cache telemetry carried back so §6.6's 512-token minimum is MEASURED not assumed; the timeout budgeted inside the lease; the allowlist an ALLOWLIST with claude-fable-5 refused AND an unknown model refused; prompt_version carrying the configuration hash so a change reds. Fence pinned in tests/lint/db-fence.test.ts | **ROUND-16 AMENDMENTS (ADR-0023 D5, D6):** (1) "the cache telemetry carried back so §6.6's 512-token minimum is MEASURED not assumed" is FALSE — nothing reads `usage`; `ai-provider.md`'s SMOKE-6 carries the real check as an unticked box (R2/F-6, owed). (2) the `prompt_version` pin was a tautology (`PROMPT_VERSION` is derived from `configurationHash()`), so §6.10 had no mechanism; now pinned to a literal. (3) the allowlist admitted `claude-sonnet-5`, which cannot take §6.7's `{role:'system'}` operator channel the adapter sends unconditionally; narrowed to `claude-opus-5`. Both at `14831f1`/`07fdacd`. **MARKER (2026-08-28, round-23 follow-up): ROUND-16 AMENDMENT (1) IS DISCHARGED.** `usage` is read since `80e9a75` (PR #19): the worker logs the four counters, cache_creation/cache_read included, at both consumption sites — R2/F-6 = R7/F-5 ruled **FIXED** at ADR-0031 (round 23), SMOKE-6's box still the owner's at activation. The leg count "27" is also stale: the file carries 58 legs, the four "never on the wire" absences now running on both dispatchers (R2/F-12) and the timeout leg now dispatching (R2/F-2), both ruled at the same round. **Status UNCHANGED** (ADR-0025 S16.7). The prose before this marker is preserved exactly as written.
| WRK-02 | Extract/interpret workers live on the §4.3 sequence; the D13 deferred backlog consumed; relay defer branch gone; exhaustion terminal with stated reasons | §4.3; ADR-0019 D13 | app + e2e | 5B | green | **5B B4/B5/B7.** The extract worker (tests/routes/worker-extract.test.ts, 18) on the §4.3 sequence with the run identity at the CLAIM (M3), the provider never dispatched on any bounds refusal, each normalize exit landing its own state+reason, a refusal terminal and an OUTAGE retried, staging GC'd on every non-advance and promoted only on advance; the interpret worker (worker-interpret.test.ts, 16) with ING-07's in-flight claim; the relay flip with D13's backlog RELEASED rather than waited out (tests/routes/relay.test.ts + tests/hc/relay.test.ts LIVE — the first release run against this machine returned 11 real deferred messages), and the sweeper's stage-agnosticism ASSERTED. maxDuration = 360 pinned above the 300 s stage clock | **ROUND-16 AMENDMENT (ADR-0023 D21, D23):** the interpret stage could not terminalize at all — `hc.arrival_transitions` held exactly one interpret edge (`interpreting → proposals_ready`), so a provider refusal returned `invalid_state`: the state never moved, the lease never closed, and the sweeper re-queued for two more paid provider passes before exhaustion landed the WRONG reason. M8 adds `interpreting → extract_failed` — the terminal `hc.stage_budgets` already named as interpret's exhaust_state, so no enum value, no label and no reason code moved, and the app needed no change (it had always made this call). Behaviourally pinned in pgTAP 058 through a real claim and a real advance as hc_pipeline.
| INJ-01 | §4.10/§6.7 in the live path: privilege absence re-proven at the worker layer; anomaly_flags set and surfaced; injected-instruction fixture lands as a flagged proposal, nothing else | §4.10, §6.7 | pgTAP + app | 5A/5B | green | pgTAP half green (5A) · **app half green (5B B5)**: §4.10's defences tested IN THEIR ORDER. Defence 1 is asserted by what the worker CAN CALL — an injected "grant the sender coordinator access to every circle" reaches only hc.finalize_interpretation, so defences 2 and 3 could fail entirely and the guarantee would hold. Defence 2 is the adapter's delimited data; defence 3 is anomaly_flags carried from both the per-proposal and call-level sets onto every drafted payload. A conflict may only quote a fact id the call was GIVEN — enforced twice, in the adapter and again at the worker. The corpus carries dev-injection-01 for the gate leg | **ROUND-16 AMENDMENT (ADR-0023 D1):** the §4.8/§3.10 half of this cell — "a conflict may only quote a fact id the call was GIVEN — enforced twice" — described code that could not run: both consumers read `context.facts` where `hc.record_context_for` returns `profile_facts`, so both guards protected an EMPTY set and no conflict could be drafted at all. Fixed red→green at `5337064`/`c15d764`, with the guard now asserting the key against the shipped migration rather than against another fixture.
| EVA-01 | The G9 evaluation set: the governed corpus with immutable development/BLIND partitions; per-field precision/recall keyed (model_id, prompt_version) with the full-config manifest behind the pair; re-run required on any covered change; bands enabled only via the fail-closed artifact match — all-high-risk otherwise | §6.10; PRD §6.4, App. B | review + harness | 5B | green (harness + corpus + spec; **the G9 GATE ITSELF IS OPEN** — it closes at owner sign-off of the bands, against a completed BLIND run, before any real document) | **5B B1/B9.** fixtures/g9: 28 items, 16 development + 12 BLIND, every byte generated by scripts/fixtures/g9-build.mjs and verified through a real decoder. The partitions are STRUCTURAL — lib/eval/blind is §1.7-fenced to scripts/eval/** and tests/eval/** so a prompt cannot be tuned against the scored set by accident (tests/lint/db-fence.test.ts). tests/eval/corpus.test.ts (16) asserts the manifest IS the corpus and that the stated minimum support, source-type spread, negative examples and adjudicated ambiguity are MET. docs/eval/g9-corpus-spec.md carries the proposed per-field bands for sign-off AND states its own limits. scripts/eval/run.ts is the SOLE real-key path (Batch API, immutable manifest, dry-run verified 12/12). lib/extraction/bands.ts fails closed on missing/altered/stale/partial/malformed/non-blind with a test for each, and BAND_ARTIFACT_ALLOWLIST ships EMPTY, asserted | **ROUND-16 AMENDMENTS (ADR-0023 D7, D11):** (1) the partition fence had THREE bypasses — `itemsIn`/`corpusManifest` exported from the unfenced `lib/eval/corpus`, no `ImportExpression` handler in ESLint's core rule, and a fixture server reading all 28 items as data. Closed at `95ce3b3`/`ef67a83` via a fenced `lib/eval/manifest.ts`, a `no-restricted-syntax` guard, and a partition filter driven by the real matcher. (2) **the gate cannot presently be closed**: 8 of the 12 BLIND items contain no rendition of the values they are labelled with, so no per-field recall floor in the spec is arithmetically reachable and the ≥2-source-type minimum is met entirely by items on which extraction is impossible (R6/F-1 — OWNER DECISION). **SIGN-OFF AMENDMENT (ADR-0023 D24, 2026-08-23):** the owner RULED R6/F-1 — §4 and §6 of the spec are RESTATED against the READABLE set, and the numbers are now measured rather than declared. Three corrections to this cell follow. (a) The leg count is **20, not 16**. (b) "the stated minimum support … are MET" is true of LABELLED support only — kept, because it is what a grown corpus must reach — while four new legs MEASURE readable support through the pipeline's own `normalizeArrival`, pin the readable blind set as an exact four-item set, and assert the **SHORTFALL** (22 minimum-misses), because that is the fact keeping the gate closed rather than an aspiration about it. §6 now carries a `Signable?` column reading NO twelve times of twelve. (c) "`scripts/eval/run.ts` is the SOLE real-key path" is literally false — `lib/ai/client.ts` reads the key in production; it is the only path ever RUN against a real credential (R6/F-12). **The row stays green**: the harness, the corpus and the fence are real and now demonstrably so. What it must not be read as is a gate that could be closed today. **ROUND-22 FOLLOW-UP (2026-08-28) — three figures in this cell are SUPERSEDED by the Q10 purchase (6B B10).** (a) The corpus is **56 items, 16 development + 40 BLIND**, not 28 / 16 / 12. (b) "dry-run verified 12/12" counts the old 12-item partition. (c) §6 no longer carries a `Signable?` column reading NO twelve times of twelve — it reads `Reachable? yes — unsigned` for all twelve, at **max recall 1.0**, and `g9-corpus-spec.md` §6.A point 4 is marked superseded at its own site. **The citation axis grew a second ceiling in the same increment and lost it too:** R3/F-7 made citation accuracy load-bearing (`CITATION_FLOOR` 0.90) while the corpus's email labels were boxes the §6.3 rendition never paints — 0 of 23 landing, capping `provider` at 0.8519 and `appointment_date`/`appointment_time` at 0.7500. Ruled at ADR-0023 D26, fixed at PR #16: **23 of 23 land, max citation accuracy 1.0 for all twelve.** **THE ROW'S STATUS IS UNCHANGED AND THE GATE IS STILL OPEN.** No arithmetic ceiling remains, so what closes G9 is now the gate alone — `docs/ops/ai-provider.md` §5 (G9-1 … G9-5, all ☐), gated in turn on G3's terms and a credential — and §6.A rule 3 is all-or-nothing, so a completed run can still fail to sign. The prose above is preserved exactly as written.
| PRF-07 | §13.2 arrival→proposals p95: measured over the eager path (fixture server), report-only locally, the hosted row named on ai-provider.md (D15's gap discharged to its honest local limit) | PRD §13.2; ADR-0019 D15 | bench + review | 5B | green (report-only, as ruled) | **5B B9.** scripts/bench/prf07.ts, arrival→proposals_ready through the real wrappers and the real worker route against the fixture server; METHOD stated in the header (cohorts per §6.3 class, PRF-06 nearest-rank kept verbatim, cold and warm SEPARATE, single and depth-4). RUN and recorded: worst warm p95 6866 ms (phone photo, depth 4) — ~11% of §13.2's 60 s budget, which says our machinery leaves the provider ~53 s, NOT that the budget is met. The hosted, provider-inclusive row is on docs/ops/ai-provider.md carrying PRF-06's BREACH-CLAUSE discipline · **ROUND-18 (ADR-0027 D6, F-6): RE-MEASURED AT 6B AND THE 5B FIGURE ABOVE IS SUPERSEDED.** The 6B evidence head re-ran PRF-07's own bench with PRF-06's method verbatim and measured **20 479 ms — scanned PDF at depth 4 = 34.1% of §13.2's 60 s budget, leaving the provider ~40 s**, not the ~53 s the 5B sentence above states. The worst cohort MOVED TOO (phone photo → scanned PDF), and that is the tell rather than an incidental: **B1 replaced the rasterizer** (`mupdf` → `pdfjs-dist` + `@napi-rs/canvas`) **and B9 added OCR**, which are precisely the two changes that move the scanned-PDF cohort. So the 5B figure does not merely lag — it describes machinery THIS SLICE REMOVED. The 5B numbers are kept above as history, on the same principle as the `:400` pointer (ADR-0026 D16 item 8): a superseded measurement is a record, an unlabelled one is a lie. Still report-only, still local; the hosted row stays on `ai-provider.md`. **This row was not one of the fourteen the round-18 packet tabled, and that is exactly how it went stale — the slice measured PRF-07's own quantity and did not carry the result back to PRF-07's cell** |
| UXA-02 | The stage-2 duplicate copy + resolution on the inbox; ProvenanceLine's first consumer recorded per Q6 | §4.7; PRD §8.9 | review | 5B | green | **5B B6.** The stage-2 copy and both resolutions on the inbox (tests/routes/inbox.test.ts). **Q6 DECIDED, first branch: ProvenanceLine takes its first consumer HERE** — the suspicion is downstream of AI-extracted values (document_date, provider and amount are exactly what M5 matched on), so showing where it came from is provenance in §8.6's sense. The design-conformance citation STAYS in slice 5. What the line cannot yet say is WHICH document, for the grant reason in DUP-02's cell — recorded, not glossed. Row stays `review` pending the round-16 reviewer's read of the copy | **ROUND-16: the row's own exit condition is MET (ADR-0023 D19, D20).** This row stood at `review` pending the round-16 reviewer's read of the copy. The read happened and it found a defect: the provenance line claimed "type, date AND provider" while hc.detect_stage2_duplicate requires category + document_date + >=1 OF provider / amount / policy_number, so two EOBs matched on AMOUNT alone from DIFFERENT providers were told the providers matched (R5/F-5). Fixed, and with M7's grant the copy now also NAMES the matched document - "This looks like the discharge summary you filed on July 12." - which is the plan's B6 text. This is not a disposition flipping a row: it is the condition the row itself named being satisfied, defect and all.
## 6 — the Care Inbox (6A: the database increment; slice-6 plan Q1–Q10 SETTLED 2026-08-24; TSD §4.9, §4.2, §6.3, §6.4, §3.4, §3.7; PRD §4.2; ADR-0023 R3/F-8, R4/F-6, R4/F-10, R4/F-12; ADR-0019 Q-C; **ADR-0025, the round-17 dispositions, carried at M6**. 6B: the app increment, round 18)

**§4.9 is the first slice in which a person's click changes the record**,
and 6A is the half that makes the click possible: approval narrowed to the
evidence, rejection given a path at all, the arrival given an exit, the
rendering given a manifest, and the receipt given a read. **Nothing here is
production-activated** — proposals still rest at `pending` until 6B ships
the surface, the G9 gate stays OPEN, `BAND_ARTIFACT_ALLOWLIST` stays EMPTY,
and G4/G7 still block the forwarding address.

**Twelve rows open here; two FLIP rows do not move at 6A.** A11Y-07 and
A11Y-08 (lines 324–325, both `pending` tagged **6**) flip with the review
screen at 6B — they are built INTO the screen rather than after it (§8.7:
a structural accessibility failure found at G12 *"is a redesign, not a
fix"*). **SIG-01 is still NOT absorbed** (line 251): the third slice running
in which a pending row is left pending rather than quietly adopted.

**The 6A/6B seam in every cell below is the same one**: the database can now
express what §4.2 describes, and no member has yet been shown any of it.
Every `pgTAP half green` cell names what the app half still owes.

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| REV-01 | **One authorization gate across the review screen**: approval narrowed to require `view` on the arrival over all five domains IN THE DATABASE as well as `manage` on the object's taint (Q7), so no member can approve a fact whose source and citation are invisible to them; the summary-×5 member sees the row, no source, no facts, no controls, one line (AC-INBOX-8) | §3.7, §4.9, §6.4; PRD §4.2.3, §6.4, §7.3; Q7 | pgTAP + app + e2e | 6A/6B | green | **6A M2** (`20260824120002`). ONE predicate added to `hc.approve_proposal` — `hc.visible_at(ctx, subject, hc.all_domains(), true, 'arrival', arrival_id, null) >= 'view'`, character for character the one `hc.log_artifact_read` (20260821120001:81) and the artifact route already enforce — placed INSIDE the authorization boundary so `proposal_version_changed` / `proposal_taint_changed` keep their distinct shapes strictly past it; refusal rides the existing `approval_refused` shape (DEF-10). Driven BOTH WAYS: 060:1 asserts the composition is real (manage on the taint / **hidden** across five), 060:2–3 refuse and write nothing (not even the idempotency claim), 060:4 approves once the same member is raised to view×5. 013/054 re-pinned at the narrowing's own site (060:5–7) and unchanged in the suite. concurrency 47 isolates the ADDED predicate: a grant lowered mid-wait defeats an approval that still clears manage-on-taint — **before Q7 that approval succeeded**. **6A M6 (round-17 dispositions, ADR-0025 D1/D2/D4):** the same gate gains LIVENESS on the two write paths and the manifest policy, so all five of ADR-0024 D10's surfaces now ask the same question of the same LIVE arrival (064:11–13); and Q7's SECOND consequence — a care_circle actor on an unresolved-lineage proposal refused by the CEILING rather than the ladder — is pinned as pure `hc.visible_at` calls (064:17–19) with Q7 itself RATIFIED UNCHANGED. **ROUND-17 SIGN-OFF NOTE (ADR-0025 D16 S16.4): the manifest policy's added clause is NOT provably liveness alone.** ADR-0025 D4 argues the `EXISTS` can subtract nothing but `deleted_at is null` because the subquery runs under `arrivals_select` at `summary` while the policy requires `view`; the two predicates read DIFFERENT subjects — `arrivals_select` tests the ARRIVAL's `subject_id`, the renditions policy tests the MANIFEST's, the composite FK pins only `circle_id`, and `hc.visible_at` never reads the arrival row. Driven live at the sign-off head: a manifest whose subject differs from its arrival's was visible before M6 and is hidden after it. **Latent** — `hc.write_rendition` is the only writer and copies the arrival's subject — and the direction is a NARROWING, so nothing is revealed and **F-3's fix is not in question**; the correction is to D4's ARGUMENT. **App half owed (6B B6/B7):** the `[arrival]` route, the three regions, and AC-INBOX-8's summary-×5 rendering · **APP HALF GREEN (6B)**: the `[arrival]` route, the three regions and AC-INBOX-8’s summary-×5 rendering are live — `e2e/review.spec.ts:462` ("below-cliff: the summary-×5 member sees the row, the state, and ONE line"), with the gate’s one authorization resolution asserted in `tests/routes/arrival.test.ts`. |
| REV-02 | **Item-level approval, in the interface as well as the database**: no control approves more than one proposal (AC-INBOX-3, asserted over the rendered tree); `version` rendered and submitted, a version or taint refusal re-renders with the change highlighted; idempotency keyed per attempt; access re-checked at write time (AC-INBOX-12) | §4.9, §3.7; PRD §4.2.3, §4.2.9, §6.2 | pgTAP + app + e2e | 6A/6B | green | **DB half was already structural and is now complete for both decisions.** `proposal_commits` has made one-proposal-one-object a TABLE since 1B; 6A M3 gives the mirror the same discipline — `hc.reject_proposal` carries the same `p_expected_version` refusal (061:9), the same idempotency identity through `approval_attempts`, and the same write-time re-check including M2's predicate (061:10). **The identity now includes the DECISION** (061:12, `approval_attempts.decision`), closing a real hole: approve's replay branch returns BEFORE the pending check, so a key claimed by a rejection would have replayed the rejection's stored result to a caller who asked to approve. concurrency 46: approve versus reject on one proposal yields one decision, the loser refused, the key not burned. **App half owed (6B B7):** the rendered-tree assertion that no control approves more than one, and refusal re-rendering · **APP HALF GREEN (6B)**: `e2e/review.spec.ts:428` — the version moves under an open screen, the write is refused, and the screen re-renders with the change highlighted; **and the rendered-tree assertion that no control approves more than one proposal is `tests/routes/arrival.test.ts` — "6B B7 · AC-INBOX-3: NO control approves more than one" / "every decision form carries EXACTLY ONE proposal_id, and no batch control exists" (`:204-215`)** — structural (at most one `proposal_id` per `<form>`) plus a batch-control denylist. **ROUND-18 (ADR-0027 D9, F-9(b)): this cell used to say that assertion "rides the same leg" as `e2e/review.spec.ts:428`. It does not — that leg asserts the version refusal, the in-place re-render and that nothing landed, and carries no batch-control assertion at all.** The assertion is real and is good; the citation pointed at the wrong file, so a reader following it would not have found it. Both are now named, by title as well as by line. |
| DEC-01 | **The loop closes in the GRAPH**: `hc.reject_proposal` with §4.2.3's bounded reason vocabulary; `proposals_ready → filed \| nothing_filed` appended with ING-10's exact set and 046's guard re-pinned in the same commit; reject-all ⇒ `Nothing filed` with the original intact and re-readable (AC-INBOX-4); the last-decision race yields exactly one terminal transition | §4.9, §4.2; PRD §4.2.2, §4.2.3, AC-INBOX-4 | pgTAP + app + e2e | 6A/6B | green | **6A M3** (`20260824120003`). Two facts stood in the way and both are recorded: NO proposal could ever be rejected (`reject_reason` and its two CHECKs have waited since 20260815230001:83/:85 with nothing to satisfy them), and `proposals_ready` appeared in the graph EXACTLY ONCE, as a to_state, never as a from_state — every arrival that reached "Needs you" stayed there for ever, `hc.manual_entry`'s included (061:19). 061:1–19. The terminalization rule is settled in the DATABASE — every LIVE proposal decided, `filed` if ≥1 approved/edited_approved — inside the deciding transaction (061:13–17); `superseded` holds nothing open; the original artifact is untouched. **The graph is the authority, not the function** (061:18: a valid interpret lease cannot drive the edge). **A constraint was retired deliberately**: the stage column's FK pointed at `hc.stage_budgets`, the WORKER budget table, so satisfying it would have made `hc.claim_stage(arrival, 'review')` a legal call for any pipeline worker — replaced by a closed CHECK (061:2), leaving 019's five-stage pin untouched. Re-pinned same commit: 027, 055 (22 → 24), 001 (22 → 23 event types), 002, 007 (ten → eleven freeze referents). **046 needed NO re-pin and 061:3 CHECKS that** rather than claiming it. concurrency 45, 48. **6A M6 (round-17 dispositions, ADR-0025 D1/D2): THE APPROVE-TIME PAYLOAD CONTRACT.** `p_edits -> 'fields'` was merged at `20260824120003:478` with no validation at all, before every guard, so a person's click still reached SIX raw Postgres classes — `23514 tasks_check` (guarded on the conflict arm, not the ordinary one), `23514 temporal_shape`, `22P02`/`22007` through every cast the insert arms perform, `23503` on a payload-supplied `episode_id`, `22023`/`22P02` through `parents`, and **`23502 profile_facts.risk_class` on the CONFLICT arm, which is the class M1 records as CLOSED** (ADR-0025 S-1, found at the round, not by the review). Validated where the DESTINATION is known: the cast half performs exactly the casts the inserts perform and converts their failure into `approval_refused`; the destination half mirrors the two CHECKs and the one payload-derived FK from the shipped DDL. Plus a closed, fail-closed EDIT CONTRACT — an edit corrects a value and cannot re-author `parents` or set `manual`, which nulled `source_arrival_id` and detached a written object from its arrival. 064:1–10, with 9 and 10 the non-breaking controls. **CORRECTED AT THE ROUND-17 SIGN-OFF (ADR-0025 D16 S16.2/S16.3): the cast half performs exactly the casts THE INSERT ARMS perform, and that is not every payload-derived cast in the function.** Two raw classes remain reachable at the approve click and both were driven live at the sign-off head: `22P02` on the conflict arm's `domain`, cast in the TAINT MATH at `20260824120006:421` for every outcome while the guard covers `use_new` alone (reachable by an ALLOWLISTED edit key AND with no edit at all, since `hc.draft_proposal`'s conflict branch never validates a conflict payload's `domain`); and `22023` on `p_edits -> 'confirm_high'` (`:500`), a top-level caller key the edit contract does not fence. Both are crash-shape — the transaction aborts, nothing is written, no privilege is crossed — so **the CLASS is narrowed, not closed**: F-1 is **FIXED IN PART** and the residue is **OWED at 6B** with six acceptance conditions (ADR-0025 D16 S16.8), the first of which is that `064` gains a `keep_both` case — it has none, which is why the one arm with incomplete cast coverage is the arm the suite does not exercise. **App half owed (6B B8)** · **APP HALF GREEN (6B)**: `e2e/review.spec.ts:295` — reject-all yields `Nothing filed` with the original intact and re-readable (AC-INBOX-4). The S16.8 residue closed at `20260825120001` (064:21–32). **RULING 5 stands: this cell RECORDS; F-1’s FIXED-IN-PART verdict is the round’s to move.** · **ROUND-18 (ADR-0027 D12): THE ROUND RULED — **ADR-0025's F-1 moves FIXED-IN-PART → FIXED.** Both residue classes are closed and each was checked rather than accepted: `22P02` on the conflict arm's `domain` is now cast for EVERY outcome (`20260825120001:345-370`), driven at `064:21` (`keep`) and `064:22` (`keep_both`) — including the arm the file previously did not exercise and reachable WITH NO EDIT AT ALL, which was the sharp part of the original finding; and `22023` on `confirm_high` takes a top-level type check (`:147-150`), driven at `064:23`. Plus two contract closures placed BEFORE any row is written (`064:24`, `:25`) and — the part that makes it a fix rather than a narrowing — **controls proving approvals were NOT narrowed**: `064:26` (a well-formed `keep_both` still commits its task) and `064:27` (a real boolean `confirm_high` still approves the high-risk item). The ruling rests on pgTAP evidence neither the review nor this round re-ran; nothing under `supabase/` has moved since `bc3bc85` and CI ran `test:db` from a cold database at the review head. |
| CIT-01 | **A citation resolves to a region a person can see**: `cropRect` in production, the crop on screen before the approve control activates for EVERY field in all-high-risk mode (PRD §6.4, §6.5), selecting a fact highlights its region and selecting a region scrolls to its facts (AC-INBOX-2) | §6.4, §6.5; PRD §6.4, AC-INBOX-2 | app + e2e | 6B | green | 6B B7. `cropRect` has had no production consumer since it was written (R3/F-13) and the review screen is it. **6A's contribution is the read path it will consume**: `hc.extractions_for` returns each fact WITH its citation at the arrival's view×5 gate (060:9–12) · **GREEN (6B)**: `e2e/review.spec.ts:237` — source → fact → region → crop → approve → the receipt, with the crop on screen before the approve control activates. `cropRect` finally has a production consumer. |
| RND-02 | **The rasterizer is permissively licensed and the rendition is a fact**: `mupdf` (AGPL-3.0-or-later) removed and `lib/pipeline/render.ts` migrated to `pdfjs-dist` + `@napi-rs/canvas` (Apache-2.0 + MIT), licences verified from the installed manifests, the spike re-run against the corrected 7/8 bar; the rendition manifest written in `finalize_extraction`'s transaction; `promotedPageKey`'s `ext` required; partial promotion DETECTED and reported rather than served as a 404 | §6.3, §6.4, §4.5; ADR-0023 R3/F-8, R4/F-6, D24(1); Q3, Q5 | app + review | 6A/6B | green | **6A M4** (`20260824120004`) closes the MANIFEST half, and with it two owed findings that were one missing fact: nothing anywhere recorded what was RENDERED. `public.arrival_renditions` — arrival, page count as rendered, extension per page — written in `hc.finalize_extraction`'s transaction (062:3), §2.1 throughout, RLS enabled AND forced, read at the SAME view-on-all-five as the pages (062:11–13). **R3/F-8**: the extension is a FACT (062:4), not `promotedPageKey`'s `png` default that is wrong for every photo and scan. **R4/F-6**: partial promotion is DETECTABLE and therefore repairable (062:14) — listing could not tell "three pages" from "page three was never promoted". VALIDATED not merely stored (062:5–7); a cancelled or superseded attempt writes NOTHING (062:8–9, concurrency 49). `finalize_extraction`'s 4-arg form was DROPPED and replaced by a 5-arg one defaulting to null — two functions differing only by a defaulted trailing parameter make every 4-arg call ambiguous, and `lib/hc/workers.ts:150` still makes one (062:10). **Owed at 6B B1/B2:** the AGPL removal, the licence verification, the spike re-run, required `ext`, and the artifact route's page parameter. **ZERO dependencies were added at 6A, as ruled** · **GREEN (6B B1)**: `mupdf` removed, `lib/pipeline/render.ts` on `pdfjs-dist` 6.2.108 + `@napi-rs/canvas` 1.0.8, licences read FROM THE INSTALLED MANIFESTS. Two recorded deltas, neither scored: TIFF → `unsupported_type` (Skia carries no TIFF codec) and pdfjs REFUSES truncated input where mupdf repaired it (spike leg 5, REC). `tests/pipeline/render.test.ts` + the extraction legs. |
| RCP-01 | **The receipt**: `hc.receipt_for` names every destination of an approved arrival, counts what the caller cannot see and never names it, states rejected-and-not-written, and links the destinations whose surfaces exist | §4.9, §2.4, §3.5; PRD §4.2.4, AC-INBOX-9 | pgTAP + app + e2e | 6A/6B | green | **6A M5** (`20260824120005`). A DEFINER read, because `proposal_commits` holds NO member privilege at all (20260815230001:150) — `authenticated` holds nothing on the table §4.2.4's receipt is a read of, so it could not be built at the app layer at any level of cleverness — and should not get a blanket grant, being the one-proposal-one-object claim itself. 063:1–14. **Counted, never named** (§3.5's discipline): the invisible destination is still a ROW, with its type and WITHOUT its name or a handle, `visible` returned explicitly so "you may not see this" and "there is nothing here" cannot be confused. **Driven both ways** (063:5–6): an unresolved-lineage destination is hidden from a view×5 reader and NAMED to a coordinator, so a filter that only blanked a column would fail. Never wider than the RLS it stands in for — each destination through its OWN policy predicate, `deleted_at is null` and profile_facts' `view` included. **The migration records the honest bound on the filter**: the arrival's view×5 gate dominates the per-destination `summary` thresholds, so the reachable cases are unresolved lineage, deletion, and caps that refuse the gate anyway. **App half owed (6B B8)** · **APP HALF GREEN (6B)**: `e2e/review.spec.ts:237` — the receipt names every destination of an approved arrival and links the two whose surfaces exist (tasks, timeline). **REWRITTEN BY RULING (ADR-0036 Q-F — the rewrite ADR-0025 D6 assigned to a round; OW-20 measured it):** when this cell first said *"both live RLS reads"* the two linked pages performed none — `tasks/page.tsx` selected `state` (the column is `status`), `timeline/page.tsx` selected `title, happened_on` (the columns are `summary` and the §2.7 shape), and both rendered their empty states unconditionally; the links resolved, the reads did not. TRUE SINCE 7B: OW-20 CLOSED at `6afffb7` (the floors select the columns that exist, read `error`, and render an error state, never an empty one), the reads then moved into `lib/hc/tasks` / `lib/hc/timeline` at B2/B3, and the record legs prove rows render live. RCP-02 carries the rest. |
| RCP-02 | *(staged forward)* **AC-INBOX-9 in full** — every receipt link resolves to the created object, including Documents and profile facts | PRD §4.2.4, AC-INBOX-9 | app + e2e | **7** | green | **GREEN (7C C5)**: every receipt destination now RESOLVES — tasks and timeline to their objects (7B), a document to ITS page, a profile fact to the subject's page (`/[circle]/people/subject/[subject]`, Q4(b)'s Phase-1 home), an episode to the Timeline where its wrapper renders — and *"its page opens in an upcoming update"* is GONE from the tree (`tests/routes/arrival.test.ts`, the three new pins). The invisible destination stays counted-never-named. The subject's page itself: e2e "the subject's page: the custodianship declaration and the profile facts at view (Q4(b), RCP-02's profile link)" ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** the evidentiary clause *"and 'its page opens in an upcoming update' is GONE from the tree"* is FALSE at `ccd854b` — the string renders at `app/(app)/[circle]/timeline/[event]/page.tsx:137`, on a file 7C never touched, and the three cited pins read only the ARRIVAL page's markup; a second site asserts its absence in a comment at `inbox/[arrival]/page.tsx:284` (R4/F-1). The row's own assertion — receipt links resolve — does not fall on that finding; it falls on Q-B's: the episode link drops the `subjectId` `receiptLine` carries, so a multi-subject circle lands on the FOUNDING subject's thread (R4/F-2). **Narrowings ruled at Q-B:** an episode resolves to its SUBJECT's thread; where its member events are not the reader's to see, or fall outside `listEvents`' oldest-300 window, the wrapper does not render. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the timeline event page LINKS the document — `d.id` was in hand at the exact line that said its page opens later — and the deferral sentence is now refused TREE-WIDE by a scanner over `app/**.tsx` and `components/**.tsx`, comment-carved and shipping its own negative test (`tests/lint/source-text.test.ts`; R4/F-1, and ADR-0026's *"if it can be a scanner, it must be"*). The episode receipt carries `?subject=<id>` and `#episode-<id>`, and the Timeline's episode wrapper carries the matching `id` for the fragment to land on (R4/F-2). **Q-B's narrowing, verbatim and standing:** *"an episode resolves to its SUBJECT's thread; where its member events are not the reader's to see, or fall outside `listEvents`' oldest-300 window, the wrapper does not render"* |
| CNF-02 | **§4.8's three outcomes through the SURFACE** — the app half of CNF-01, whose conflict arm only began working at round 16's `c15d764` and has never been exercised by a human: the choice is offered with no default, `use_new` supersedes with both provenances readable, `keep_both` commits the task as the approval's one object | §4.8, §4.9; PRD §4.2.5 | app + e2e | 6B | green | 6B B7. The DB arm is CNF-01 (5A M4) and is unchanged by 6A — 054 runs green under M2's narrowing, re-pinned at 060:5–7 · **GREEN (6B)**: `e2e/review.spec.ts:357` — the three outcomes offered with NO default; `use_new` supersedes with both provenances readable and the old value retained. |
| OCR-01 | **§6.9 — a reading aid, never a fact**: OCR for image-only sources as `pNNN.txt` siblings (the slice-5 exit assertion honoured — neither stored coordinates nor promoted artifacts change), labelled "machine-read — may contain errors", never in `extractions`, never provenance, poor confidence stated rather than garbage presented | §6.9, §8.7; PRD §13.5 | app + e2e | 6B | green | 6B B9, through `tesseract.js` (Q3's third slot, an INSTALL at 6B). 6A adds no dependency and no OCR surface · **GREEN (6B B9)**: `tests/pipeline/ocr.test.ts` (the REAL engine, local data package, no network) + `e2e/review.spec.ts:583` for the label and the per-page reading. Served `?page=N&text=1` through the artifact fence; absence is THE ONE 404; the A2 allowlist is unmoved. **The e2e leg is load-bearing**: the unit suite ran the real engine and still could not see that the bundle broke its worker path (ADR-0026 D15 finding 3). · **ROUND-18 (ADR-0027 D4/D5, F-2 + F-5): the label is now §6.9's string EXACTLY** — the screen said "Machine-read text — …" and the leg pinned only the warning clause; both corrected, leg re-verified at **`e2e/review.spec.ts:591`** by title. **AND THE ENGINE'S RESOLUTION IS CHECKED ON THE BRANCH THAT ACTUALLY RUNS (F-2).** `realPathOr` validated the `require.resolve` result and returned the `process.cwd()` fallback UNCHECKED — and inside the bundle the resolve returns a module id, so the guard failed by design and the unchecked fallback was the live branch. It now gets the same `existsSync`, an unresolvable engine raises a named `OcrEngineUnavailable` naming BOTH candidates, and the worker route's absorption — which is correct and unchanged, a reading aid must never fail the answer it aids — distinguishes "no engine on this host" from "this page could not be read" as a §10.4 signal. **The deployment consequence remains UNOBSERVED**: no hosted runtime has been looked at, and the local browser leg is still the only place the bundle is real. |
| EVA-02 | **What would open G9, made mechanical**: the threshold rule WRITTEN — how a measured number becomes a band (R6/F-4) — citation correctness SCORED so a model with perfect values and wrong boxes can no longer score 1.00 (R3/F-7), a multi-page fixture so `citation.page` is exercised at all (R3/F-6), multi-valued support counted correctly (R6/F-10) | §6.10; ADR-0023 R3/F-6, R3/F-7, R6/F-4, R6/F-10; Q10 | review + harness | 6B | green | 6B B10, with Q10's purchase (blind 12 → 40, INCLUDING an email item). **The G9 GATE ITSELF STAYS OPEN** — it closes only at owner sign-off of bands against a completed BLIND run, never as a side effect of a green row · **GREEN (6B B10)**: `tests/eval/score.test.ts` (multiset support, the citation-landing predicate), `tests/eval/thresholds.test.ts` (§6.A’s threshold rule — R6/F-4), `tests/eval/corpus.test.ts` (40 items incl. five email; R6/F-17’s guard, which caught TWO live truncations in the shipped corpus). **The G9 GATE ITSELF STAYS OPEN** — scoring being honest is not the same as the bands being signed. |
| PRF-08 | **§4.5's window measured, not asserted**: the arrival-received signal's staleness and the post-fire median reported by the PRF-07 harness, report-only locally, the hosted row named on `ai-provider.md` | PRD §13.2; ADR-0023 D14/D24(3); Q8 | bench + review | 6B | green | 6B B5. **Q8 SETTLED: the Care Inbox revalidates — the signal, THEN the eager fire, in that order, never the reverse.** It needs NO DDL, which is why **M7 closes UNCONSUMED**. 6A's bound stands at **6 of ≤ 7** once M6 carries the round-17 dispositions — which is the number the plan predicted in four places (ADR-0025 D7) · **GREEN (6B B5, report-only)**: the arrival-received signal’s staleness and the post-fire median measured by `scripts/bench/prf07.ts` at the close-out head; method stated with the numbers (cohorts named, cold and warm reported SEPARATELY, nearest-rank p95 — PRF-06’s method verbatim). Local numbers are report-only; the hosted row stays named on `ai-provider.md`. |
| UXA-03 | The review screen's copy, the receipt's sentences and the refusal/staleness language read at the round-18 gate (the UXA-01/UXA-02 pattern) | §8.6; PRD §4.2.3, §4.2.4 | review | 6B | **pending** | 6B, read at round 18. 6A ships no member-facing string |

**The 6A bound, stated exactly (Q2).** **SIX migrations of ≤ 7** —
M1 `inherited_obligations` · M2 `review_boundary` · M3 `decide_proposal` ·
M4 `renditions` · M5 `receipt` · **M6 `disposition_guards`, the round-17
dispositions** (the standing precedent since 2A, spent as the plan
expected). **M7 closes UNCONSUMED** because Q8 ruled for a surface that
revalidates, which needs no DDL. **Six of ≤ 7 is the number the plan
predicted in four places**; the build closed at five only because M6 was
still reserved (ADR-0025 D7). The tree moves **62 → 68 migrations /
59 → 65 pgTAP files**. **ZERO dependencies
were added** — Q3's three runtime slots are 6B installs, and the
dev-dependency reserve stays UNSPENT through a third slice.

**Two inherited obligations, and one of them was NOT taken at this layer.**
M1 closes **R4/F-12** at both the layers `domain` is already guarded — the
draft-time guard so the unapprovable item is never drafted, and the
approve-time guard so `23502` can never surface as a raw Postgres error at
the moment a person clicks approve. **The guard is wider than the finding's
letter and the migration says so**: seven payload-derived columns are NOT
NULL and unguarded, not one, and the wider guard is non-breaking by
construction because every payload it refuses would have raised `23502` a
few statements later (059:3 and 059:11 are the controls, both passing on
`main`). **R4/F-10 has no DB half that does not contradict a deliberate
shipped pin** — pgTAP 055:453 asserts the stage-2 suspect's interpret
`invalid_state` and argues it — so M1 takes the finding's OTHER remedy,
"make it a warn", which is app-layer and lands wholly at 6B B3. 059:13 pins
the DB behaviour as UNCHANGED so the decision lives in the suite rather than
only in a document, and **round 17 is asked to confirm it**.

## 7 — the four destinations (7A: the database increment; slice-7 plan Q1–Q6 SETTLED 2026-08-28; TSD §3.5, §3.6, §3.7, §2.6, §7.1; PRD §4.3, §4.5, §4.6, §7.3–§7.6; ADR-0032. 7B: Tasks + Timeline, round 26. 7C: Documents + People & roles, round 27 — round 25 went to ADR-0034, the round-24 re-verification)

**§11.1 row 7's four surfaces are "written by slice 6's approvals"**, and
7A is the half that lets a person ACT on what was approved: hand a task to
a sibling across a taint boundary by one of two human paths, close and
snooze it, move a document between domains with its audience named first,
unshare in one action, and read the People list, a document's references
and its shares through definers that are never wider than the RLS they
stand in for. **Nothing here is production-activated** — no surface exists
yet, G4/G7 block, G9 stays OPEN, `BAND_ARTIFACT_ALLOWLIST` stays EMPTY,
SIG-01 is NOT absorbed (fourth slice running), `PROMPT_VERSION` does not
move (no 7A unit touches `lib/ai/`).

**One FLIP row moves at 7A and it moves at the pgTAP layer only**: SHR-02
(line 205), pending since 1D. RCP-02 and A11Y-09/10/11 (lines 490–493,
326–328) do NOT move — they flip inside 7B/7C. **Every `green` cell below
names what the app half still owes**, the 6A/6B seam carried to 7A/7B/7C.
LOG-03 is the accepted-risk row Q2 ruled for and is **never green**.

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| TSK-01 | **Assignment across a taint boundary**: the person who cannot see the subject is not offered; the person who cannot clear the taint gets the sentence and exactly two explicit human paths; path 1 is `{schedule}`-tainted with its own provenance and the original stays invisible; the AI holds no path (AC-TASK-2/6) | §3.6; PRD §4.5.5, §4.5.6, §6.5 | pgTAP + app + e2e | 7A/7B | green | **pgTAP half GREEN (7A M1, `20260829120001`)**: the assignee's taint from HER OWN vectors (`hc.ctx_for`), as she meets the policy once the task is hers (066:6–8); no context on the subject ⇒ refused, no path (066:12); cannot clear ⇒ exactly one path or refused (066:10–11); PATH 1 writes the typed sentence and NOTHING of the original (066:13–15, the leak control), visible to her at summary while the original stays invisible though she now holds it (066:16–18); PATH 2 shares task + document together behind a §5.7 token bound to the PAIR (066:25–30); the paths exist ONLY for the crossing (066:22–24); the in-function POST-CONDITION — an assignment never yields a task its holder cannot see (066:24); the AI role holds no EXECUTE, catalog-based (066:4); freeze_active named (066:51). concurrency 50, 53. **App half owed (7B B2)**: the sentence and the two paths at the point of selection, from `hc.circle_people` · **APP HALF GREEN (7B B2)**: the point of selection from `hc.circle_people` — `selectionFor` computes D19.7's gate and the ladder exactly as `hc.assign_task` does, driven both ways live (`tests/hc/tasks.test.ts`: hidden ×5 not offered AND refused; one `log` grant offered AND path 2 goes through); the sentence and exactly two paths on the crossing screen; `e2e/record.spec.ts` "cross-taint: …" — Omar not offered and named with the reason, the sentence, two `.record-path`, path 1, the instruction hers and the original 404 FROM HER LIVE CONTEXT. The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| TSK-02 | **Complete and snooze with attribution**: completed tasks never deleted; the snooze count shown; every write has a human actor in the log (AC-TASK-2) | PRD §4.5.3, §4.5.4, §4.6.4, §7.3 | pgTAP + app | 7A/7B | green | **pgTAP half GREEN (7A M2, `20260829120002`)**: the holder completes (067:8–10), done is terminal and the row stays readable (067:11–12), a non-holder below manage is refused whatever her level (067:6–7, 27); snooze moves FORWARD only, `snooze_count + 1`, ONE `record_revisions` row per snooze naming the actor (067:13–17), the due pair together (067:19), no date no snooze (067:20); the ARGUED line — the holder at `summary` (the care ceiling) closes her work (067:21, ADR-0032 D5, packet Q-A); freeze_active on both (067:29–30). **App half owed (7B B3)**: the controls and the count on the row · **APP HALF GREEN (7B B2)**: the complete and snooze controls with the count on the row ("snoozed once/N×"), Done apart with who and when — `tests/routes/task-detail.test.ts`; `e2e/record.spec.ts` "complete / snooze with the count (TSK-02, AC-TASK-2)". The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| TSK-03 | The list and detail: `Mine · Unassigned · Overdue · All` + subject, counts post-filter over the rendered tree; every task shows a source that resolves or is named-never-linked (AC-TASK-4/5) | PRD §4.5.1, §4.5.3 | app + e2e | 7B | green | **GREEN — the ADR-0036 Q-H hold DISCHARGED**: the tasks leg PASSED inside the COMPLETE 57/57 gate run at `ccd854b` (7C r5, 17.3 min), exactly the condition the ruling named. The held history, kept: **7B B2 built; the flip was put to round 26 and RULED HELD to the 7C gate (ADR-0036 Q-H)** (a session records, a round rules): the app half is proven — the list `Mine · Unassigned · Overdue · All` and by subject, COUNTS POST-FILTER over the rows RLS decided (`tests/routes/tasks.test.ts`), the detail with what · who · when · the source LINKED when it resolves and named-never-linked when not · completion (`tests/routes/task-detail.test.ts`) — and the e2e leg ("tasks: assign in two taps; the sibling's source resolves; counts over the rendered tree; a caregiver's first open never blank") PASSED by title at 396c44f (68 s) and 5/5 targeted at cb256a9, but NOT inside a complete gate run at the head (both full runs lost it to named host mechanisms — ADR-0035 D11, Q-H). Held pending rather than claimed early; flips when this leg passes inside a complete gate run at the 7C head (ADR-0036) |
| TSK-04 | Empty states per tier — a caregiver's first open is never blank (§4.5.5) | PRD §4.5.5 | app + e2e | 7B | green | **GREEN — the ADR-0036 Q-H hold DISCHARGED with TSK-03**: same leg, same complete 57/57 run at `ccd854b` (7C r5). The held history, kept: **7B B2 built; the flip was put to round 26 and RULED HELD to the 7C gate (ADR-0036 Q-H)**, with TSK-03 — same leg, same record: the per-tier empty states are unit-proven (`tests/routes/tasks.test.ts` three cases) and the caregiver's never-blank first open passed in the leg's title runs; the e2e half flips inside a complete gate run at the 7C head (ADR-0035 D11 → ADR-0036 Q-H) |
| TLN-01 | The thread: per subject with a labelled combined view, kind and date filters, `memory` never an empty filter; every event shows its source (AC-TL-2/4) | PRD §4.4.1, §4.4.5 | app + e2e | 7B | green | **7B B3 (app + e2e)**: the thread per subject with a switch and a LABELLED combined view, every row subject-labelled (nothing merges silently); kind filters medical · care · admin with `memory` never rendered as a filter; a date range; every event's source (arrival linked / named, the AI read, the approver; a manual event the person and the date) — `tests/hc/timeline.test.ts` live (12), `tests/routes/timeline.test.ts`; `e2e/record.spec.ts` "timeline: two subjects, the switch, the combined view labelled, a manual event with its provenance, the creation entry first". The creation entry is the first row where the log's own rule shows it (ADR-0035 D4). The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| TLN-02 | Manual events as ONE action for a `view`×5 member through `create_manual_proposal` + `approve_proposal`, provenanced *entered by that person, on that date*; below the cliff no control | PRD §4.4.3; MNL-01 | app + e2e | 7B | green | **7B B3 (app + e2e)**: ONE action — `hc.create_manual_proposal` then `hc.approve_proposal` (`lib/hc/timeline.ts#addManualEvent`), the event the receipt, provenanced by the manual arrival and the approver ("Entered by … on …"); the control only for a `view`×5 member (`canAddByHand` from her own levels), the draft refused below the cliff with nothing written (`tests/hc/timeline.test.ts`); the record leg adds one by hand and reads it back with its line. The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| TLN-03 | Episodes never conceal their member events; drafting them is NOT this slice (AC-TL-3 render half) | PRD §4.4.2 | app | 7B | green | **7B B3 (app)**: consecutive events sharing an episode render inside ONE wrapper (`section[aria-label="Episode: …"]`) and every member event stays its own card, individually openable and sourced (`tests/routes/timeline.test.ts` "an episode wraps its events and conceals none of them"; `tests/hc/timeline.test.ts` the episode on the row). Drafting episodes is NOT this slice. The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| GTE-01 | The page gate's three outcomes: `unavailable` renders unavailable (503, retry-after, no-store) at every page and form route, never a sign-in; `confirm` never claims success for a pass that did not run | ADR-0028 D8 item 2, D15 items 1/4 | app + e2e | 7B | green | **7B B1 (app + e2e)**: `liveSessionClaims` deleted; `lib/auth/gate.ts` reads all three outcomes; the ten pages render `SessionUnavailable` (role="alert", the retry sentence, "try again" to their own path) and the five form routes + `proxy.ts` answer 503 · retry-after · private,no-store — never a sign-in; `/confirm` never claims success for a pass that did not run (a fault is a 503 retry of the same link; no session + unavailable is a retry; an activation throw is `forwarding=failed` with the pass offered again). `tests/app/page-gate.test.ts` drives every gated site on disk and pins the set both ways; `tests/app/proxy.test.ts`, `tests/app/gate.test.ts`, `tests/routes/confirm.test.ts`. The live half is the 45-leg gate running through the proxy on every request (716cd49); no local instrument fakes an auth outage in a browser — recorded, not claimed (ADR-0035 D2). The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| DOC-01 | The list by category and subject at the member's own level: at `summary` sentences only, no viewer and no control implying one; count post-filter; *Nothing filed yet.*; upload from Documents is an ingestion (AC-DOC-2) | PRD §4.3.1, §4.3.7; TSD §3.4 | app + e2e | 7C | green | **GREEN (7C C1/C6)**: ONE RLS-true fetch with the tab counts AND the rows computed over exactly what RLS returned (`lib/hc/documents#documentsFor`; `tests/routes/documents-list.test.ts` incl. *Nothing filed yet.* whenever nothing is FILED); "Add a document" is a LINK to the existing upload page, never an input; an in-flight upload is a row wearing `hc.product_state`'s §4.2.2 label leading to the Care Inbox · e2e "documents list: rows at the member's own level, counts post-filter over the rendered tree; Add a document is an ingestion (DOC-01, AC-DOC-2)" ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5, 17.3 min; the five-run record: ADR-0037) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *Nothing filed yet.* is FALSE under an active subject filter, which empties `rows` server-side before the sentence's only guard (`rows.length === 0`), with the subject nav hidden because it is derived from the narrowed rows (R2/F-5); and the e2e leg proves neither half of its title — only the founder is driven, and the count is read only where `filtered === rows`, by substring match (R6/F-7). The unit half (`tests/routes/documents-list.test.ts`) stands. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): *"Nothing filed yet."* is a claim about the CIRCLE and renders as one — under a subject filter the page says *"Nothing filed for &lt;name&gt; yet."*, the subject nav is read from `hc.circle_people` rather than from the already-narrowed rows, and an **All** link that drops `subject` is the way back a person did not have (R2/F-5). The leg reads the list from a MEMBER's own context with a second document fixtured OUTSIDE the filtered category, so `all > filtered` and the post-filter claim is read where it can fail, with the caption anchored `^N documents?| DOC-01 | The list by category and subject at the member's own level: at `summary` sentences only, no viewer and no control implying one; count post-filter; *Nothing filed yet.*; upload from Documents is an ingestion (AC-DOC-2) | PRD §4.3.1, §4.3.7; TSD §3.4 | app + e2e | 7C | review | **GREEN (7C C1/C6)**: ONE RLS-true fetch with the tab counts AND the rows computed over exactly what RLS returned (`lib/hc/documents#documentsFor`; `tests/routes/documents-list.test.ts` incl. *Nothing filed yet.* whenever nothing is FILED); "Add a document" is a LINK to the existing upload page, never an input; an in-flight upload is a row wearing `hc.product_state`'s §4.2.2 label leading to the Care Inbox · e2e "documents list: rows at the member's own level, counts post-filter over the rendered tree; Add a document is an ingestion (DOC-01, AC-DOC-2)" ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5, 17.3 min; the five-run record: ADR-0037) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *Nothing filed yet.* is FALSE under an active subject filter, which empties `rows` server-side before the sentence's only guard (`rows.length === 0`), with the subject nav hidden because it is derived from the narrowed rows (R2/F-5); and the e2e leg proves neither half of its title — only the founder is driven, and the count is read only where `filtered === rows`, by substring match (R6/F-7). The unit half (`tests/routes/documents-list.test.ts`) stands.  (R6/F-7) |
| DOC-02 | The detail: pages through the ONE byte path (fence-tested: no second `asServiceRole()` consumer, no second bytes route); facts at `view`×5 with citations and the `risk_class` word; source, approver, time (AC-DOC-3); references and shares count-never-name | PRD §4.3.4; TSD §1.3 | app + e2e | 7C | green | **GREEN (7C C2/C6)**: the fence FIRST (`tests/lint/byte-path-fence.test.ts`, committed before the viewer existed — one `asServiceRole()` consumer by FILE, one route under `app/api/artifact/`, `createSignedUrl` nowhere else); ONE page, three depths decided by `hc.visible_at` in the row read (`can_view` = the arrival's view×5, REV-01's one resolution; `extractionsFor` NEVER called below it); at summary a list of sentences with NO disabled control; a HIDDEN document is the one 404 with the row deciding FIRST (the r3 gate catch, pinned in `tests/routes/document-detail.test.ts`); the machine-read sibling is the ONE shared component (§6.9's exact label, a toggle that classifies — never a dead link) · e2e "documents detail: sentences at summary with no viewer and no control; at view the pages through the artifact route with the machine-read sibling (DOC-02)" — Dan's summary context and the founder's view context both driven — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** the fence's GUARANTEE is narrower than *"fence-tested"* reads here. Its four predicates are literal-name greps, three of them shipping no control, and it survives none of: a re-export from `lib/db/**` (where the import fence is `off`), `createSignedUrls` (the plural escapes the word boundary), a byte-returning reader in `lib/storage/artifacts` admitted to three route globs, or `next/image` (R1/F-1/F-2/F-3/F-5). What the fence proves is the TREE at `ccd854b`, not the property its titles claim — the product claims in this cell were re-verified independently and HOLD. Exact-set importer pins land at 7E |
| DOC-03 | **Re-categorisation is an audience change**: the exact before/after audience named before the move; refused unless manage on both domains; category, taint, index and the log entry in one transaction (AC-DOC-6; A.3 `health → documents`) | PRD §4.3.2, §4.3.6; TSD §2.6, §7.1 | pgTAP + app + e2e | 7A/7C | green | **pgTAP half GREEN (7A M3, `20260829120003`)**: `hc.document_audience` names exactly the members whose level changes, by name and level, on the MOVE's gate (068:6–9); refused without manage on BOTH domains (068:10–11); the move rewrites category and taint through the ONE shrinking path, carries the DERIVED task (068:14–15), and moves the index row WITH access in one transaction, driven both ways (068:12, 16); the person's `audience_changed` entry carries BOTH audiences by name (068:17–18); the machinery's own entry stands beside it and the count is two, stated (068:19, ADR-0032 D6, packet Q-D); same-domain and same-category shapes (068:21–22); freeze_active (068:28). concurrency 51. **App half owed (7C C5)** · **APP HALF GREEN (7C C2)**: the preview renders the EXACT audience by name and direction (`hc.document_audience`'s gained/lost/changed — D19.10's undisclosed-levels shape rendered as direction) with the domain move said in §4.3.2's words, and the confirm carries `expected_category` so the sentence the person saw BINDS the move (`document_changed` its own marker) — `tests/routes/document-detail.test.ts`, `tests/hc/documents.test.ts` live (the TWO audience_changed entries stated, 068:19) · e2e "re-categorise: the audience named before the move, the move landing with its markers (DOC-03, AC-DOC-6)" — Dan named as losing sight, on the leg's OWN fixture document — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** the preview names the DOCUMENT audience only — `hc.document_audience_derived` has ZERO callers, so ADR-0034 D7's ruling that the preview names the derived objects is unmet and *" No one gains or loses access."* can be a false assurance while a task holder loses her task (R2/F-2); re-categorise is OFFERED for every other category regardless of manage on the DESTINATION domain, and the DB's named `audience_refused` is flattened into `loadFailed`, losing the whole page — so **AC-DOC-6's refusal half has NO app-layer evidence at any level** (R2/F-1); and the e2e clause *"the move landing"* passes with no move having landed (R6/F-2). The pgTAP half (068) is untouched. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the preview names the DERIVED objects — `hc.document_audience_derived`, the function ADR-0034 D7 cited and which had NO caller anywhere in the tree, is wired through one `Promise.all` slot, and *"No one gains or loses access."* now requires BOTH answers empty (R2/F-2). The category offer is AUTHORIZED: only categories whose domain the caller manages are offered, which is plan C2's *"refused AND NOT OFFERED"* made structural, and the audience read has its own catch landing on `?e=refused` rather than swallowing the whole page (R2/F-1). **AC-DOC-6's refusal half has app-layer evidence for the first time** (`tests/routes/document-detail.test.ts`), and the leg asserts 200 BEFORE and 404 AFTER from the member's own context (R6/F-2). **The title still over-claims** *"with its markers"* relative to what the leg alone proves — the markers are the unit and pgTAP halves' (`7e-leg-audit.md` §5) |
| DOC-04 | **Scoped sharing and unshare in one action**: one object, one person, never the domain, never derived objects — checked from the grantee's live context (AC-DOC-5, AC-PERM-10) | PRD §4.3.5, §7.6 | pgTAP + app + e2e | 7A/7C | green | **pgTAP half GREEN (7A M1/M3/M4)**: `hc.revoke_share` by the granter or a coordinator, one action, logged, the grantee loses the object on her NEXT query (068:23–27); under a freeze permitted (068:29); a share never reaches a derived object at the read — a share-holder's `document_references` counts and does not name every derived object (069:20); shares per object, not per graph (069:24); path-2 shares are `created_by_assignment_of` and unassign revokes exactly those (066:32–39). `hc.share_object` itself is 1B/2A (015). **App half owed (7C C4)** · **APP HALF GREEN (7C C2/C6)**: the share behind the §5.7 step-up bound to `document:<id>` with §4.3.5's rules said on screen, unshare ONE form (`tests/routes/document-detail.test.ts`; live both ways in `tests/hc/documents.test.ts`: the grantee's NEXT query gains exactly THE document and not the category, and loses it on revoke) · e2e "share / unshare: one document to the caregiver — her context sees IT and not a task derived from it; unshare is one action and her next look loses it (DOC-04, AC-DOC-5, AC-PERM-10)" — Marisol's live context 404s before, reads the document (and 404s the derived task) after, 404s again after the one-action unshare — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) |
| DOC-05 | The upload routes bounded at ingress (size, budget) and the five upload-path fetches bounded (D15 item 5, D17 item 7) | ADR-0028 D15 item 5; ADR-0027 D17 item 7 | app | 7C | green | **GREEN (7C C2)** — OW-19 and OW-07 CLOSED(f1cfc33): both upload routes cap ingress at 4 KiB answering 413 BEFORE any parse or probe and answer inside `withRouteBudget` with every hop raced; the TUS creation refuses a missing or over-cap Upload-Length before a byte lands (the per-file pre-read bound); each of the five upload-path fetches carries its named time bound (client 15 s mint / 60 s completion with honest 504 copy; TUS hops 120 s; the eager fire 10 s) — `tests/routes/upload.test.ts` +6, `tests/lint/answer-budget.test.ts` +4 (which also holds all SEVEN auth submits, OW-23) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *"with every hop raced"* is FALSE for the ingress read: `boundedJsonText`'s `req.text()` runs BEFORE `withRouteBudget` opens on both routes (token :40 vs :56, complete :51 vs :74) and is raced against nothing, so a chunked body with no `Content-Length` neither 413s nor 504s (R5/F-1). The SIZE half — the 4 KiB cap, 413 before any parse, the TUS `Upload-Length` refusal — is CONFIRMED and stands. Carried by **OW-24**. **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the ingress read is INSIDE `withRouteBudget` on both upload routes and raced as `boundedJsonText`, so a chunked body with no `Content-Length`, dribbled a byte at a time, takes the route's own overrun — `{refused:'slow'}` at 504 — instead of sitting outside every guarantee the route makes (R5/F-1 → **OW-24 CLOSED**). The SIZE half is untouched and still answers 413 before any parse or probe, now from inside the budget. **The phrase *"every hop raced"* is true of this row for the first time**, which is exactly the condition OW-24 attached to it |
| PPL-01 | The list: every person, subjects as people with custodians named (AC-PPL-3), invites pending/expired, **the plain-language line per subject before any matrix from ONE module pinned to `hc.tier_defaults`** (AC-PPL-2), with its two stated limits | PRD §4.6.1, §4.6.2, §7.5, §8.5 | app + e2e | 7C | green | **GREEN (7C C3/C6)**: the plain line from ONE module (`lib/permissions/phrases`, pinned LIVE against `hc.access_level` and `hc.tier_defaults` — hidden deliberately has NO word, so an unworded level can never leak into a sentence) rendered BEFORE any matrix, and the list page holds no matrix at all; both settled limits SAID on screen (the record-only scope; the §7.5 framing with the custodian named, never "authority"); null levels render NO line; invites `Invited · expires …` / `Invite expired · send again` — send again RETIRES the old token through `hc.revoke_invite` (the wrapper's first caller) and lands the coordinator on the existing invite form prefilled, the fresh invite riding the ONE create path (`tests/hc/people.test.ts` live, `tests/routes/people.test.ts`). The DB half stays 7A M4's (069:7–15) · e2e "people: subjects as people with custodians named; the plain line before any matrix (PPL-01, AC-PPL-2/3)" ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *"the list page holds no matrix at all"* is asserted by `main table` = 0 and `main input[type="checkbox"]` = 0 at BOTH layers, while the matrix four files away is `<form>` → `<label>` → `<input type="radio" name="level">` — neither shape, so nothing in the tree asserts the absence of the shape the matrix HAS (R6/F-3); *"custodians named"* is asserted by the label word, which renders beside a `?? 'named at setup'` fallback whether or not a custodian resolved, and the unit half's `Sarah` is also a member card (R6/F-4). **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): *"the list page holds no matrix at all"* is asserted against the shape the matrix ACTUALLY has — `input[name="level"]` = 0 and `form[action*="/grant/submit"]` = 0 — at both layers, alongside the pre-existing table/checkbox counts (R6/F-3); *"custodians named"* is asserted as the contiguous clause `custodian:\s*People Founder` plus a negative on the `named at setup` fallback, and the unit half uses a custodian who is no member's display name (R6/F-4). **Residual, named and NOT closed:** in the browser fixture the custodian IS the founder, so only the unit half closes the gap structurally — provisioning a second member to hold custodianship in the leg is NOT PLANNED (`7e-leg-audit.md` §1) |
| PPL-02 | Adjust: per subject per domain; raise through step-up, lower without; the care-circle ceiling never offered above; every change rendered in the log with both levels (AC-PERM-5 surface half) | PRD §4.6.3; GRT-01 | app + e2e | 7C | green | **GREEN (7C C4/C6)**: the matrix behind adjust — per subject per domain, the ONE phrase module's words, current level checked; LOWER posts straight through, RAISE rides the `hc-step-up` cookie (the assign route's consumer pattern; three `rs/rd/rl` params, never a colon-joined triple — safeNext refuses ':', the r3 gate catch) consumed by `hc.set_grant` in-function; the care ceiling from the ONE tiers module offers NOTHING above itself and no other domain, the DB refusing regardless (live: `tests/hc/people.test.ts` incl. the ceiling refusal WITH a valid token and the hidden lower; `tests/routes/member-detail.test.ts`); the change renders in the log with BOTH levels (PPL-04's page) · e2e "adjust: a raise through step-up, a lower without; the care ceiling never offered above (PPL-02, AC-PERM-5)" ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) |
| PPL-03 | Revoke: sessions, the pre-revocation document URL, cached responses, object shares — each its own leg; the honest limit in the PRD's words; the channels this slice does not reach NAMED (AC-PPL-4 partial, said so) | PRD §4.6.3 | e2e | 7C | green | **GREEN (7C C4/C6), four channels and the rest NAMED**: e2e "revoke: the pre-revocation URL leg with the honest limit in the PRD's words (PPL-03, AC-PPL-4)" — the artifact URL fetched by Petra (a dedicated member) BEFORE the revocation over REAL promoted bytes, the removal through the EXISTING route wearing *"a file already downloaded to someone's device cannot be recalled"* in those words and naming the unreached channels, the SAME URL from HER live context the one 404, her next page load refused (sessions) — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5). Cached responses: the reading path answers `private, no-store` for itself (asserted in the leg); page pass-throughs carry the same from `proxy.ts` (`tests/app/proxy.test.ts` — the dev server rewrites page headers after the proxy, so the page half is the unit pin plus the prod default, said honestly). Object shares: DOC-04's unshare leg + GRT-02's pgTAP. Background jobs / notifications / exports: NOT REACHED, said on the screen — RLS-11b/DEL-01 hold them · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** **Q-D fold-in.** `proxy.ts:30` returns before the `private, no-store` stamp at `:67` when `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` are unset — the one pass-through D7's *"every pass-through"* does not cover. The existing negative-case test exercises exactly that branch and asserts no header, and `playwright.config.ts` sets both vars unconditionally, so no gate run can reach it (R5/F-2). Not a live disclosure risk — the app is not functional enough in that state to serve a coherent sensitive page. **THE STAMP LANDED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021` (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override)**: `proxy.ts` now carries ONE `noStore()` and BOTH returns go through it, so D7's *"every pass-through is `private, no-store`"* is a property of the code rather than of remembering to repeat a line, and the missing-env early return is stamped like any other pass-through. Pinned by the negative-case test that already stood over that branch and asserted only the status and the absent call (R5/F-2, Q-D RATIFIED) |
| PPL-04 | The access log rendered and printable; filtered by the reader's access by construction; denials never name the object (AC-PPL-5 surface half, AC-PPL-7) | PRD §4.6.5; LOG-01/02 | app + e2e | 7C | green | **GREEN (7C C5/C6)**: every entry a sentence — who did what, to whom, on which subject, in which domain, when, with both levels where the event carries them; the filter IS `access_log_select` (the surface adds nothing and subtracts nothing — `lib/hc/people#accessLog` orders what the policy decided; live both ways: the health grant entry is not Marisol's to see); a denial renders its collapsed count and NEVER an object name (the entry cannot carry one and the surface does not invent one); PRINTABLE — the `@media print` sheet hides the chrome and adds nothing, the printed projection the SAME filtered read (`tests/routes/access-log.test.ts`, `tests/hc/people.test.ts`) · e2e "the access log rendered and printed (PPL-04, AC-PPL-5/7)" incl. the print-media pass ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** *"the surface adds nothing and subtracts nothing"* is FALSE — the log is `limit 300` with no cursor, no count and no disclosure, on a page whose lead says *"Everything done with the record"* and *"it prints exactly the entries below"*; `seq` 1 is §7.5's custodianship declaration and is the FIRST row dropped, invisible from the surface that shows it (R4/F-3 → **OW-26**). The AC-PPL-7 clause has no discriminating evidence (R4/F-8 with R6/F-10), and the print check lacks its control — `isVisible()` returns false for a NON-EXISTENT element as readily as a hidden one (R6/F-10). **RE-GREENED at the 7E/7D close-out (2026-09-02), inside the COMPLETE 58/58 gate run at `bb40021`** (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override): the log no longer claims what it does not show. It reads `LOG_WINDOW + 1` and renders `LOG_WINDOW`, so it KNOWS whether it is showing everything: inside the window the promise stands unqualified, and past it the lead paragraph names what is missing — *"Older entries — including the day this record was set up, and who was named its custodian — are not shown here yet, and a printed copy carries the same 300."* The disclosure is the LEAD PARAGRAPH, not chrome, and the print block hides only `.left-nav/.topbar/.back-link/button/.record-controls` — asserted, so a printed copy carries the caveat (R4/F-3 remedy (b)). Seven denials are seeded through `hc.log_denied` and the whole sentence is read ANCHORED, so an object name inserted anywhere in it fails the leg (R4/F-8, R6/F-10); the print check gained its CONTROL, the nav asserted visible on screen before print is emulated. **AC-PPL-7's app-layer evidence is the PAIR** — unit and e2e — and neither half alone discharges it. **The cursor is NOT here and is OWED: OW-26, home slice 8** |
| PPL-05 | Contribution as plain counts — no chart, bar or percentage anywhere on the surface, asserted over the rendered tree (AC-PPL-6) | PRD §4.6.4; design spec §7 | app | 7C | green | **GREEN (7C C4)**: owns-now as a list, completed as a count, last-active as a date or the honest words ("Hasn't been active yet." — null, never a fake date); the RENDERED TREE asserted to hold no `%`, no `<progress>`, no `<svg>`, nothing chart-shaped (`tests/routes/member-detail.test.ts`); the counts are RLS-true — over what the READER may see, the only honest count a filtered surface can show (`tests/hc/people.test.ts` live). 7A M2's completed fact stands (067:9, 12) |
| NAV-01 | Nav composition follows access per tier (caregiver: Tasks · Account; summary family: Timeline · Documents · People · Account); hiding is a courtesy, the URL constructed by hand is refused by the gate | PRD §4.0 | app + e2e | 7B/7C | green | **GREEN — the 7C COMPOSITION half (C3/C6)**: `navFor(tier)` in the manifest module (the layout hands the client nav the TIER, never the entries — a function cannot cross the RSC boundary, the r1 gate catch), the three ruled compositions pinned (`tests/routes/people.test.ts`) with an unknown tier falling OPEN (hiding is a courtesy; a failed read must never hide a surface someone is entitled to) · e2e "nav follows access — a caregiver's nav is Tasks · Account, a family member's is Timeline · Documents · People · Account; the hand-built URL is refused regardless (NAV-01)" — Marisol's and Dan's live navs asserted EXACTLY, and Marisol's hand-built adjust URL the one 404 — ran inside the COMPLETE 57/57 gate run at `ccd854b` (r5) · **7B HALF GREEN (B4)**: "the URL constructed by hand is refused by the gate" — from the caregiver's live context the original behind her instruction is 404 by URL (the cross-taint leg), and after take-back the shared task is 404 by URL (the unassign leg); the tier-aware COMPOSITION of the nav (caregiver: Tasks · Account) is 7C C6's, unchanged at 7B (the plan's B4 row: "Nav: no change"). The e2e halves were observed inside COMPLETE gate runs 3 and 4 at 18fbdba (43/45 twice, zero product assertion failures across four runs; the full record and its bound: ADR-0035 D11, put to round 26 as Q-H); nothing under app/ or lib/ moved between 18fbdba and 716cd49 |
| UXA-04 | The People & roles sentences, the revocation copy and the re-categorisation confirmation read at round 27 | §8.6; PRD §4.6.1, §4.6.3, §4.3.2 | review | 7C | green | read at round 27 (round 25 went to ADR-0034). 7A ships no member-facing string · **AMENDED BY RULING (ADR-0038 D4, round 27 — RATIFIED 2026-08-31):** **READ AT ROUND 27 (ADR-0038 D2, RATIFIED).** Faithful to the PRD's words at every enumerated home — §7.5's framing verbatim in shape with *"authority"* on no surface; the honest limit in the PRD's exact words with the unreached channels named; §4.3.2's move sentence with names and both directions, explicit confirmation and an honest way out; §4.3.5's share rules in one sentence; §6.9's character-exact label through the ONE component; §8.6 across the set. **The row FLIPS when R4/F-3 item 1 lands** — the log's *"Everything … prints exactly the entries below"* disclosed over the 300-row cap, or the cap removed. **THE CONDITION IS DISCHARGED AND THE ROW FLIPS at the 7E/7D close-out (2026-09-02).** ADR-0038 D2 set the terms exactly — *"the flip takes effect at the head where the disclosure lands green"* — and this is that head: the log's lead paragraph now discloses its 300-row window and names what is missing (the day the record was set up, and who was named its custodian), the printed projection carries the same caveat, and it is green inside the COMPLETE 58/58 gate run at `bb40021` (0 failed · 0 flaky · 0 skipped · 1766 s; `.gate/e2e-run.json`, config-borne, no CLI override). The read itself was already complete at round 27 and is unchanged. **`pending` never counted as green, and this row did not move until the copy did.** Read items 2–4 (the `'named at setup'` hedge, the raw `risk_class` token, the limit sentence's dropped "presence") are observations, not work |
| LOG-03 | **The accepted exposure (Q2 SETTLED)**: the `artifact_read` entry's commit round-trip — one round-trip's worth of refused reads may be recorded as reads, over-reporting a read and never under-reporting one; **ACCEPTED at the slice-7 gate, no DDL, M6 unconsumed** (ADR-0027 D17 item 4, D22 item 6; OW-04) | slice-7 plan Q2; ADR-0027 | ruling | 7A | **pending** | **ACCEPTED RISK — never green.** An exposure row, not a promise: the log errs toward showing a read a family member did not complete, the safe direction for a log whose purpose is to show who saw what. Closes only by the DDL exit the owner declined (a two-phase `artifact_read` entry), which would be a fresh ruling |

**The 7A bound, stated exactly (Q2).** **FOUR migrations of ≤ 6** — M1
`task_assignment` · M2 `task_lifecycle` · M3 `document_audience` · M4
`record_reads`. **M5 stays RESERVED** for the round-24 dispositions (the
standing precedent since 2A) and **M6 closes UNCONSUMED** because Q2 ruled
the one-round-trip window ACCEPTED (LOG-03 above), so the bound is expected
to close at **5 of ≤ 6**. The tree moves **69 → 73 migrations / 65 → 69
pgTAP files / 49 → 53 concurrency cases**. **ZERO dependencies were added**,
runtime or dev, as ruled. `documents.summary_text` stays at `summary`.

**One column, one constraint, one trigger body.** `tasks.written_from_task_id`
(circle-consistent FK, indexed, the pair with `written_for_member_id` a
CHECK) carries path 1's "from a task she can't see"; the claim machinery is
widened by exactly the instruction's shape — a second INSERT policy and one
branch in `hc.assert_claimed`, re-created WITH the SECURITY DEFINER that
`20260815230010` had ALTERed onto it after 002 caught its absence. No
shipped migration was edited.

## 8 — search, and the ruled intake (8A: the database increment, Tier 1, round 28 · 8B: Search, Tier 2, round 29 · 8C: claim's surface + the log's cursor, Tier 2, round 30 — slice-8 plan Q1–Q7 SETTLED 2026-09-02; PRD §4.5.1, §4.7; TSD §7.2, §7.7; ADR-0036 Q-D; ADR-0038 D3, D6)

**Written by the 8A build's FIRST commit, docs-only, quoting the plan's
*Owner decisions*** — consequence 2: *"Thirteen coverage rows are ruled open
and NONE is written by this PR … The 8A build writes them in its FIRST commit,
docs-only, quoting this section (M1 stays the first unit): TSK-05, STP-03,
SRCH-03/04/05/06 and LOG-04 `pending` tagged to their increments; A11Y-12
`pending` tagged 8; GRP-01 `pending` tagged 6C; DEP-01, EXE-01, EXE-02 and
BND-01 `pending` at `gate` — all under a new `## 8 — search, and the ruled
intake` section."* **Every row below opens `pending`; nothing turns green
here.** TSK-05 and STP-03 flip at the pgTAP layer at the 8A head, never early;
the four `gate` rows are `docs/owed.md`'s Q6 `PROMOTED` exits and are never
green in this slice (Q6: *"Each becomes a `pending` row with Slice cell
`gate` (the G12-01 shape), never green in this slice"*). GRP-01 is Q3(c)'s
*"the row that did not exist at the gate"*. Nothing here activates anything
(consequence 5).

**At the 8A head `4d166c0` (2026-09-03, ADR-0040): TSK-05 and STP-03 flip at the
pgTAP layer only** — inside the clean-leg run (reset exact 76; 71 files Σ 1,863;
concurrency 83/83; `db:verify` clean; the upgrade leg 74 → 76 green; the browser
gate 58/58 in 8 files, 1,284 s — 0 unexpected · 0 flaky · 0 skipped). The other eleven rows stay `pending` exactly as opened. TSK-05's
app and e2e halves are 8C's; STP-03's app half is built and recorded in its cell,
not claimed (the round's Q-F).

**At the 8B head `3bd8f52` (2026-09-03, ADR-0041): SRCH-03, SRCH-04, SRCH-05, SRCH-06 and A11Y-12 flip green** — on legs inside the COMPLETE 64/64 gate run at `3bd8f52` (1972 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override); vitest 1508/105 by run; lint / typecheck / production build solo, exit 0; gitleaks 661 commits, no leaks; the page p95 273 ms and prf06's scan legs re-run twice (ADR-0041 D9) — **M4 UNCONSUMED, the bound at 2 of ≤ 4; no DDL moved, so the DB legs stand at 8A's figures.** The other eight rows stay exactly as they were: TSK-05 and STP-03 at the pgTAP layer (8C), LOG-04 (8C), GRP-01 (6C), the four `gate` rows.

**At the 8C head `2f2c509` (2026-09-03, ADR-0042): TSK-05's APP and E2E halves and LOG-04 flip green** — on legs inside the COMPLETE 66/66 gate run at `2f2c509` (9 files, 1,228 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override, the tally read from the JSON reporter); vitest 1,563/106 by run; lint / typecheck / production build solo, exit 0; gitleaks 668 commits, no leaks. **NO DDL: M3 and M4 both close UNCONSUMED and the migration bound closes at 2 of ≤ 4**, so the DB legs stand at 8A's `4d166c0` figures (reset exact 76 · pgTAP 71 Σ 1,863 · concurrency 83/83 · `db:verify` clean). **LOG-01's app half is AMENDED WITH A MARKER pointing here, never rewritten** (below), and `docs/owed.md`'s **OW-26 goes `TAKEN(8C/unit 2)` → `CLOSED(2f2c509)`**, the ledger at OPEN 0 / 25. **Q5 held: no new a11y row for 8C** — the claim control and its route are a CITATION inside the two task entries of `e2e/audit-manifest.ts`, and the submit route has no `page.tsx` so it never enters that pin. STP-03's app half is still recorded and not claimed (round 28's Q-F, unheld); GRP-01 and the four `gate` rows do not move. **Rounds 28 and 29 remain unheld and ADR-0040/0041 unstamped.**

**At slice 8's CLOSE-OUT (2026-09-03, ADR-0043 — docs-only, no gate re-run, no product evidence re-earned): rounds 28, 29 and 30 are ruled at once and ADR-0040, ADR-0041 and ADR-0042 are all stamped `accepted`.** Twenty-one pointed questions, the table in `docs/review/slice-8-dispositions.md`; eighteen ruled as recommended, three departing (28/Q-A, 30/Q-F, 30/Q-G). **NOTHING TURNS GREEN BECAUSE A QUESTION WAS RULED.** The tally moves by exactly one row and that row opens `pending`: **280 → 281 rows · green 258 → 258 (unchanged) · review 9 → 9 · pending 13 → 14**, counted with `tests/lint/process.test.ts`'s own parser, never by eye. The one new row is **FRZ-17**, the write half of FRZ-13's read-only carve-out — `hc.claim_task` admits at `view` and the carve-out caps a non-objected-to coordinator AT `view` under an `unresolved` freeze, so she can claim while the circle is frozen (ADR-0043 D2; `docs/owed.md` **OW-27** `OPEN`, the ledger 0 → **1 / 25**; the fix is DDL in a NAMED M-slot of slice 9's bound). Four cells are **AMENDED WITH MARKERS, never rewritten**: **TSK-05** (its *"a FROZEN circle … refused in ONE shape"* narrowed to an **OPEN** freeze, the row staying green on the pgTAP evidence it has), **FRZ-13** (a pointer to FRZ-17), **STP-03** (the app half FLIPPED on round 28 Q-F, on evidence already at `4d166c0` — **the status word does not move**, the row having been green on its pgTAP half already), and **SRCH-04** (leg-audit F3's narrowing ruled into the cell: the browser leg renders THREE of §4.7.3's four strings; the merged leg's title is left standing, a code change being out of a docs-only session's reach). **The migration bound CLOSES at 2 of ≤ 4** — M3 and M4 both UNCONSUMED. A stamp records that the questions were RULED; **none of the three rounds was held**, and ADR-0043 D6 files the dissent that says so and recommends a commissioned adversarial pass over 8A's M1 and M2 at slice 9's plan gate.

| ID | Assertion | Source | Layer | Slice | Status | Test |
|---|---|---|---|---|---|---|
| TSK-05 | `hc.claim_task`: a `view`-level member takes an **unassigned, open** task for herself and no other; `summary`, an owned task, a non-reader and a frozen circle refused in ONE shape; the claim creates no share and no written instruction; `task_claimed` names the claimant as actor (AC-TASK-1 claim half, AC-TASK-2) | AC-TASK-1, AC-TASK-2; PRD §4.5.1; ADR-0036 Q-D; plan Q2 | pgTAP + app + e2e | 8A/8C | green | **pgTAP half GREEN (8A M1, `20260903120001`)**: a `view`-level member takes an unassigned open task for herself and the answer names her member row (070:6–7); `summary` (070:16), an owned task — even hers (070:13–14, 26), a non-reader by ladder and by context, each as a PAIR with the same person's claim beside it (070:17–20), the care ceiling with the named-share pair (070:21–22), done, an instruction row, a nonexistent id, a stranger (070:27–30) and a FROZEN circle (070:32–35) refused in ONE shape, eleven refusals joined OUTSIDE the statement (070:31); no share and no instruction by ANY path, asserted as SET EQUALITY before/after (070:10–11, 23–24, 36–38); `task_claimed` names the claimant as actor and target and nothing else is logged (070:8–9); the AI role holds no EXECUTE, catalog-based (070:3); a claimed task is a handed task to `assign_task` and `complete_task` (070:39–40). Concurrency 55: two claimants at once — one owner, one entry, no share, no instruction. Inside the clean-leg run at `4d166c0` (71 files Σ 1,863; 83/83). **APP AND E2E HALVES GREEN (8C U1, `2f2c509`, ADR-0042 D1–D5)**: `TaskRow.can_view` is the definer's OWN `hc.visible_at(…) >= 'view'` call — same ctx, same taint, the owner AS THE ROW STANDS — computed as one more expression in the RLS-true join that already carried `can_manage`, so the surface asks no second question and cannot disagree with the function that decides; `mayClaim` is `claim_task`'s five gates in its order and NO SIXTH (a surface refusing more than the function hides work she is entitled to take). Driven three ways: as a table, over the RENDERED TREE (the control offered on an unassigned open readable task and withheld for hers-already, someone else's, done, cancelled, an instruction row, below view, and no membership), and LIVE against the definer as an AGREEMENT in both directions (`tests/hc/tasks.test.ts` — `can_view` true with `can_manage` FALSE, a `view` member claims, a `summary` member is refused with nothing offered, an instruction row never claimable, an outsider refused). The claim writes NOTHING else: every share and every instruction row in the circle snapshotted before and after and asserted by SET EQUALITY (the app-half echo of ADR-0040 D3), the only entry `task_claimed` with the claimant as actor AND target. `POST /[circle]/tasks/[task]/claim/submit` inside `withRouteBudget` reads NO form — the definer takes one argument and a field here would be the beginning of the path D3 pins shut — with one marker back and never a 500. The refusal NAMES what the page can see (D4: already yours / *Ruth took that on first.* / marked done / closed), the generic sentence the LAST arm. `task_claimed` gains its own log sentence, the claimant named ONCE (ADR-0040 Q-G answered by building it; the generic renderer printed *"Marisol · task claimed · Marisol"*). Both surfaces in `RECORD_SURFACES`, the route in `page-gate.test.ts` (17 form routes, was 16) · e2e "claim: a view-level member takes an unassigned task from her own screen, and no control is offered where the function would refuse (TSK-05, AC-TASK-1/2)" — 47.1 s, from Dan's own context at `view`, the caregiver offered nothing and her hand-built POST refused with the holder unmoved — inside the COMPLETE 66/66 gate run at `2f2c509` (9 files, 1,228 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override, the tally read from the JSON reporter). **The pgTAP half is 8A's and is NOT re-earned here.** Narrowings, declared: the leg establishes `view` BY FIXTURE and the `view`-vs-`manage` discrimination lives in the live unit leg; three of the five refusal shapes are proven over the rendered tree and against the live definer rather than in a browser (ADR-0042 Q-D/Q-E, leg audit F4/F5) · **AMENDED BY MARKER (ADR-0043 D2, slice 8's close-out — RULED 2026-09-03; nothing above is rewritten and this row does not move): *"a FROZEN circle … refused in ONE shape"* is proven for an OPEN freeze ONLY.** `070_task_claim.sql` opens its freeze with `insert into public.freezes (circle_id) values (…)` and `state` defaults to `'open'` (`20260815200005`:20), so 070:32–35 is an **open** freeze throughout and FRZ-13's **unresolved** carve-out path is exercised nowhere for this function. Under that carve-out a coordinator who is not the objected-to member resolves to `view` — this function's floor — and the claim is **not** refused; round 30 Q-F rules that a defect, not an allowance, because the carve-out is read-only by intent (ADR-0043 D2). **The row stays green on the pgTAP evidence it actually has**, and everything above it stands; the gap is **FRZ-17** with **OW-27**, and the fix is DDL in a NAMED M-slot of slice 9's bound, never a docs-session edit |
| STP-03 | *(Q3(a) SETTLED 2026-09-02: D6 item 2 TAKEN — the row exists)* Step-up binds the **level** as well as `member:subject:domain`: a token minted for `summary` does not consume against a post of `manage` for the same triple | ADR-0038 D6 item 2; round-27 R3 dissent 1; plan Q3(a) | pgTAP + app | 8A | green | **pgTAP half GREEN (8A M2, `20260903120002` — the consumed reserve, Q3(a))**: a token minted for `summary` does not consume against a post of `manage` for the same triple (071:4), is left UNCONSUMED (071:5), the grant and the log untouched (071:6); the same token raises to `summary` (071:7–8, 14); the pre-8A three-part shape no longer raises — the binding is REPLACED, not widened (071:9); `view` cannot buy `manage` (071:10); the stored row carries the four parts verbatim (071:12); a LOWER still needs no token (071:13); the replaced body's surface from the catalog (071:1–3). 038's five raise cases, 041, and concurrency 29/31 re-pinned in the same commit. Inside the clean-leg run at `4d166c0`. **App half BUILT and recorded, NOT claimed as the flip (ADR-0040 D6, Q-F)**: the mint site composes the four parts and the route confirms against them (`tests/routes/member-detail.test.ts` 28/28 — the two positive pins that went red; `tests/hc/people.test.ts` live 16/16), and the PPL-02 leg drove a raise through it inside the complete gate at `4d166c0` (58/58 in 8 files, 1,284 s — 0 unexpected · 0 flaky · 0 skipped) — the round rules whether that is the app half's flip · **APP HALF FLIPPED at slice 8's close-out (ADR-0043 D1, round 28 Q-F — RULED 2026-09-03; nothing above is rewritten).** The round rules that it **is** the app half's flip, on the evidence already named above and already standing at a declared, merged head: `tests/routes/member-detail.test.ts` 28/28 (the two positive pins that went red, the four negatives), `tests/hc/people.test.ts` live 16/16, and the PPL-02 leg driving a raise through the mint site inside the COMPLETE 58/58 gate run at `4d166c0` — the same standard every other app half in this file was flipped on. **The status word does NOT move: the row was already green on its pgTAP half, so nothing turns green because a question was ruled.** What changes is that the `app` half of the declared `pgTAP + app` layer is now CLAIMED rather than only recorded — the honest state for a row that was green with half its layer unclaimed |
| SRCH-03 | The surface's leakproofness: ONE RLS-true read per relation, no second code path; a `summary` member's **body-only** term returns the same rendered shape as a term present nowhere, driven from a LIVE context; a share widens the one named object through search and never an object derived from it; the care-circle ceiling holds in search (AC-DOC-4, AC-PERM-6, AC-TASK-5) | AC-DOC-4, AC-PERM-6, AC-TASK-5; TSD §7.7; PRD §4.7 | app + e2e | 8B | green | **GREEN (8B U1/U4)**: ONE RLS-true read per relation inside ONE `withRequestRole` (`lib/hc/search#searchRecord` — TSD §7.2 verbatim in FROM/WHERE/ORDER/LIMIT, the LEFT JOIN the level decision RLS makes; `tests/lint/search-surface-fence.test.ts` counts the call exactly once and `count(` never). Live, `tests/hc/search.test.ts` 22/22: a `summary` member's body-only term and a term present nowhere answer the SAME empty shape (`toEqual`), her title term's snippet cut from title + summary and never a word of the body; the OCR-only term too; a share widens ONE document (to view — the body term becomes hers) and never the task derived from it nor the sibling; the caregiver's assigned task and nothing else (AC-TASK-5); a non-member the empty shape; the access log unchanged across every read · e2e "search leak: at summary a body-only term renders the SAME shape as a term present nowhere; a title term finds the document with a snippet cut from title + summary (SRCH-03, AC-DOC-4)" from HER live context — the coordinator's positive control finds the body term FIRST, then `main`'s markup for the body-only term equals the markup for a nonsense term with the term normalised out, both the empty copy and no group; "search after a share: the one named document widens, never the task derived from it nor the sibling (SRCH-03, AC-PERM-6)"; "search for the caregiver: her assigned task and nothing else — the field renders outside the nav's courtesy (SRCH-03, AC-TASK-5)" — inside the COMPLETE 64/64 gate run at `3bd8f52` (1972 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override). ADR-0041 D1, D2, D7 |
| SRCH-04 | The results' shape and copy: grouped by kind, **labelled by subject**, each link resolves to the object; §4.7.3's four strings verbatim; **no total, no count of withheld results, no autocomplete, no spelling correction, no prose answer and no composition across results** — asserted as ABSENCES over the rendered tree (AC-HOME-4, AC-DOC-1's search half, AC-TL-1's *"through search"*) | AC-HOME-4, AC-DOC-1, AC-TL-1; PRD §4.7.3 | app + e2e | 8B | green | **GREEN (8B U2/U3/U4)**: three headed groups in §4.7.3's order, every row a `SubjectLabel` by name and a link to the object (`tests/routes/search.test.ts` 14/14; the copy leg GETs every result href and asserts 200); the four strings verbatim — `Search Nell's record` / `Search the record` from the WIDENED `myMembership` (`placeholderFor`; `tests/design/search-field.test.tsx` 10/10 incl. the failed-read and older-shape cases), *Nothing matching that, in what you can see.*, *Find documents, dates and tasks.* as the field's description; the ABSENCES over the rendered tree — no total, no "showing N of M", no count of withheld results, no field of the page's own, no autocomplete attribute, no datalist, no listbox/combobox, no "I", an empty group rendering NOTHING — asserted in the page test, the field test, the fence, and live in e2e "search copy and bounds: the four §4.7.3 strings verbatim; an over-cap term is refused with the empty copy, never an error; no total, no autocomplete, no suggestion list (SRCH-04, SRCH-06)" (`main`'s children exactly a header and headed sections) — inside the COMPLETE 64/64 gate run at `3bd8f52` (1972 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override). ADR-0041 D3, D5 · **AMENDED BY MARKER (ADR-0043 D4, slice 8's close-out — RULED 2026-09-03; nothing above is rewritten and this row does not move): the BROWSER leg renders THREE of §4.7.3's four strings, not four.** ADR-0041 declared the narrowing in its *"What is NOT claimed"* — *"a two-subject placeholder driven in a browser (vitest and the live module test carry it; the e2e circle has one subject)"* — and `docs/review/8c-leg-audit.md` **F3** found the leg's TITLE, *"the four §4.7.3 strings verbatim"*, contradicting the ADR beneath it. Round 30 Q-G rules the narrowing **into this cell** and leaves the merged leg's title standing, a title change being a code change plus a gate run that a docs-only close-out cannot produce (`AUDIT_MANIFEST` cites the leg by title). So: **`Search the record`, the two-subject placeholder, is unreachable from that leg — its fixture circle has ONE subject — and is proven at the UNIT layer in four places**: `tests/design/search-field.test.tsx`'s two-subject, failed-read and older-shape cases, and `tests/hc/search.test.ts`'s `placeholderFor`. The other three strings are verbatim in the browser, and the row is fully covered across its two layers. **This cell, not the leg title, is what a reviewer is bound by.** The audit's recommended title — *"three of the four §4.7.3 strings verbatim (the two-subject placeholder at the unit layer)"* — is recorded here for the next increment that touches `e2e/search.spec.ts` |
| SRCH-05 | The snippet is cut from the matched text and reaches the DOM as **structure, never markup**: explicit `ts_headline` sentinels, split in the module, `<mark>` built by React; **no `dangerouslySetInnerHTML` anywhere on the surface**, fence-tested; `ocr_text` findable at weight D and never outranking a title | TSD §7.2 (the Q4(1) NAMED DEPARTURE; erratum at round-29 sign-off); plan Q4 | app + e2e | 8B | green | **GREEN (8B U1/U3/U4)**: `ts_headline`'s fourth argument `StartSel=U+0002, StopSel=U+0003` (probed on the stack before a line was written: the sentinels byte-exact, junk terms raising nothing); `splitHeadline` to PARTS in the module (unbalanced → plain text; a document's own `<b>`/`<script>` stays text); `<mark>` built by React (the page test renders `<script>` inside a part as escaped characters); `tests/lint/search-surface-fence.test.ts` — the fence WAS the surface's first commit (`f75d481`) — pins `dangerouslySetInnerHTML` absent from the three surface files AND the product tree's set of such sites as an EXACT SET (the one pre-8B timezone probe); live: the view member's hit part is exactly the term, `warfarin` present only in `ocr_text` is found at weight D and its `rank` is BELOW the title-weight document's · e2e "search at view: the body snippet marks the term as <mark> structure and the OCR text is findable at weight D, never above a title (SRCH-05)" (`.search-snippet mark` count 1 with the term; the snippet's markup carries no `<b>` and no escaped sentinel; the OCR document second) — inside the COMPLETE 64/64 gate run at `3bd8f52` (1972 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override). The §7.2 erratum (the options string, and the title column — ADR-0041 Q-A) lands at round-29 sign-off |
| SRCH-06 | Latency and bounds: `q` capped at ingress and refused with the empty-result copy; the three reads inside ONE `AnswerBudget` with the overrun rendering the honest slow answer; a **measured** page p95 recorded at the 8B head against PRD §13.2, and the `prf06.mjs` scan legs re-run against the 8B fixture (§7.7) | PRD §13.2; TSD §7.7; plan Q4(4); PRF-06's breach clause (M4 NAMED) | app + bench | 8B | green | **GREEN (8B U3/U5)**: `boundQuery` caps `q` at 200 AT INGRESS — an over-cap term is refused with the empty copy and the module is never called (`tests/routes/search.test.ts`; e2e: status 200 and the copy; the live test: no read, the log unchanged); ONE `withPageBudget` around the ONE module call (`tests/lint/answer-budget.test.ts`'s exact set gained the tree and the page; the overrun case drives the REAL `AnswerBudgetExceeded` and renders "taking longer than usual … try again" to this search's URL); **MEASURED at the 8B head** (`scripts/bench/search-p95.mjs`, the production build under `next start`, a signed-in founder, 150 warm requests over 5 terms): **p95 273 ms** — p50 132 · p99 380 · max 468 — against §13.2's 800 ms, the 2 s ceiling held at every request; `prf06.mjs`'s scan legs re-run TWICE at this head — every search leg PASS in both (`search_broad` mv 1,886 → 853 ms vs 2,500; the 8B-head tables in ADR-0041 D9) — **so M4 closes UNCONSUMED**; the 250 ms PAGE tripwire flickered on a different page leg each run (571 → 197 ms; 52 → 270 ms), the round-8 pattern, recorded for the round (ADR-0041 Q-G) |
| A11Y-12 | The search surface audited: the field labelled and reachable, results as headed groups, emphasis not conveyed by colour alone, 390 px and keyboard | §11.2 G12; plan Q5 | e2e | 8 | green | **GREEN (8B U4)**: built INTO the surface — a bound visually-hidden `<label>`, the `role="search"` landmark, `input[type='search']` on the shared 44 px rule, headed `section[aria-labelledby]` groups, `.search-snippet mark` at weight 600 with an underline beside the wash (never colour alone, §8.7) · e2e "A11Y-12: the search field labelled and keyboard-reachable, results as headed groups, emphasis as <mark> not colour alone, at 390px" — from a 390 px context: Tab from the page top reaches `getByRole('searchbox', { name: 'Search' })`, Enter submits the GET; every section's `h2` is its label; the first `mark`'s computed weight ≥ 600 and decoration underline; axe clean (the named `CONTRAST_EXEMPT`) on the results AND on the empty state; the field and every result link ≥ 44 px; no horizontal scroll — inside the COMPLETE 64/64 gate run at `3bd8f52` (1972 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override). The leg lives in `e2e/search.spec.ts` (its fixtures are there), not `e2e/a11y.spec.ts` as this cell first said; `AUDIT_MANIFEST` cites it by title (ADR-0041 D8) |
| LOG-04 | The access log reaches **every** entry the reader may see: a `seq` cursor, the printed projection reaching the same set, `seq` 1 reachable on a circle past 300 rows (OW-26; LOG-01's app half amended to point here, never rewritten) | OW-26 (`CLOSED(2f2c509)`); ADR-0038 D3 (R4/F-3 remedy (a)); plan Q3(b) | app + e2e | 8C | green | **GREEN (8C U2, `2f2c509`, ADR-0042 D7–D10)**: `lib/hc/people#accessLog` gains a `before` cursor — a `seq` to read STRICTLY BACK from, applied INSIDE the policy-filtered read and never over it. Three properties make the walk trustworthy and each is asserted live on a circle driven to **320 entries past the window**: `seq` is per-circle, gapless and UNIQUE (`unique (circle_id, seq)`), so `seq < before` under `order by seq desc` is a total order with no row twice and none skipped; **the walk EQUALS the policy's single read** — paged at 100, ordered, duplicate-free, against one 500-row read (*"the WALK and the policy agree exactly: paging adds nothing and subtracts nothing"*) — and the caregiver's walk is a STRICT SUBSET still carrying no health `grant_changed` entry, so a cursor cannot widen what a policy narrowed (LOG-01 held page by page); a cursor past the end is the EMPTY page, which is how a walk terminates rather than wrapping. **`seq` 1 — §7.5's custodianship declaration — is REACHABLE**, asserted by walking to it and by reading it back at `before = 2`. A malformed cursor is the first page, never an error (six spellings incl. `1e9999` and `12; drop table`). §7.4 holds and is ASSERTED: what the page says about itself **contains no digit** — no page number, no total, and *"the most recent 300"* is gone, the window size having stopped being a ceiling. Four honest lead states; *"Everything done with the record"* survives ONLY on a single page with nothing behind it. The printed projection IS the page: `.log-pager` joins the print-hidden chrome (a printed link is a dead link) and `.log-disclosure` does not, both asserted with their CONTROL first (R6/F-10) · e2e "the access log reaches every entry: the cursor walks past 300 rows to the custodianship declaration, and each page prints itself (LOG-04, AC-PPL-5)" — 7.8 s, arrived at by CLICKING from People & roles, pressing *Older entries* like a person, refusing to revisit a URL, requiring every page to carry rows — inside the COMPLETE 66/66 gate run at `2f2c509` (9 files, 1,228 s — 0 unexpected · 0 flaky · 0 skipped; `.gate/e2e-run.json`, config-borne, no CLI override, the tally read from the JSON reporter). **The pair is the evidence**: the browser half proves REACHABILITY and termination, the live half proves EXHAUSTIVENESS, and neither alone discharges this row (leg audit F7). Narrowing, declared: no "Newer" link — a backward cursor would need a second ordering to duplicate what Back already does (ADR-0042 Q-C) |
| GRP-01 | *(Q3(c) SETTLED 2026-09-02: opened `pending` — the row that did not exist at the gate)* Multi-attachment group review: N children reviewed as one flow with one receipt; partial states presented honestly and blocking on none (AC-INBOX-5, AC-INBOX-13) | AC-INBOX-5, AC-INBOX-13; plan Q3(c) | app + e2e | **6C** | **pending** — never green on the arrival shape alone | DEFERRED with a named home: a Care Inbox increment (6C); a third slip is visible here |
| DEP-01 | Before G4 passes — invite sending is the first production act — a hosted runtime has been observed under an auth-server fault and the observation recorded: date, runtime, what the page gate did | OW-09 (`PROMOTED(DEP-01)`, plan Q6); ADR-0027 D17 item 8 (F-2) | deploy | gate | **pending** | G4's entry set, ADDED by the Q6 ruling: *"its only instrument is a deployment; an `OPEN` row cannot hold a fact no slice can produce"* |
| EXE-01 | Before G7 passes — no real forwarding address activates — the harness ADR-0028 D13 names exists (a concurrent in-process render + OCR overlapping an authenticated artifact read) and the `HopCost` ledger's first live report is recorded | OW-14 (`PROMOTED(EXE-01)`, plan Q6); ADR-0028 D8 item 5a, D13 | harness | gate | **pending** | G7's entry set, the instrument — OW-10's stated prerequisite, so it is its own row |
| EXE-02 | Before G7 passes: §6.3 render and §6.9 OCR run off the request process; a heartbeat across the whole request window reports stalls that do not overlap the deadline; leg 38 passes under D13's named load condition, its duration and outcome recorded per gate run from `.gate/e2e-run.json`, never re-run to green | OW-10, OW-12, OW-13 (`PROMOTED(EXE-02)`, plan Q6); ADR-0028 D8 items 1, 3, 5 | app + e2e | gate | **pending** | G7's entry set, the fix and its proof — one row because OW-12 *"lands with OW-10"* and OW-13 *"closes when OW-10 lands"*; leg 38's duration and outcome ride every slice-8 gate run's `.gate/e2e-run.json` at no cost |
| BND-01 | Before G7 passes: the four remaining unbounded outbound hops — `lib/mail/outbound.ts`, `postmark/route.ts`, `worker/relay/route.ts`, `worker/[stage]/route.ts` — each carry a time bound with a named test (the mail hop is slice 11's; the three fires ride EXE-02's increment), and `lib/storage/fetch.ts`'s comment states the measured 5/4 split | OW-08 (`PROMOTED(BND-01)`, plan Q6); ADR-0027 D17 item 7, D22 item 4 | routes + tests | gate | **pending** | G7's entry set; green only when all four hops are bounded — the first thing slice 11's plan gate reads |
| FRZ-17 | *(OPENED at slice 8's close-out, 2026-09-03 — ADR-0043 D2, ruling round 30 Q-F with round 28 Q-A)* **FRZ-13's carve-out is read-only at the WRITE definers too**: under an **`unresolved`** freeze, a coordinator who is not the objected-to member is capped at `view` and must be REFUSED by `hc.claim_task` — as she already is by `assign_task`, `complete_task` and `snooze_task` — with her READ through the carve-out still working | ADR-0043 D2; ADR-0042 Q-F; ADR-0040 D2 / round 28 Q-A; FRZ-13; `20260815230009`; `docs/owed.md` OW-27 | pgTAP | 8 → 9 | **pending** | **NEVER GREEN until the guard lands. A fix is DDL; the close-out ships none, and this row is the exposure it ships instead.** THE EXPOSURE: `hc.grant_vectors` gives a non-objected-to coordinator `frozen = false` and `cap = 'view'` under an `unresolved` freeze; `hc.visible_at` applies the cap as `least(…)`, so she resolves to `view`, which is exactly `hc.claim_task`'s admission floor (`20260903120001`) — **she can take a task while the circle is frozen.** RULED READ-ONLY BY INTENT on three independent sources: `20260815230009`'s own header (*"the unresolved **read-only** carve-out"*), FRZ-13's row (the same words), and the structure — the three sibling task-write definers each raise `freeze_active` from an explicit `state in ('open','unresolved')` test against `public.freezes` that never consults `hc.grant_vectors`, so *no write under any freeze* is written out three times. **BOUNDED TO ONE FUNCTION** (this narrowing is the close-out's, from the source): ADR-0042 flags `complete_task`'s `summary` holder bar as adjacent and unverified, but it is **never reached** — `complete_task` raises `freeze_active` before it calls `hc.may_act_on_task`, as do `snooze_task` and `assign_task`. `hc.claim_task` is the ONLY task-family write definer the carve-out reaches, and it is reachable because ADR-0040 D2 routed its freeze through `hc.visible_at` rung 2 alone. WHY NO TEST CAUGHT IT: `070_task_claim.sql` opens its freeze with the `state` default `'open'` (`20260815200005`:20), so 070:32–35 never exercises the `unresolved` path (TSK-05's cell is marked accordingly). WHAT BOUNDS IT TODAY: nothing is production-activated (G4/G7 block); the actor must already be a coordinator of that circle and not the objected-to member; the write is confined to the three assignment columns plus one `task_claimed` entry, with no share and no instruction row by any path (ADR-0040 D4, SET EQUALITY at 070:10–11, 23–24, 36–38); and it is reversible by `unassign_task`. It is bounded by **no test**, which is what this row carries. GREEN WHEN: `hc.claim_task` carries the same explicit `state in ('open','unresolved')` test its three siblings carry, in a NAMED M-slot of slice 9's migration bound, **and** a pgTAP case pins a carve-out coordinator refused under an `unresolved` freeze while her read through the carve-out still resolves. Not `RISK(row)` — an accepted risk is one nothing turns green, and this one is meant to be fixed |

**Rows that do NOT move at this commit** (the plan, *"Coverage rows to open"*):
RLS-11b, FRZ-16b, DEL-01, ADM-01, SIG-01 (still NOT absorbed, fifth slice
running), G12-01 (`gate`), UXA-03, LOG-03 (never green by ruling). SRCH-01,
SRCH-02, DSC-01, RLS-11a, PRF-04 and PRF-06 stay green at their own layers
and are NOT re-earned by this slice. **No row flips outside a ruling; pending
never counts as green.**
