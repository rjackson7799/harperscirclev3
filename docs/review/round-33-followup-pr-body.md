# Correct Home read failures, local-day selection and browser fixtures [DO NOT MERGE without owner sign-off]

Home could show an empty success message when reads failed, and date-only items could disappear from upcoming activity at noon UTC. The follow-up preserves operational failures as retry states, limits onboarding to known coordinators, and classifies dates by their recorded/subject local day. Floating or unrecognized-zone values remain available in the record without claiming a known past/future placement. No RLS, schema, auth-state or dependency changes.

The Home accessibility test now owns its synthetic circle instead of reusing data populated by earlier tests. Completion-to-Home navigation and the below-cliff inbox card are asserted in the browser walkthroughs. These browser assertions remain unrun.

The follow-up also adds the public Supabase CA to deployment tracing, excludes local artifacts from Vercel uploads, and makes non-loopback AI fixture-server binding require a credential. Local loopback fixture behavior remains keyless. The fixture-server change is local only; no additional service was provisioned.

Validation: fresh `npm run verify:local` passed on clean commit `0d93dbb6765062bf7c42a3088add5eba875852d0`: whole-tree lint, 1,621/1,621 application tests, and standard production build including TypeScript. This full run includes the final zone guard and supersedes the narrower prior verification. Build retains the tracing warning in unchanged OCR code; Vite also reports existing configuration/import warnings. The new guarded local command runs the checks sequentially with the existing single test worker. Staging dependencies and efficient validation are documented in `docs/ops/development-validation.md`.

Review findings: `docs/review/round-33-findings.md`. Delegated decisions: ADR-0048. Dispositions retain three OWED evidence closures under OW-34; OW-35 records the existing inbox-leg gap. HOME-05 is pending until fresh latency evidence; staging/browser and production gates remain unchanged. This is a locally prepared PR body: the branch has not been pushed and no follow-up PR has been opened.

Subsequent validation: the fixture access change at `1cf6db5` passed 64 affected tests, changed-file lint and whole-tree TypeScript. This is separate from the full application run above. The existing protected Vercel preview was built from `a8a4d45` and passed its remote build; later fixture changes are not deployed. HTTP checks exercised synthetic unverified signup, setup and coordinator Home, with inactive forwarding and a signed-out redirect. See `docs/ops/staging-setup-smoke.md` for the exact deployment, evidence and limitations. These checks do not establish browser, email delivery, pipeline or latency clearance.

Later staging evidence adds Care Circle/Family/removal HTTP checks and a controlled runtime-database outage with normal controls before/after; the temporary fault preview was deleted. A supported manual-entry workload with 2,021 events and 501 claimed tasks measured p95 982 ms over 150 valid HTTP responses. Benchmark tooling corrections at `7f1267c` passed seven isolated regressions, changed-file lint and whole-tree TypeScript. These results do not replace the historical full-suite run or close database-only, mixed-workload or browser gates.

Current execution order: `docs/development-plan.md`. Finish available database-performance and acceptance evidence, then consolidate the Home review packet. Browser troubleshooting and additional services are deferred under the owner's decisions. Completed-founder auth routing remains a separate future auth increment. Owner sign-off remains required for merge; production gates remain blocked.

Consolidated checkpoint: docs/review/round-33-followup-packet.md. At source head dfed830, actual Home wrappers measured p95 407–661 ms from Windows; the simple-query wrapper baseline was 414 ms. A separate PostgreSQL execution diagnostic measured upcomingEvents p95 297.249 ms, above 250 ms. This remains an open performance gate, not an authorized migration. See docs/ops/staging-home-database-performance.md. Browser and final merge-ready validation remain outstanding.
